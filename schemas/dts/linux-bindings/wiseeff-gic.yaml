%YAML 1.2
---
$schema: http://devicetree.org/meta-schemas/core.yaml#
$id: http://devicetree.org/schemas/vendor/wiseeff/wiseeff-gic.yaml#
title: wiseeff,gic
description: WiseEff vendor binding for wiseeff,gic (generated from vendor property schema).
maintainers:
  - WiseEff
allOf:
  - $ref: /schemas/device.yaml#
select:
  properties:
    compatible:
      contains:
        const: wiseeff,gic
  required:
    - compatible
properties:
  compatible:
    type: string
    pattern: ^wiseeff,gic$
  interrupt-controller: true
  '#interrupt-cells':
    $ref: /schemas/types.yaml#/definitions/uint32-array
required:
  - compatible
additionalProperties: false
unevaluatedProperties: false
