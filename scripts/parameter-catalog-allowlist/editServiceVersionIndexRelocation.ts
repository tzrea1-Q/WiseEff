/** Fixed #859 entry points; shared validation keeps its configuration private. */
export {
  editServiceVersionIndexRelocationRecordPath,
  applyReviewedEditServiceVersionIndexRelocation,
  verifyHistoricalEditServiceVersionIndexRelocation,
} from "./runtimeTopologyRelocation";
