global:
  resolve_timeout: 5m

route:
  receiver: local-dashboard
  group_by:
    - alertname
    - component
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-dashboard
