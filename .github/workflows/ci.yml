name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-and-test:
    name: Build and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Frontend tests
        run: npm test

      - name: Backend tests
        run: npm run test:server

      - name: M1 E2E gate
        run: echo "Run npm run test:e2e locally or in staging with DATABASE_URL, db:migrate, db:seed:m0, and db:seed:m1. CI does not run it by default because this workflow has no PostgreSQL service."
