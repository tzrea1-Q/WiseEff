# 本地闭环轮次 — 2026-09-21

> English: [English](../../../../exec-plans/active/849-inventory/local-closure-20260921.md)

基线：`5355f973bfb42dbc4bf47bfac25d56204bf550a9`。分支：`codex/849-local-closure`。状态：六项已接受的本地事项完成并经独立复核，可交付 PR。不构成程序 SEAL；边界扫描仍按下文记录失败。

用户授权按下列顺序完成六项本地工作，包括实施、本地验证、独立评审、证据对账和最终一个 PR。本轮替代此前逐项等待确认的暂停规则。不授权合并、生产访问或修改、关闭 Issue、弱化 checker、DROP 后继表或完整 T3.4a SEAL。实施及评审子智能体使用 GPT-5.6-Luna、当前工具暴露的最高推理强度 `xhigh`。

| 顺序 | 本地结果 | 状态／验收 |
| --- | --- | --- |
| 1 | 草稿 DELETE 项目作用域及审计 | 本地完成：错误项目／租户／owner 拒绝、成功审计及审计失败回滚。纳入最终后端 20 文件／211 测试通过结果。 |
| 2 | canonical 审批流程契约 | 保留单阶段软件审批，覆盖当前准确项目角色、指定合格审核人、撤回／驳回／重提及批准重放。最终后端检查通过。 |
| 3 | TD-125 空 Catalog 与绑定物化 | 实现 canonical-only GET、真实绑定物化、DTS／JSON 属性编辑／删除及终态墓碑。最终后端检查包含已有数据升级、owner 和锁的对抗场景；完整 topology Gate0 仍单独未闭环。 |
| 4 | 精确种子／来源／保留证明 | 按已接受的完成实例重放边界完成：真实本地存储、372 个准确元组及 ID、独立值、非参数行／附件保留；种子检查纳入最终后端通过结果。不宣称独立首次分配的 ID 相等。 |
| 5 | 边界残留与 relocation | 已复核记录包含 237 对 consumer／265 对 family 映射，移除 12 个消失的 allowance。脚本 8 文件／219 测试通过。原生扫描为 3,568 命中、3,491 准许、77 未准许，失效／错配／增长均为零，退出 1。已完成获授权的记录修补；完整 T1.4 保持未闭环。 |
| 6 | 评审意见及中英状态对账 | 核心、前端、种子、重启与 relocation 评审完成；最终属性删除评审及当前证据在下文对账。未授权合并或修改 Issue 状态。 |

附带 minimal-upgrade 文档／探针完成判据差异可在本机修正。原生 amd64 终端验收仍需外部环境，本地源码检查不完成 T3.2。Hosted、用户部署、Windows 安装包／硬件及完整程序 SEAL 保留独立证据归属。

## 环境与归属

- 新建隔离工作树，保留全部既有工作树。复用已有本机依赖安装，不修改依赖目录。
- PostgreSQL helper `127.0.0.1:55438`、容器 `wiseeff-g668-pg`。最终后端运行从 `template0` 新建 `wiseeff_849_delete_*`，显式设置两个数据库 URL，使用单 worker。旧 `wiseeff_ut_routes` 基库发生本地 0160 漂移后不再用于后端迁移，未修写其迁移历史。禁止共享 `5432/wiseeff`、`wiseeff_lane_849`，禁止迁移 `postgres` 维护库。
- 主智能体负责集成、持久记录、共享 checker、提交及 PR。子智能体仅修改界定路径；后续事项可提前只读调查，实施保持有序。
- UI 改动采用真实 API 模式 1440×900 浏览器证据。相关原生聚焦测试、typecheck/build 和文档门禁保持必需；集成后执行广泛验收，不对每次小改重复运行。

## 风险场景

1. DELETE：URL 项目不符、组织／owner 不符、草稿不存在、canonical 转 topology、成功删除、审计失败回滚。
2. 审批：准确阶段、指定合格人员、自审、跨项目、角色撤销／用户停用、过期／重复提交、最终批准前当前值及源不变。
3. Catalog：无指针／已发布空集合、有 canonical 行、越权、发布／同步失败、重试、不占用 fixture lineage、不重置用户值。
4. 种子：完整自然键与定位、JSON／DTS 分离、输入顺序、重放、自定义项目及非参数身份／关系。
5. 证据：保留可信基线、精确目标字节、原安全扫描器及历史锚点，不增长 allowance、不制造清零。

逐项追加实测命令、数量及评审处置。本计划不产生新的通过声明。

## 第一项 — 已复现的边界失败

helper 55438 真实 PG 路由复现：`catalogProjectValueDraftDelete.integration.test.ts`，修复前 **3 项测试／3 项失败**。错误项目能删掉同一用户的另一项目草稿；成功删除无审计；注入审计失败后未回滚删除。修复归属共享领域事务并覆盖 v1 调用，不能只补 v2 路由。

修复后路由文件 **6/6** 通过，包含 v1 错误项目拒绝、v2 错误项目／异主／异组织拒绝、成功审计及审计失败回滚。canonical 草稿集成 **14/14**、三个聚焦单元文件 **63/63** 通过；第一项中间工作树 `npm run typecheck` 通过。这些为本地实测，不是最终集成验收。

## 第二项 — 实施前契约决策

普通 topology 提交保留 `hardware_review → software_review → software_merge` 及三个项目角色池。canonical 源工作流保留现有单阶段 `software_review` 批准／生效单元。本地修正为：可选指定一个软件审核员并明确合格角色池兜底、canonical 只检查软件审核候选池、批准或重放结果披露前在事务内校验当前激活状态与项目角色并与撤权串行。不引入指定人员独占执行，不把 canonical 改成普通三阶段流程。依据：`2026-09-15-project-review-role-governance.md` 的普通提交范围、`2026-09-15-parameter-unification-round-report.md` 第 1.5 节明确的软件审批授权，以及 `source-occurrence-workflow-contract.md` 的原子批准要求。本轮新写决策不能作为其自身已获验收的独立证据。独立只读调查：`workflow_contract` 子智能体；第一项聚焦验证通过后已开始实施。

## 2026-09-21 owner 已接受的决定

owner 对三项建议回复“按照你的建议继续”，确认本轮本地交付边界如下：

1. 保留 canonical 软件审核单阶段，补齐属性编辑／删除及对应回归；不恢复空 Catalog 回退，不静默移除操作覆盖。
2. 接受同一已完成实例交换顺序重放时的种子身份稳定，既有分配器不变；已在 T1.3 设计 §8 及 B2-15 记录澄清。
3. 交付经过复核的本地边界记录修复，并明确保留未解决的扫描命中。完整 T1.4 清零验收继续开放，需要另行明确边界契约；未授权弱化 checker 或增长 allowance。

以下段落记录促成上述决定的原始冲突，不再代表待答复的授权请求。

决策时，现有 topology 浏览器套件依赖已移除的空 Catalog 回退，并覆盖三阶段审批及属性删除；canonical 源请求只支持 `set`。已接受的修正是在独立 operation ID 下补齐 canonical 编辑／删除覆盖。原有操作 ID 和断言保留；完整 topology Gate0 验收仍单独计算。

边界 checker 最初在过期 destination blob 处终止。第一批独立复核后的重绑输出 214 个未准许、155 个失效 allowance。第二批新增 144 对精确映射，并移除 11 个旧片段已消失的准许项；连同第一批 TOP 退役，该轮原生结果为 3,561 命中、3,491 已准许、70 未准许，失效 allowance、元数据不一致及增长均为零（退出码 1）。必须保留 2xx 的路由及从锁定绑定读取的 SQL 中仍有命中没有既有历史 allowance 映射。owner 已接受保留这些命中交付；完整零命中闭环仍开放。未修改 checker 规则或历史 runtime/edit 记录。

## 属性删除扩展前的 canonical 浏览器及物化实测

本节浏览器和联合测试计数均早于属性删除扩展及第二条 JSON 浏览器测试，不能证明当前两测试文件通过。删除设计随后通过独立 Spec 和 Standards 审查；实施和最终原生验证仍进行中。严格接口契约在独立纯 DTO 单测 harness 中完成 **18 项中的 4 项失败 → 18 项通过**（未执行 PostgreSQL setup），覆盖动作保留、DTS／JSON deleted manifest 和历史状态。

已接受的种子判据是完成实例重放：同一实例交换项目／文件输入顺序后，全部 binding ID 保持不变。独立初始化数据库仍随机分配来源身份；身份分配器和绑定键均不变。

既有 canonical 审核组件没有生产调用方，HTTP 撤回方法也未接入应用端口。两项失败的前端测试复现了缺失的队列及撤回入口。接通现有组件与方法后，两份 UI 文件 9/9 通过；适配器／客户端／runtime／无障碍检查 4 文件／41 测试通过。待审核为空时保留队列外壳，使历史仍可访问；仅提交者显示撤回操作。

独立评审随后发现全局最高角色可能让用户在其他项目看到审核按钮，审核员也能看到自己提交的审核操作。新增三项失败用例复现了这些界面错误；修复后两份 UI 文件 14/14 通过。页面改用登录用户的准确项目角色（在认证状态加载时保留），面板对提交者隐藏审核操作；服务端授权仍是最终判据。合并后的相关前端检查为 **9 文件／253 测试通过**，该中间集成树 `npm run build` 通过（仍有既有的 bundler 外置模块／chunk 大小提示）。

`canonical-value-workflow.acceptance.spec.ts` 在 helper 55438 自有临时运行栈、真实 HTTP 授权、显式首版本安装、真实源文件上传及本地存储下，以 1440×900 跑通 1/1。覆盖 UI 创建／删除草稿及刷新、指定软件审核员、批准前当前值不变、撤回、驳回、重新提交、批准和刷新后历史。截图检查发现顶部项目名称不符；新增浏览器断言在修复前失败，修复顶部／深链接后通过，并验证切换至另一项目的空队列。这是新增 canonical 浏览器证据，不是未修改的 topology Gate0 套件通过。该次执行还完成了临时运行栈清理。

`catalogProjectBindingRead.integration.test.ts` 在真实 PG 及本地对象存储下 2/2 通过。先有源文件、后发布 Catalog 时保持空态，直到现有 HTTP 上传 owner 物化值。重复上传返回 409/CONFLICT，精确绑定／值／pin／文件／修订身份不变；重复 GET 和新建数据库连接池读取保持一致。新建连接池不等于 API 进程重启或目标服务器验收。

独立 fixture 评审否决了最初的内存存储完成声明，并指出种子值和保留证明缺失。修补后的 T1.3 使用自有文件系统 ObjectStore，验证 archive 字节／摘要、372 个准确自然键到 binding ID 的对应、独立热管理／JSON 值、非空自定义项目角色关系及共享对象保留。通过真实授权创建草稿并提交后，再以完整行快照验证重放。完整三项目 fixture 缺失 JSON 指针时，九份文件及三个修订准备完成后预检失败，canonical 绑定／值均为零；随后正常输入成功。交换项目和文件顺序后 seed digest 及已分配身份在完成重放时不变。种子联合检查 **3 文件／10 测试通过**。这不证明独立数据库首次分配的身份等价。后续 owner 决定接受完成实例重放作为第四项身份边界；该限定事项已完成。

最新核心修补使待审核申请的持久草稿退出可编辑列表，驳回／撤回后重新可编辑；批量提交中每项成功即从本地列表移除，后续项失败不会恢复已提交项。聚焦复现为后端 **17 项中 1 失败**、前端 **26 项中 1 失败**，修补后均通过。指定审核员资格检查先获取既有用户行锁，再读取准确项目角色；真实并发撤权用例修复前失败，修复后并入既有 canonical 草稿套件，**18 项通过**。独立评审撤回了一条错误的浏览器标签报告，并接受了草稿列表修补。

这些修补后的联合后端检查 **9 文件／145 测试通过**，受影响前端 **5 文件／212 测试通过**，`npm run build` 通过，真实 API 浏览器流程 **1/1 通过**，包括提交／刷新无可编辑待审草稿、后续撤回／驳回／重提。下文旧计数为中间观察，不是可累加的额外测试。

注册审批的用户行锁在移除冗余申请行锁后，真实 PostgreSQL 聚焦测试通过 **31/31**；已有条件式审批更新保留并发决定冲突时的事务回滚。相关后端联合检查在 helper 55438 上 **9 文件／144 测试通过**，覆盖草稿删除、canonical 工作流、物化读取及角色写入；不包含仍在修补的种子测试。文档治理检查通过；首次数据库结构文档检查因缺少 pgvector 跳过，随后显式指定 helper 55438 重跑，生成文档与迁移一致且没有跳过。这些是中间工作树观察，不是最终联合验收。

## 当前边界清单

原生命令未变：`npm run parameter-catalog-boundaries:check -- --trusted-base-sha 5355f973bfb42dbc4bf47bfac25d56204bf550a9`。最终 77 个未准许命中与过期位置已分开，验收仍失败。相比此前的 70 个，新增 7 个均来自 JSON owner／回滚集成测试的直接 SQL 探针，未为其增加准许：

| 家族 | 数量 | 涉及的当前表面 |
| --- | ---: | --- |
| CGH | 27 | 定义验证、必须保留的治理详情／overlay 路由，以及导入和验证测试 |
| FIL | 22 | 先前 15 个锁定绑定／规范／冲突／同步／回写命中，加 7 个 canonical JSON 集成测试 SQL 命中 |
| MOD | 14 | 现有模块影响／dismissal 读取及测试 fixture |
| TOP | 8 | 使用旧定义及锁定绑定行准备数据的既有源编辑回归测试 |
| KNW | 4 | 知识路由测试、对比关联及参数引用身份 |
| DBG | 1 | 调试绑定的 `parameter_spec_id` 身份 |
| PRJ | 1 | 本轮增加项目作用域后的草稿 DELETE SQL 表达式 |

独立评审在应用前发现 family 候选的一处重复调用错配。历史验证调用应归属于当前第 264 行的旧测试；新增测试第 248 行中的相同调用仍保持未准许，回归断言固定了该区别。current record 的 hash 及 shard 准确减集检查绑定受审字节。清单测试仍要求 `report.status === "failed"`，更新诊断数量不构成新增准许或新的通过基线。

## 最终种子保留证明

种子保留检查最终补入预存的非参数知识记录及真实存储附件。本组织完整 entry/file 行、归属／关系、元数据、checksum 和实际字节在首次初始化及完成重放前后均一致。更新后的 T1.3 集成测试 **1/1 通过**，独立评审确认保留证明缺口关闭。此前 3 文件／10 测试证据早于这一新增断言，种子生产代码未变。

## 实际 API 重启及清理归属

canonical 浏览器现在停止身份准确匹配的 API 进程组，并在相同端口、数据库及 ObjectStore 上启动替换进程。重启后比较完整的已批准绑定、当前值、导出源字节及已批准历史。本地浏览器 **1/1 通过**；非敏感进程凭据记录旧 PID `78593`、新 PID `78736`、端口 `60825`、开始时间标识及命令 hash。`LOG_ANALYSIS_DETERMINISTIC=true` 仅属于此浏览器 fixture 的 API 环境并随重启保留，未成为共享运行器默认值，也不构成外部模型验收。

独立重启评审最初发现三个问题：替换进程发布失败未贯穿清理状态、监督启动账本拒绝合法替换，以及 macOS 秒级开始时间可能误拒绝不同 PID。修补后未解决的重启失败会被保存并阻止再次重启，清理记录失败并保留数据库／ObjectStore 证据。最终清理器要求每份历史／当前启动记录与完整替换链准确对应，在向任何 writer 发信号前确认所有历史进程组已退出，仅允许接管当前进程。历史记录缺失、额外、未知或仍存活时继续拒绝；同一进程判断包含 PID。

受影响脚本 **4 文件／84 测试通过**，包含直接调用返回 runtime 的 `restartApi()` 及 `dispose("success")` 的故障注入。生命周期 fixture 使用合成进程探测／信号，最终隔离修补后 **2/2 再次通过**，不向真实系统进程发送信号。这些故障注入检查与真实浏览器进程重启分别记录。Node TypeScript 及最终 `npm run build` 通过，增量独立复核关闭了三项重启发现。交接前仍须执行原生文档门禁，不宣称完整受监督 Gate0 或目标服务器验收。

## 属性删除中间检查

该中间检查点前端 **12 文件／279 测试通过**，纯 DTO／JSON parser **2 文件／29 测试通过**，显式禁用了 PostgreSQL global setup。浏览器仅收集不构成验收；最终原生执行结果见下文。

解析器与 E2E 的独立 Spec 审查在源码检查范围内通过。直接回归在原实现和提前拒绝的实现上都通过后，审查撤回了未经证实的“实例根可被删除”问题：原有最终缺失证明已会拒绝。浏览器断言现已比较 JSON 精确字节，并通过严格 DTO 校验真实删除值导出，将 pin、base pin、请求与证明和 PostgreSQL 比对。最终生产者／迁移对齐及执行证据仍须完成。

## 最终本地验证及评审

最终后端使用原生 `npm run test:server -- <受影响文件>`，两个数据库 URL 显式指定新建 helper-55438 数据库并使用单 worker。**20 文件／211 测试通过，零失败、零跳过**（`/tmp/849-final-candidate-server.log`）。涵盖前述九个核心文件、三个种子文件、严格 DTO／JSON／DTS 源检查、ProjectValue service／集成／并发、保护读取适配器及已有数据属性删除迁移。联合运行中的纯单测不单独冒称 PostgreSQL 集成证据。

未再改动的受影响前端 **12 文件／279 测试通过**（`/tmp/849-current-delete-frontend.log`）；八个 relocation／重启／清理脚本文件 **219 测试通过**（`/tmp/849-final-scripts.log`）。`npm run build` 通过（`/tmp/849-final-reviewed-build.log`），保留已有 bundler externalization／chunk-size 警告。新增 0160／0161 后已在 helper PostgreSQL 临时库重新生成 schema 文档。

最终真实 API 浏览器文件在 1440×900 **2/2 通过**（`/tmp/849-final-candidate-browser.log`），覆盖 DTS 编辑／删除、JSON 删除、精确导出／历史、实际 API 替换和重启后读取；三张审批截图均已目视检查。测试在主动停服期间离开页面，就绪后重新打开，证明持久化／重启，不证明前端不断线重连。此前一次因后台 `/api/v1/me` 轮询撞上主动停服而失败，未新增诊断豁免。`npm run docs:check` 使用可达 helper 验证 schema 当前有效，无跳过。operation matrix 与 `npm run acceptance:coverage -- --results /tmp/849-canonical-browser-results/results.json` 通过；显式结果路径避免误读旧的仅收集报告，静态映射覆盖不代表跑过完整 Gate0。

独立 Standards（`standards_review`）和 Spec（`seed_independent_review`）源码复核均在本轮已接受本地范围内 **PASS，无剩余生产阻断项**。审查者排除自己的实现／测试路径，由主智能体和另一审查者覆盖对应增量。修补及可运行证据包括：

- 严格 action／format／state DTO；批准后重新读取完整请求，JSON 删除响应不再误标为 DTS。
- 不可变终态墓碑、精确历史导出、保护读取拒绝，以及已删除 ID 进入旧草稿／导入回退前的拒绝。
- append／CAS 的请求、动作、base pin、目标／兄弟绑定一致性；source occurrence 与 Binding 按序锁定并重读成员集合。两连接探针在修补前复现缺少 FK 引用锁。
- DTS 精确前后字节及唯一最大序删除效果、JSON 成员／缺失证明、请求／值／历史／审计／源的完整归属、定位符摘要 owner 校验。错误 applied-value 复用和错误 JSON 摘要在 Red 中被接受，修补后拒绝；DTS 同序效果及审计失败均回滚。
- 已有数据从 0159 → 0160 → 0161 后原 Binding／value／pin／history／源版本行及 active projection 不变。JSON digest helper owner／EXECUTE 权限断言先失败，补齐对称 owner／revoke 后通过。

JSON 重注册绕过结论已**撤回**：真实源／数据库探针在原实现上已拒绝，未修改 `canonicalJsonSource.ts`。此前的 JSON 实例根删除绕过也因原有缺失证明已拒绝而撤回。两项均不记为已修复缺陷。

当前边界记录字节已独立复核：consumer SHA-256 `97f3190a80d0800fac88b6d3d5b60897ed24312ef056bdc59e2099dfa6e712a9`、237 对映射；family SHA-256 `6e55b1378acf6f4439392ae53971092a996c12b41fe358fa31622f052e7f4cf2`、265 对映射。原生扫描仍以 **77 个未准许项失败**，不构成 T1.4 清零验收；未放宽 scanner、历史 runtime／edit 记录或 allowance 增长限制。

## 测试目标事件

最初一次 server 测试遗漏 `TEST_DATABASE_URL`，连接到 harness 默认的 `127.0.0.1:5432/wiseeff`。global setup 在不可变迁移 0151 校验和检查处失败，尚未执行该共享库的待迁移 SQL 或测试。后续只读查询观察到此前两小时新增迁移为零、空迁移校验和为零；这不代表已对所有元数据作前后比对。未尝试修复或回滚该数据库，已告知用户。随后的核心验证明确使用 helper 55438 URL；该 setup 失败不计为 Red 测试或验收证据。

后续 JSON parser 子任务中，worker 违反不运行数据库的要求，再次误用未限定目标的 `npm run test:server -- server/modules/parameter-files/jsonSource.test.ts`。两个数据库 URL 均未显式设置，因此又选择了共享 `5432/wiseeff`。实际错误栈经 `testDatabase.ts:210` 进入共享库 `applyMigrations` 的校验和检查，在 0151 处停止，未收集测试，也未进入该共享路径的待迁移循环。此前模板准备可能复用或创建模板、清理旧测试库；现有输出无法确定实际分支。元数据 bootstrap／checksum 回填及模板操作的影响均未知，不宣称零变更。未再查询、修复、回滚或清理共享库，已告知用户，并将该 worker 限制为只读审查。随后隔离 parser 运行只提供 **10/10 纯单测**证据，无数据库／global setup；没有观测到 parser Red。剩余 PostgreSQL 验证必须使用显式 helper 55438 wrapper。
