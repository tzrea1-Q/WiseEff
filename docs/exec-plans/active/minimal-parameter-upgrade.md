# Minimal parameter upgrade

> Chinese: [中文](../../zh-CN/exec-plans/active/minimal-parameter-upgrade.md)

Status: implementation. Accepted user scope: 2026-09-09. Base: `cda6737a8f177a8bbd2f3bc7d195f8e3037bfa74`.

Current product candidate: `f51569d2c32f6a27e8f64271c358fefcd62b8d61`, tree `258d0a5bfb1e5af1cb3022b6ea544fdc96834945`. This increment joins Binding stabilize to an outer transaction (savepoint, no inner COMMIT) so DTS ingest sync and catalog import-preview rewrite commit with their audit events (ADR-0027). Dedicated Hosted for this SHA is pending. Last green page-loop was [34424170420](https://github.com/tzrea1-Q/WiseEff/actions/runs/34424170420) on `2cf966a9790df97e7502a0609fd0452c36685be3` (`pageProjectValues` 1000/2000/3000/3000, cleanup complete) with PR L1 [34424167069](https://github.com/tzrea1-Q/WiseEff/actions/runs/34424167069); that SHA is not reusable after the audit join. Pre-relocate Hosted `e35ac1b66` / 34387796500 is historical only.

Local follow-up (not pushed): independent review of `f32131111` left a SPEC-1 P2: empty catalog candidate sets skipped `matchCatalogImportRow`, so a precise BindingId/DefinitionId from another project fell through to legacy `createImportPreview` as `added`. The import wrapper now always matches source identities against the (possibly empty) candidate set before any preview write; name-only rows with no catalog hits still take the legacy preview path. STD-1 and SPEC-2 were not reworked.

Local follow-up (not pushed): independent review of `194834481` found three reachable defects. STD-1: canonical save used `canEditParameters(auth)` without the target project. SPEC-1: catalog import preview collapsed candidates with `Map(name → row)`, so a precise identity could be overwritten by another same-name binding. SPEC-2: save reused `sourceRef` but accepted client `baseRevisionId` without proving that revision belongs to that config-set, org, and project. Fixes stay in Binding: scoped `canEditParameters(auth, projectId)` after the catalog binding is found; `matchCatalogImportRow` (binding id, then unique definition id, then unique name; no silent fallback); `resolveConfigRevisionForSource` on the write session. Hosted 34439804231 / L1 34439801677 remain evidence of `194834481` only. `complete: false`. visualAndConsoleReview pending. Do not push, merge, or mark Ready from this increment.

Disposition: **not merge-ready**. Project values under repository-published definitions; no web definition-publication. Ingest sync of a registered published definition now writebacks inside `withAuditedWrite`; workbench save already did; import preview rewrite and apply stay on the same transaction as their audits. Remaining: dedicated Hosted and PR L1 on this SHA, independent review of this increment, human visual/console review. `complete: false`. Draft PR #825 stays Draft. PR #824 remains Draft/Open at `848304d780abd680b937b41f81afcb298a260ead`. No merge or production action occurred. Earlier execution records below retain their historical identities; this paragraph is the current disposition.

Latest terminal execution: [34321449970](https://github.com/tzrea1-Q/WiseEff/actions/runs/34321449970), candidate `77ccbce28e420a916ef678a4565b193e18da12f8`, actual image `sha256:61ef694c5989710fe1eacf063874470dbeeba9646782fed18b7a9993082d3752`. Nominal upgrade, 126-table/183-record/661-constraint preservation, actual HTTP/worker business, first repository publication, restart, same-SHA no-op, migration kill/isolation and both restores passed; cleanup completed. All nine screenshots were inspected: Chinese is readable, original node name/path and post-restart publication are visible at 1440×900, 768×1024 and 390×844. Console records zero errors and one warning whose text was not captured; network records one pre-login `/me` 401 and only 200 responses after login. Desktop unpublished still rendered a clipped empty table; the next one-condition fix omits the unused workspace while retaining the unpublished banner. Its real-browser recheck is pending. Page create/edit/save/import remains unimplemented. The [55-file source package](https://github.com/tzrea1-Q/WiseEff/actions/runs/34321449970/artifacts/10092434503) matches Git bytes, hashes and diff; [scanned evidence ZIP](https://github.com/tzrea1-Q/WiseEff/actions/runs/34321449970/artifacts/10092433807) SHA-256 is `6fc2ad9b33065e628ff9f9a17efa856e9e2108d7cfb2035d96504c8a7ed2f5f4`.

The remaining browser failures were fixture failures: run 34318719563 waited for a top-bar control absent from the actual homepage; run 34319888419 expected the node editor to use the binding dialog title and exposed missing runner CJK fonts; run 34321169638 installed fonts but stopped before Docker because `rg` was unavailable. None counts as browser acceptance. Existing controls, stock CJK fonts and native `grep` corrected those checks without changing timeouts or product permissions. On unchanged backend code at 77ccbce28, the final owned PostgreSQL run passed all 3934 tests in 503 files and `docs:check`, with cleanup complete. The empty-layout increment passed 16 focused frontend tests, `ui:check` and build; its added assertion first failed on the real rendering defect.

Current verified runtime: `37d7cea70b179f149212bbd7dac2ee098c0cdc6f`, [run 34316513041](https://github.com/tzrea1-Q/WiseEff/actions/runs/34316513041). Upgrade and both actual restore paths preserve every original field of 183 records in 126 tables and all 661 original PK/FK/UNIQUE/CHECK constraints. The migration was actually killed; proxy/worker stayed stopped, queue stayed paused, ordinary resume returned 70, and explicit rollback passed. Cleanup completed. Actual candidate image: `sha256:a10b2c4fe3ea62a1d20c020b14bd9c72a50ccfe8d4a76717e56228d4a9fadc0a`. All 51 packaged source files, hashes and diff match this exact candidate. Independent Standards/Spec closed the runtime findings, including orphan install and mode/status handling; page create/edit/save/import remains open. Standard required CI was skipped.

Browser increment run [34317375612](https://github.com/tzrea1-Q/WiseEff/actions/runs/34317375612), `f850f2f13e882dc1749adbce6da00ab38aca19e5`, was cancelled after review found that screenshots would bypass the existing artifact scan/ZIP boundary. Its retained artifact contains only partial stage JSON, no browser files and no cleanup attestation. It proves no browser acceptance. The followup reuses the existing sanitizer, scanner and ZIP encoder, publishes only a scanned read-only ZIP after cleanup attempts, and registers the actual browser session for exact redaction. A killed owner cannot upload a partial raw tree. Artifact/finalizer/CI regressions: 59 passed; build passed. The corrected browser execution is pending.

Previous execution: [34314419833](https://github.com/tzrea1-Q/WiseEff/actions/runs/34314419833), runtime `9c722080bb979cf798c039246c7629c337bf2fe8`; [sanitized evidence](minimal-parameter-upgrade-evidence/terminal-9c722080b.json). Nominal upgrade and both whole-state restores passed on the same nonempty deployment. The second upgrade actually killed the migration container, persisted `recovery-required`, stopped proxy/worker, paused the queue and refused ordinary resume with exit 70. Explicit rollback restored all 183 original rows, objects and task state. Wrong-target refusal, real HTTP/worker business, first repository publication, restart and same-SHA preservation passed. Native source-lock tests passed 4/4 without changing the timeout. Cleanup completed. Dedicated CI success does not cover skipped required suites or page editing/import. Source archive bytes and hashes match this candidate; the diff matches with Hosted's eight-character object abbreviation.

Review increment `37d7cea70b179f149212bbd7dac2ee098c0cdc6f` refuses first install over orphan Catalog rows, filters child process environments with the existing allowlist, restricts initialization mode to plan/apply and exposes the persisted mode in status. Actual PostgreSQL regression: 35 passed; status/action cases: 5 passed; environment helper: 1 passed; build and boundary checks passed. The terminal oracle now compares original PK/FK/UNIQUE/CHECK definitions and validation state as well as original row fields; its new execution is [34316513041](https://github.com/tzrea1-Q/WiseEff/actions/runs/34316513041), pending. Independent review followup is pending. The confirmed remaining product seam is page create/edit/save/import. The alleged three-part BullMQ ID defect was withdrawn after checking the pinned implementation.

Earlier observations below retain their original execution identities and incomplete stages; the latest status above supersedes their pending-state wording.

Current terminal milestone: [run 34310318652](https://github.com/tzrea1-Q/WiseEff/actions/runs/34310318652) passed at `c1d44f17f036a561ac46f28fb35f9c1f1e56f234`; [sanitized evidence](minimal-parameter-upgrade-evidence/terminal-c1d44f17f.json). The same nonempty deployment completed actual upgrade, 126-table / 183-original-row field comparison, object preservation, original login/permission isolation, candidate HTTP/worker business, first repository publication, normal restart, same-SHA no-op and whole-state restore with object/Redis checks. Cleanup completed. Candidate image: `sha256:33aa2fe161d2a19fbf39d601eed637d2b74a7e4b3e0cc2410bd1d4462b7926c1`. This dedicated job passed; standard required suites were skipped, and the evidence remains `complete:false`. Page edit/save/import and complete browser acceptance remain open. The next driver increment adds wrong-target refusal and an actual killed migration followed by explicit recovery on that same deployment; it has not yet been executed.

Preserve all non-parameter records, original keys and relationships, object bytes and task state. Leave legacy parameter tables and shared references intact. Start the new parameter system unpublished and empty; never fabricate a release. Deliver actual create/read/update, basic import, production API/worker startup, restart persistence, and one backup restore through the existing self-hosted upgrade entry.

The selected route is `--parameter-data-mode new-empty`, bound to its plan and run, never a runtime bypass. Historical migrations and ledgers remain immutable. Reject unknown sources and partial conversions before downtime. Management initialization must not recur on restart or repeat execution. Failures after migration retain isolation.

This accepted run profile replaces the full populated certification scope for this branch. PR #824 and Scratch remain separate. Old parameter equivalence, mapping/Archive and P0–P16 certification are not prerequisites for this route. No merge or production execution is authorized.

## Execution and evidence

One implementer, one Docker lane. Reuse the fixed `82344044b436a8dafecefbb85dfd724cecb05e3f` old-source fixture. First run the vertical probe; fix its first observed failure before expanding tests. Preserve original HTTP upload failures as failures even when an explicit synthetic producer proves old worker behavior.

| Step | Acceptance | Current evidence |
| --- | --- | --- |
| Old source to migrations | Full source ledger/schema verified; protected rows retained | Actual terminal upgrade in run 34316513041 applies 0129–0139 and preserves every original field of 183 records across 126 tables, plus 661 original constraints. The earlier 190-record component probe is separate evidence. |
| Empty catalog to first real data | Actual authenticated page/API, legitimate publication, import regression | Production API returns unpublished empty state; compiler/installer creates a real nonempty first release and API reads it after restart. Hosted run 34424170420 on `2cf966a97` drove page register `csub_acme_power`, DTS ingest `iin_max=1000`, workbench save `2000`, import update `3000`, restart re-read `3000`. ADR-0027 ingest/preview join is `f51569d2c`; Hosted of that SHA is pending. |
| Upgrade entry and restart | Existing controller, run-bound initialization, readiness and queue/proxy restoration | Native amd64 terminal apply, original login/node, first repository publication, normal API/worker restart, same-SHA preservation and wrong-target refusal passed in run 34316513041. Actual migration kill retained isolation; resume returned 70. Local ARM guard is retained. |
| Preservation and recovery | Every protected original record/relationship, objects and necessary tasks; actual restore | Run 34316513041 preserves all 183 original records and 661 constraints through upgrade and both whole-state restores, with original object bytes/metadata and actual queue payload/state. New HTTP upload and real worker complete. Earlier object-outage and individual-store probes remain separate component evidence. |
| Review and delivery | Stable focused/full gates, three browser viewports, independent Standards/Spec, required CI | Independent runtime findings closed through 77ccbce28. Relocate + DTO/guard Hosted 34424170420 and PR L1 34424167069 passed on `2cf966a97`. ADR-0027 ingest/preview join is `f51569d2c`; dedicated Hosted and independent review of that increment are pending. Human visual/console review remains pending. |

Source copies, trusted target build and maintenance window are production inputs, not reasons to block isolated implementation. Any conflict requiring new privileges or publication semantics must name the specific blocked operation. No broad new controller or checker is in scope.

Current evidence is WIP, not a sealed candidate or target-server build. API/worker business used ARM image `sha256:9bb4554d4e20b34a09f452663630bca2d9c4d8121e0af4a3e70a3076c09e0ae9`; the retry/first-Subject browser build was `sha256:afd02abed466a6afd93056624b700f65f0fcb69b6777cf4e48d915c4ce8de4cc`. Later refresh/hydration and Redis restore edits are not covered by those images. Focused results: 60 backend units, 31 Catalog frontend tests, one live-project hydration regression, three offline Redis restore shell cases. The browser checkpoint expired after partial observations; that process ended nonzero and cleaned its source. Auxiliary browser resources were separately removed. No complete browser acceptance is claimed.

The terminal probe is `scripts/run-minimal-upgrade-acceptance.ts`, launched by the `minimal-upgrade` workflow-dispatch mode. It creates its own stock Compose deployment and invokes `upgrade.sh`; the local native ARM preflight rejects before deployment creation. After the first repository publication and restart it now registers `acme,power` on the catalog page, ingests a matching DTS through the production config-set/file APIs, reads/saves/imports the published `iin_max` value on the workbench/import pages, and re-reads it after a second API/worker restart, then continues same-SHA, rollback, and the killed-migration restore. `complete:false` still preserves independent review, required CI, and final validation. This tested isolated entry is not a production execution approval.

Hosted run [34306500953](https://github.com/tzrea1-Q/WiseEff/actions/runs/34306500953), code `75a5f88f7849fbb78b7d487a24c5f0e99679378e`, built the fixed old source as actual amd64 image `sha256:f26bb4fc466ff1a65c980503d1ba8afb32460bc8a9fc04017d05a2f0dc7b3fa4`. It stopped at `old-start` / `minimal-terminal-api-not-ready`, before upgrade or migration, and cleaned its deployment. Dependency/command diagnostics have now been added using the existing sanitizer; the source is not patched and the timeout is unchanged. The separate documentation job failed because the Chinese plan used English section headings; those headings are corrected. This run is not a required-CI pass.

The object restore probe reproduced lost S3 metadata despite equal bytes, including with native `mc mirror --preserve`. The local single-node MinIO volume recovery now restores original bytes, `contentType`, `originalFileName`, `retentionClass`, and removes a post-backup object. These are actual stopped-service snapshot/restore observations, not whole-terminal acceptance. Nine focused upgrade shell cases and 143 App regressions pass; the documentation governance check passes, with schema-document verification still pending on the owned database.

Hosted run [34307587109](https://github.com/tzrea1-Q/WiseEff/actions/runs/34307587109), code `3f7ca488a28dc5875dabc343ce2999b67ea3e7b2`, failed before upgrade at old startup: the fixture omitted required `XIAOZE_CHECKPOINTER=postgres`. Its actual source image was `sha256:55773c688794fa58fc107219e12f682c4256c3e3d4f442ae40f0637d9158d6d5`; cleanup completed. The fixture now supplies that configuration and runs the fixed old source's own environment validator before building. The old code and timeout remain unchanged. Run [34308789909](https://github.com/tzrea1-Q/WiseEff/actions/runs/34308789909) tests the correction and extended driver at `68f660b61`; no result is claimed yet.

Remaining product seam: the Catalog contract publishes reviewed repository bundles; a page proposal is publication intent only. The selected project-value path now calls existing `stabilizeCanonicalBinding` / `writebackProtectedReference` for a registered published definition, with the actual config-set and config revision as source. Workbench save of a canonical pin writebacks. Import updates an existing binding and does not create definition identity. The terminal probe now drives same-deployment page register/ingest/read/save/import/restart; Hosted evidence for that loop is still open. No permission expansion or fabricated source is authorized by this plan.

Run `34308789909` was subsequently cancelled: the old target-job condition also matched the new minimal mode. The unintended target job failed browser warmup at `https://example.invalid/`; it is not target validation. The terminal job was interrupted and has no completion/cleanup attestation. The target condition now excludes `minimal-upgrade`, with a regression also requiring that the isolated job consumes no repository secrets. All 12 CI configuration tests pass. This routing correction must precede the next terminal dispatch.

Run [34309051449](https://github.com/tzrea1-Q/WiseEff/actions/runs/34309051449), code `b7bc76af66f99427a0c4e21d1bf86e217c076141`, reached real old API readiness, then failed `old-synthetic-business`: its bootstrap fixture used the retired department name as an Organization. No upgrade occurred; cleanup completed. Actual old image: `sha256:e588b50fe38b5603e8399d85e52ca001346cde047447debade2154016da55cce`. The fixture now uses a valid synthetic Organization and binds `WISEEFF_PUBLIC_URL` to its actual loopback proxy port. The self-hosted static check also now expects the stronger clean-environment `fdtoverlay` command; 18 tests and `selfhost:check` pass. Run [34309386308](https://github.com/tzrea1-Q/WiseEff/actions/runs/34309386308) exercises these changes at `be6d17dae`; its result remains pending.

Run [34309386308](https://github.com/tzrea1-Q/WiseEff/actions/runs/34309386308) reached `terminal-apply` at `be6d17dae43d149dfccdba4f7719f7b1101cbb74`: real original user/project/node/legacy spec/log object and completed native old worker job were created. Source image `sha256:f725bdfea4274ed13c02f3e8c937a5521655d35ea2042ef2478213da73625fce`, candidate image `sha256:7d72c04b59a6529b7c76ba82cccf4ff0f40249c301ed0548ae2de2c46ba54ac4`. The controller applied all 11 migrations and observed canonical unpublished state, then exited 70 at `candidate-proxy-public`; fixture cleanup completed. Local native Caddy reproduced the fixture's invalid inline block syntax (`Unexpected next token after '{' on same line`). Multiline syntax passes `caddy adapt`. The driver now validates Caddy before building, checks readiness through the actual loopback proxy and retains sanitized proxy/journal diagnostics. Record/object comparisons, restart and combined restore were not reached in this run.

Run [34311496178](https://github.com/tzrea1-Q/WiseEff/actions/runs/34311496178), code `16d908183626a6f2b840e84bded1226ddf642244`, passed wrong-target refusal and repeated the nominal upgrade, preservation, publication, restart, no-op and whole-state restore. The second upgrade refused before migration because rollback's retained image alias was not recognized as the fixed old source. No migration interruption was reached; cleanup completed. Source image: `sha256:0ef5304700eb48e10c4668bf050c6f6ee44851e4ee8586f7c1d4cc6458e27971`; candidate: `sha256:f1f3662cdbce759f6a819ad7ab46665a9cde15c9e72df32f94f807f141756622`. The fix recognizes only the controlled rollback alias when its actual running image exactly equals the retained source-SHA image. Missing, mismatched and arbitrary aliases remain rejected. The failed driver's journal diagnostic referenced the first restored run; it now records the active run and controller exit explicitly.

Local validation on the subsequent working tree: frontend 438 files / 3378 tests passed after waiting for actual cutover UI readiness; build and boundary checks passed. An owned PostgreSQL full backend run had 3931 passed / 1 failed: the new empty-state SQL crossed the legacy dependency guard. The query now lives in the existing Catalog pointer owner, with a real orphaned-projection regression. Scripts had 1245 passed / 15 failed / 5 skipped: the old Wayfinder exporter ignored the owned database URL and attempted its default Docker container, failing at the nonexistent test database before export. The rerun binds `WAYFINDER_POSTGRES_CONTAINER` to the owned container. These failures remain recorded; rerun results are pending. Database migration and docs checks passed; owned resources were cleaned.

## Documentation Impact Matrix

| Area | Action | Files |
| --- | --- | --- |
| Repository maps | Review | `AGENTS.md`, `ARCHITECTURE.md` |
| Planning | Update | This plan and its Chinese companion |
| Product and architecture | Review | `CONTEXT.md`, `docs/design-docs/domain-model.md` |
| Testing and frontend | Review | `docs/developer/verification-matrix.md`, `docs/developer/browser-acceptance-coverage-map.md`, `docs/developer/user-operation-coverage-matrix.md` |
| Reliability and references | Update | `ops/self-hosted/upgrade.md`, `ops/self-hosted/upgrade.zh-CN.md` |
| Security and governance | Review | `docs/SECURITY.md`, `docs/agents/agent-delivery-protocol.md` |
| Generated schema | Review | `docs/generated/db-schema.md` |

## Documentation Update Gate

Keep this plan active until all acceptance rows have candidate-specific evidence and every documentation row is resolved. Before closure run `npm run docs:check`, record actual tested upgrade/recovery commands, exact candidate/image identity, full source/diff and independent review. Separately report code merge readiness and production execution prerequisites.
