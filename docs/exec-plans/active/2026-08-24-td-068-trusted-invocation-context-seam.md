# TD-068 trusted invocation context seam (#610)

> Chinese: [中文](../../zh-CN/exec-plans/active/2026-08-24-td-068-trusted-invocation-context-seam.md)

Status: #610 implementation and final-tree local CI are complete on `codex/td-068-trusted-invocation-context`; merge-ready under the owner's documented full-local-CI exception while the monthly GitHub Actions quota is exhausted. This plan remains active as the shared migration record: the slice establishes the server-internal context and policy/audit seams, while TD-068 stays Open until #611–#615 land.

## Goal

Provide one branded, server-owned `user` / `agent` / `system` trusted invocation context without changing legacy call sites or public request contracts.

## Scope and implementation

- `server/modules/auth/trustedInvocation.ts`: discriminated context, immutable authenticated-principal snapshots, strict constructors, runtime brand validation, and Agent approval correlation.
- `server/modules/auth/trustedInvocationPolicy.ts`: required-context human-required policy seam with stable `403` refusal details.
- `server/modules/audit/trustedAudit.ts` and `server/modules/audit/auditedWrite.ts`: actor/audit projection plus transaction and pool refusal writers that preserve system null-user semantics.
- Focused tests cover constructor invariants, policy outcomes, actor/audit projection, platform-scoped system audit, and malformed-context failure before a query.
- Existing optional `actorType` callers remain unchanged for later migration tickets. No request DTO, header, body, `/me`, or OpenAPI contract is changed.

## Verification

- Focused trusted-context tests: 3 files, 15 tests passed.
- After rebasing onto repaired main `a57a88806`, the complete final-tree matrix passed: frontend 408 files / 3022 tests; scripts 69 files / 830 passed / 5 skipped; bridge 21 files / 138 tests; server 355 files passed / 1 skipped and 2739 tests passed / 4 skipped.
- `npm run build`, `npm run contract:check`, `npm run docs:check`, `npm run acceptance:ci`, the required DTS toolchain check (dtc/fdtoverlay 1.8.1 and dtschema 2026.6), and the three-project DTS seed compile passed against an isolated pgvector database.
- `npm run lint` completed with 0 errors and the 299-warning frontend baseline. `git diff --check origin/main...HEAD` passed.
- `npm run ui:check` passed with every ratchet at baseline. `npm run logs:eval` passed 16/16 scenarios and 4/4 meta checks; generated timestamps were restored and not committed.
- The inherited main-red visual failures were repaired independently by #617 / PR #619 without changing any committed PNG. On this final #610 tree, workflow-equivalent MCR Playwright from an empty pgvector database and isolated object root passed `acceptance:quality` and all 97/97 quality tests. A separate empty database and object root passed `acceptance:smoke` 4/4 under the CI production-HMAC profile.
- Final Standards and Spec reviews were run separately against `origin/main...HEAD`; each reported zero findings. GitHub checks that cannot run because of the exhausted quota are not described as green; the owner explicitly approved the complete final-tree local matrix as merge authority.

## Follow-up boundary

Tickets #611–#615 construct context at HTTP/Xiaoze/system entry points and migrate the five DTS reload mutations and parameter-sensitive production writes. This plan does not close TD-068, refactor unrelated audits, or claim target/device readiness.

## #611 implementation checkpoint

- [x] Xiaoze read-only and approval-gated tool calls receive server-owned durable tool-call ids and persist through the orchestrator/tool-registry seam.
- [x] Execution reconstructs `AgentInvocationContext` from the active authenticated principal plus persisted session, tool-call, and approval records.
- [x] Approval resume validates session/tool-call/approval correlation before execution; edited arguments replace the persisted payload and are re-authorized in the same transaction.
- [x] Request-local auth, invocation, and approval data remain outside checkpoint channel state; missing durable resume correlation fails before execution.
- [ ] #612–#615 remain open follow-up migrations.

## #611 verification

- Focused Xiaoze/orchestrator/AG-UI tests passed: 5 files, 57 tests.
- Full server suite passed on this branch: 354 files passed, 2 skipped; 2738 tests passed, 8 skipped.
- `npm run build`, `npm run contract:check`, `npm run docs:check`, and `git diff --check` passed. The local database-schema portion of `docs:check` was skipped because pgvector is unavailable on this host.

## #611 repair addendum (historical pre-final-rebase checkpoint)

The #610 history and verification above are retained unchanged. This historical #611 repair checkpoint was recorded on:

- Branch: `codex/td-068-durable-agent-provenance`
- Base: `origin/main@f52038848`
- Repair commit: `0dd0b39c6` (`fix(agent): close durable Xiaoze execution gaps (#611)`)
- Boundary: reuse the #610 trusted invocation context seam; require a persistent `AgentOrchestrator` at `createXiaozeAgentFactory`; route proactive read-only perception through the same durable orchestrator; and pass one transaction authorization proof through the public `ToolRegistry.run` seam. No new provenance foundation, temporary UUID, or test-only execution fallback was added.
- Every executing Xiaoze tool, including read-only perception, now obtains its persisted session/tool-call record through the orchestrator. Read-only contexts carry the persisted `sessionId` and `toolCallId`, with `approvalId: null`.

The PostgreSQL durable-resume proof creates the session, action tool call, approval, and interrupt through public AG-UI input in instance A. Instance B then uses a fresh PostgreSQL connection, checkpointer, registry, orchestrator, and the same authenticated principal to resume from the persisted checkpoint. The public registry execution seam captures and asserts `initiator: agent`, the unchanged user/organization principal, and the persisted session/tool-call/approval ids. It also asserts that `editedArgs` is the complete persisted payload replacement, that the replacement is re-authorized once inside the transaction, and that the authorization proof is passed into execution before the domain-write seam.

The same public resume path rejects a different approval id, a different body thread, an approval/tool-call mismatch, a tool-call/checkpoint mismatch, another user in the organization, and another organization. Each rejection occurs before `ToolRegistry.run`; the integration seam observes no additional execution and no domain write, while the pending durable rows remain pending.

### #611 repair verification

- TDD red evidence: the focused factory/orchestrator command first reported 2 failing tests (missing persistent-orchestrator enforcement and duplicate-authorization expectation); the suggest-route test first reported 1 failing persistence/context assertion while the old direct registry fallback was temporarily present.
- Focused green command passed: `npm run test:server -- server/modules/agent/orchestrator.test.ts server/modules/agent/toolRegistry.test.ts server/modules/agent/xiaoze/agUiEndpoint.test.ts server/modules/agent/xiaoze/agUiEndpoint.concurrency.test.ts server/modules/agent/xiaoze/agUiEndpoint.assembly.test.ts server/modules/agent/xiaoze/planningGraph.test.ts server/modules/agent/xiaoze/durableAgentResume.integration.test.ts server/modules/agent/xiaoze/suggestRoutes.test.ts` — 8 files, 73 tests passed.
- PostgreSQL durable-resume command passed: `npm run test:server -- server/modules/agent/xiaoze/durableAgentResume.integration.test.ts server/modules/agent/xiaoze/suggestRoutes.test.ts` — 2 files, 8 tests passed.
- Full server command passed: `npm run test:server` — 355 files passed, 2 skipped; 2743 tests passed, 8 skipped.
- `npm run build` passed (`tsc -b` and Vite build); Vite retained the existing large-chunk warning.
- `npm run contract:check` passed: OpenAPI contract artifact is current.
- `npm run docs:check` passed. Its database-schema/pgvector check was skipped because the host does not provide the pgvector extension; CI remains the pgvector/pgvector:pg16 verification boundary.
- `npm run lint` passed with 0 errors and the existing 299 frontend warnings.
- Verification-matrix acceptance specs were run separately with isolated ports and object roots, not the running 5173/8787 services. `xiaoze-perception.acceptance.spec.ts` at frontend `5175` / API `18787` with `/tmp/wiseeff-611-perception.47mXaE` passed 4/4 including warmup. `xiaoze-action.acceptance.spec.ts` at frontend `5176` / API `18788` with `/tmp/wiseeff-611-action.73LvlO` completed 6 passed, 1 skipped, 1 failed; the only failure was the known main-red browser approval-card timeout at `e2e/acceptance/xiaoze-action.acceptance.spec.ts:776` after 60 seconds. The other action flows passed, and this UI baseline issue is outside the #611 durable provenance repair.
- After the plan updates and documentation commit, `git diff --check origin/main...HEAD` passed.

### #611 cleanup repair continuation (historical pre-final-rebase checkpoint)

This continuation is test-only and does not change production behavior, the trusted invocation model, ToolRegistry authorization proof, or AG-UI interfaces.

- Cleanup repair commit: `79d059635` (`test(agent): guarantee durable resume cleanup (#611)`).
- The entire `withTempDatabase` callback is covered by an outer `try/finally`. Instance A and B saver handles are registered immediately after construction, before saver setup can fail; the instance B database connection is registered immediately after creation. The cleanup closes the instance B connection, instance A saver, instance B saver, and shared checkpointer/probe saver in that order, with optional handles and repeated cleanup handled safely.
- Reset of shared saver state is present in the callback `finally` and the surrounding test `finally`. Each cleanup operation catches its own failure so cleanup cannot replace the first setup, interrupt, resume, or assertion error.
- The focused regression deliberately throws after instance A is ready and before instance B is created. It asserts the original sentinel error is preserved and instance A's saver is closed. This covers the failure boundary requested for A setup/interrupt-to-B creation; the same outer boundary also covers B connection/setup and every positive/negative resume assertion.
- TDD red: before this repair, `npm run test:server -- server/modules/agent/xiaoze/durableAgentResume.integration.test.ts` reported 1 failed test (A saver end count 0) and an unhandled PostgreSQL `57P01` connection error. Green: the same file passed 3/3 tests.
- Requested focused command passed: `npm run test:server -- server/modules/agent/xiaoze/durableAgentResume.integration.test.ts server/modules/agent/orchestrator.test.ts server/modules/agent/toolRegistry.test.ts server/modules/agent/xiaoze/agUiEndpoint.test.ts server/modules/agent/xiaoze/planningGraph.test.ts` — 5 files, 60 tests passed.
- Repeated full server command passed: `npm run test:server` — 355 files passed, 2 skipped; 2744 tests passed, 8 skipped.
- Repeated `npm run build` passed with the existing Vite large-chunk warning. `npm run contract:check` passed: OpenAPI contract artifact is current. `npm run lint` passed with 0 errors and 299 existing warnings.
- Parent/current pgvector revalidation used `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run docs:check` against the local `pgvector/pgvector:pg16` container. Documentation governance passed and `db-schema artifact is current`; the schema check was not skipped.
- After this plan update and its separate documentation commit, `git diff --check origin/main...HEAD` passed.

The preceding #611 repair evidence retains its original host boundary, where the pgvector check was skipped; this cleanup continuation records the later pgvector revalidation above. No PR was opened or merged at that checkpoint. It did not claim complete local CI green because the action UI baseline failure still existed at that time.

## #611 final rebase and landing verification

- Finalization branch/worktree: `codex/td-068-durable-agent-provenance-final` at `/Users/tzrea1/Develop/WiseEff-td611-final`.
- Final rebase base: `origin/main@c9abd61c7bcf1508c7728f330cb6b2e40f4534ba`. The five rebased commits are `8284fc8e0`, `a13550b73`, `be77cd27d`, `9977885a2`, and production/test tip `68d76d88e`; `git range-diff` reported each patch exactly equivalent to its pre-rebase commit.
- The final diff contains only the bilingual active plan and the related `server/modules/agent/**` implementation/tests. The `XiaozeProvider` repair from #625 is inherited from `main` and is not reintroduced by #611.
- Exact-tree PostgreSQL verification passed: the durable-resume file passed 3/3; the five-file Agent focus passed 60/60; the complete server suite passed 356 files with 2747 tests passed and 5 tests skipped.
- `npm run test:all` passed: frontend 411 files / 3048 tests, scripts 69 files / 926 passed / 5 skipped, bridge 21 files / 138 tests, and server 356 files / 2747 passed / 5 skipped. `npm run build`, `npm run contract:check`, pgvector-backed `npm run docs:check`, `npm run lint` (0 errors; existing 299 warnings), `npm run selfhost:check`, `npm run acceptance:ci`, `npm run acceptance:models`, `npm run acceptance:coverage`, `npm run acceptance:operations`, and `git diff --check` passed.
- Owned-runtime Xiaoze acceptance passed on source-clean production/test tip `68d76d88e`: action 7 passed / 1 planned skip, perception 4 passed, two consecutive approval-card runs each passed 2/2 including warmup, and planning passed 3/3 including warmup. The planning run `full-20260825t013501071z-68d76d88e58d-880ccd19` finished with both processes stopped and its exact database/object root removed.
- The earlier approval-card main-red was repaired independently by #625 before this final rebase. Final action evidence confirms the card is visible and approvable, the chat remains open, no domain write occurs before approval, resume completes with Agent provenance, and the open change-request count changes from 0 to 1.
- `npm run acceptance:evidence` was run but did not pass because this focused Xiaoze run does not contain the full P0/P1 operation-record corpus. The command is required when operation-evidence coverage changes; #611 changes neither the operation matrix nor evidence helpers, so this focused-corpus failure is recorded but is not claimed as a pass or used as the #611 landing gate.
- GitHub Actions remain unavailable because the monthly quota is exhausted. The owner authorized the complete exact-tree local matrix as merge authority. The parent/session owner still owns final review, PR creation, merge, issue closure, and local `main` synchronization.

## #612 implementation checkpoint

- Scope: migrate all five DTS reload mutation entries — start-run, restore-baseline, deploy, configuration update, and promote-to-drafts — plus their five HTTP mutation routes to the branded `TrustedInvocationContext` seam.
- HTTP routes construct `createUserInvocation(auth)` inside the server and pass a `TrustedRefusalAuditSink` backed by a server-owned PostgreSQL pool root. Request bodies, query strings, headers, DTO fields, and arbitrary `actorType` strings do not select provenance; the route regression tests cover all five routes with body/header spoof values.
- Every domain entry validates the brand and authenticated-principal match before mutation. User calls retain the existing permission, sensitive-token, lease, snapshot, bridge-capability, transaction, and audit behavior; Agent and System calls fail before domain/device writes.
- The mutation context requires a branded server-owned refusal sink and rejects a missing or malformed sink as an internal invariant failure; refusal writers never fall back to the caller's transaction.
- Agent refusal remains `dts-reload-agent-refused`. System refusal is fixed as `dts-reload-system-refused`; System refusal audits use the platform path with `actorUserId: null`, `organizationId: null`, and the constructed service/job identity. Agent refusal audits retain principal, session, tool-call, approval, action, target, request, and `requireHuman` correlation.
- Refusal evidence uses the existing trusted audit writer through the branded sink. The real PostgreSQL matrix runs each of the 25 operation/context cells inside an outer transaction, rolls that transaction back, and verifies durable refusal evidence, unchanged domain tables, no lease/snapshot/device side effect, and no success audit.

### #612 TDD and verification evidence

- Red: `npm run test:server -- server/modules/dts-reload/routes.test.ts --run` failed 1 test on the pre-migration implementation because the configuration route passed only `{ requestId: "test-request" }`; the test required a formally constructed server-owned user invocation and client `actorType: "agent"` was not allowed to affect it.
- Focused green: `npm run test:server -- --run server/modules/dts-reload` passed 18 files / 219 tests, including service, deploy, restore-baseline, configuration, promote, routes, and the real PostgreSQL provenance matrix.
- The provenance matrix has five operations × user/agent/system/missing/malformed contexts. User cases reach the original business validation path; Agent/System cases return stable 403s with durable truthful refusal audits; missing/malformed cases raise `INVALID_TRUSTED_INVOCATION_CONTEXT`; all rejected cells have no mutation, success audit, lease, snapshot, or device call.
- Hardware/HDC validation is intentionally out of scope. The deploy matrix uses an adapter spy to prove refusal happens before the bridge/device seam; it is not hardware evidence.
- #613–#615 remain open follow-up migrations. This shared plan stays active and is not moved to `completed/` by #612.

### #612 pre-repair verification boundary (historical)

- Before the independent-review repair, `npm run test:server` timed out in 4 unchanged parameter/parameter-topology PostgreSQL integration tests, while 353 files passed / 2 skipped and 2750 tests passed / 8 skipped. The pre-repair DTS-focused command was green at 18 files / 217 tests. This historical result is retained for provenance and is not the repair result below.
- Before the independent-review repair, `npm run test:all` failed in its frontend phase on 5 existing UI tests; the same command on a clean `origin/main` worktree failed 3 different existing frontend tests. No unrelated frontend fix was included in #612.
- The earlier focused-corpus `npm run acceptance:evidence` result was not used as a #612 gate. #612 does not change operation-evidence coverage, so the command remains intentionally unrun for this repair; no HDC or hardware acceptance was requested or claimed.

## #612 independent review repair checkpoint

- The repair worktree is `/Users/tzrea1/Develop/WiseEff-td612` on `codex/td-068-dts-reload-user-provenance`, rebased without conflict from `49ddd3925` onto `origin/main@537e932ce101a76606347ce8ab0f67303ace1068`. The earlier #611 branch/worktree references below are historical and are not the current #612 branch.
- P1 Red: `npm run test:server -- --run server/modules/dts-reload/provenance.integration.test.ts` failed the caller-transaction refusal regression because the old `refusalDb: tx` path returned Agent 403 but its audit disappeared after rollback (`expected 1`, received `0`). The repair replaces the raw database field with a private-branded `TrustedRefusalAuditSink`; only `createPostgresDatabase` roots can create it, and a root-owned closure writes through the independent pool.
- P2 Red: the exact configuration audit assertion failed with `targetId: "org-1"` instead of the existing public `"dts-reload"` contract. The repair fixes that target while retaining Agent kind/code, `reason`, `requireHuman`, action, principal, session, tool-call, approval, and trace correlation.
- P2 false-green Red: after exact reason/message assertions were added while the old fixtures still omitted the refusal handle, both malformed and principal-mismatch cases failed at the earlier refusal-database guard. The fixtures now carry a valid formal sink, so malformed reaches `context must come from a server-owned constructor` and mismatch reaches `DTS reload invocation principal does not match the authenticated principal`; neither writes refusal or success audit.
- P2 HTTP coverage: the real PostgreSQL matrix now uses the public HTTP router and real five DTS domains, with body/header `actorType` spoofing on all five routes and query spoofing on configuration. It compares no-spoof and spoof business outcomes, verifies user success audit projection, state behavior, absence of Agent/System refusal, and zero deploy bridge calls. It uses an isolated object-store fake only at the pre-device seam; this is not HDC evidence.
- The shared root/sink runtime tests prove that a session wrapper, savepoint/transaction handle, or raw `{ write() }` object is not a trusted sink and fails with `INVALID_TRUSTED_INVOCATION_CONTEXT`. The DTS test helper uses the formal root/sink constructor; it does not wrap a test transaction as a sink.

### #612 repair verification result

- On the final repair code tree based on `origin/main@537e932ce101a76606347ce8ab0f67303ace1068`, `npx tsc -b --pretty false` passed; `npm run test:server` passed 358 files / 2759 tests with 2 files / 8 tests skipped; and the second exact `npm run test:all` run passed frontend 418 files / 3096 tests, scripts 69 files / 948 tests with 5 skipped, bridge 21 files / 138 tests, and server 358 files / 2759 tests with 2 files / 8 tests skipped.
- Focused evidence passed: provenance 1 file / 4 tests, routes 1 file / 5 tests, all DTS reload 18 files / 219 tests, audit 6 files / 26 tests, shared database 4 files / 47 tests, and promote cleanup 1 file / 9 tests.
- The first exact `npm run test:all` attempt failed only in the scripts phase at `scripts/finalize-gate0-upload.test.ts` (1 failed / 947 passed / 5 skipped) while parsing an incomplete `ready.json`. The direct single-file command passed 23/23 on the repair branch five times and on the clean `origin/main` worktree three times; no unrelated source fix was made. The repeat full run passed. Existing warnings included `Not implemented: navigation to another Document` and `ps: process id too large: 999999999`.
- `npm run build`, `npm run contract:check`, `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run docs:check`, `npm run selfhost:check`, and `git diff --check origin/main...HEAD` passed. Build retained existing Vite externalized-module and large-chunk warnings; lint passed with 0 errors and 299 existing warnings. `npm run acceptance:evidence` remains unrun because operation-evidence coverage is unchanged; GitHub Actions remain unavailable because the monthly quota is exhausted.
- No HDC or hardware validation is claimed. The deploy adapter spy proves only that the refusal/validation seam precedes device invocation. #613–#615 remain open, so this bilingual plan remains active.

## #613 implementation checkpoint

- Scope: parameter submission now requires one branded `TrustedInvocationContext` plus a branded, root-owned refusal sink across binding drafts, node-enablement drafts, structured DTS edits, and semantic/retained-legacy Xiaoze action paths. Direct HTTP routes construct `createUserInvocation(auth)` after authentication; request `actorType`, `initiator`, headers, and query data remain outside the provenance contract.
- The domain validates the context, refusal sink, request correlation, and user/Agent principal match before permission checks, queries, transactions, draft consumption, candidate changes, submission rows, or success audits. Nested structured submission passes the same context object. Missing, malformed, or principal-substitution contexts fail closed instead of defaulting to user.
- The Xiaoze action path requires the orchestrator-owned Agent invocation to match the durable session, tool-call, and approval ids. Approval remains correlation evidence and never changes `initiator: agent` into user. Both semantic and retained-legacy identity paths pass that same invocation into the domain.
- Critical sensitive-node submissions allow the existing capable direct user but return stable `403` for Agent and System. High-tier and non-sensitive Agent submissions retain their existing capability/approval behavior. Agent refusal audit records the accountable user, organization, session, tool-call, approval, and trace; System refusal records its service/job identity with a null actor user. Refusals use the independent root sink and survive caller transaction rollback; successful submission and its trusted audit remain in one transaction.
- #614 topology/writeback governance, #615 global legacy actor-label removal, TD-123 device-write audit, public API/DTO changes, frontend work, and HDC/hardware/live-provider readiness remain out of scope. TD-068 and parent #609 remain Open, and this plan stays active.

### #613 TDD and verification evidence

- Red evidence on the pre-migration implementation: `actionTools.test.ts` reported 2 failures because semantic and legacy submission received only handwritten `{ actorType: "agent" }`; `routes.test.ts` reported 1 failure because the HTTP route supplied no branded user invocation; `submissionProvenance.test.ts` reported 2 failures because missing/malformed context reached the database instead of failing before a transaction.
- Focused final tree: 14 files / 185 tests passed, covering routes, binding/enablement/structured submission, semantic/legacy Xiaoze, orchestrator, registry, critical policy, and PostgreSQL provenance. The owned PostgreSQL matrix covers user/Agent/System/missing/malformed and critical/high/non-sensitive outcomes; it asserts zero rejected-domain residue and zero success audit, durable refusal after outer rollback, truthful correlation, organization substitution refusal, and joint rollback of successful domain rows plus success audit.
- Independent Standards review found that a successful non-sensitive System submission was initially projected to platform scope. The added PostgreSQL assertion failed with no tenant audit row; both structured and ordinary success writers now pass the authenticated tenant scope for System while retaining `actor_type: system` and `actor_user_id: null`. The focused repair command passed 3 files / 37 tests, followed by `npx tsc -b`.
- `npx tsc -b` passed. `npm run test:all` passed: frontend 418 files / 3096 tests; scripts 69 files / 948 passed / 5 skipped; bridge 21 files / 138 tests; server 361 files / 2766 passed / 4 skipped. The first standalone full-server run had one migrated test-fixture failure (`parameter-files/integration.test.ts`, expected 201 and received 500); after injecting the formal test refusal sink, that file passed 3/3 and the complete server phase in `test:all` passed.
- `npm run build`, `npm run contract:check`, pgvector-backed `npm run docs:check`, `npm run lint` (0 errors; inherited 299 warnings), `npm run selfhost:check`, and `npm run acceptance:ci` passed. Build retained the inherited browser-externalization and large-chunk warnings; tests retained jsdom navigation, `ps: process id too large`, and planned skip output.
- Owned-runtime perception acceptance passed 4/4 at frontend `5174` and an allocated API port, without touching the existing 5173/8787 listeners. Action acceptance completed 3 passed / 1 planned skip / 4 failed: approved-write cases selected a seeded semantic binding with no source text and stopped before `submitParameterChanges` (`Config set source text unavailable for typed edit`). A clean detached `origin/main@b676a1e32` run reproduced the same focused approval failure and exact error, so no unrelated fixture repair was absorbed. Every generated database, object root, API/frontend process, and browser run was independently owned and cleaned; one recovery from the repository helper's undefined `stopRuntime` failure moved its exact object root to Trash after deleting the marker-proven database.
- No PR was created or merged and no Issue was closed. Parent/session owner retains final acceptance, PR, merge, and Issue updates.

## #613 independent review repair checkpoint

- The repair stays on `codex/td-068-parameter-submission-provenance` in `/Users/tzrea1/Develop/WiseEff-td613`. It started from feature HEAD `cbbe13fce16097e5fdd5f3ee6d4a127e3411364f` on `origin/main@b676a1e320f1d7fcc1c5e9baaba78c3510c97b14`; production and PostgreSQL repair commit `8bd4eb844` was appended without amending history.
- P1 root cause: the semantic Xiaoze early guard used `loadBindingContext.node_locator`, whose subquery selected a latest logical-node revision by string id ordering, while the central binding-draft guard used the flat parameter source projection. Neither guard supplied the exact revision's `compatible`, so a compatible-only critical rule could be bypassed.
- Both binding guards now use `loadLogicalNodeSubmissionContext`, which accepts organization, project, stable logical-node id, and an exact server-owned config revision. It has no latest or client fallback and canonicalizes the first persisted DTS compatible token using the same single-compatible match input consumed by the sensitive-rule matcher.
- Xiaoze resolves the newest binding revision by numeric `dts_config_revisions.revision_number`, restricted to a revision that also carries the binding's stable logical node, then performs the guard before value parsing, draft creation, or candidate creation. The central seam resolves from the locked draft's persisted `base_config_revision_id` and binding logical-node id; it never substitutes the candidate, current head, request data, or flat source projection.
- PostgreSQL Red on the pre-repair behavior: the new compatible-only critical action test expected Agent 403 but the promise resolved successfully with a change request. Green proves early Agent refusal before draft/candidate/round/change-request/item/success-audit creation, durable correlation after outer rollback, and central refusal of a pre-existing valid draft/candidate without consuming or promoting either. The same central draft also proves System refusal with null actor user, incapable direct-user 403 with unchanged state, and capable direct-user success.
- Existing path-critical, high-tier Agent, non-sensitive Agent, missing/malformed trusted context, route-owned user context, structured submission, and success/refusal atomicity matrices remain regression gates. This repair changes no frontend, route, DTO, public API, migration, or schema. It does not implement #614/#615 trusted-provenance work or change those Issues' interfaces or acceptance state; the shared enablement loader's quoted-compatible normalization does, however, narrowly tighten compatible-rule matching for the topology enablement edit caller described below. No HDC/device readiness is claimed.

### #613 independent repair verification result

- `npx tsc -b --pretty false` passed. After independent-review repair, the exact focused command passed 9 files / 83 tests: compatible PostgreSQL action, binding-central, and enablement-central checks; submission provenance; structured submission; action unit/sensitive tests; Xiaoze assembly; parameter routes; sensitive-node policy; and post-cutover workflow.
- The first complete server run found two Xiaoze assembly fixture failures because its module mock did not expose the new exact-node loader/trusted guard. No production fallback was added: append-only test commit `3326d8eec` wires the real production dependency shape, its file passed 5/5, and the complete server rerun passed 361 files / 2768 tests with 1 file / 4 tests skipped.
- `npm run test:all` passed on the review-repaired code tip: frontend 418 files / 3096 tests; scripts 69 files / 948 passed / 5 skipped; bridge 21 files / 138 tests; server 361 files / 2770 passed / 4 skipped. The standalone complete server rerun also passed 2770 / 4 skipped. Test output retained inherited jsdom navigation and `ps: process id too large` warnings.
- `npm run build`, `npm run contract:check`, pgvector-backed `npm run docs:check`, `npm run lint` (0 errors / inherited 299 warnings), `npm run selfhost:check`, and `npm run acceptance:ci` passed. Build retained inherited browser-externalization and large-chunk warnings.
- Owned-runtime Xiaoze acceptance passed again on review-repaired code tip `6652a1286`: 10 tests passed with 1 planned skip; action passed all 6 executable cases, perception passed 3/3, and runtime warmup passed. Run `full-20260826t151950508z-6652a128651f-dbb0f41f` used API `18800` and frontend `5180`; both processes stopped and the exact database/object root were removed. Existing `8787`/`5173` ports were not used or signalled.
- A preceding controller invocation failed before Playwright because the wrapper attempted to mark the browser phase `running` without the required process-start identity. Its owned API/frontend had already stopped; marker-bound orphan recovery removed only run `full-20260826t150116846z-3326d8eec622-b675ae8d`'s exact database/object root and verified both ports unused and the database absent. This harness retry is not reported as a product-test pass or failure.
- Fresh Standards review found a P1 inconsistency: enablement returned persisted quoted compatible text while the new binding loader canonicalized it. Commit `6652a1286` centralizes compatible-token canonicalization for both exact-revision loaders and adds owned-PostgreSQL enablement Agent/System durable refusal, unchanged draft/candidate state, truthful audit, and capable-user success coverage.
- Fresh Spec review found two P2 evidence gaps. The PostgreSQL central test now creates a higher numeric binding/logical-node revision with a different safe compatible, proving the persisted draft base wins over latest/head; a separate semantic compatible-only high rule proves an approved capable Agent still succeeds with truthful Agent audit. The bilingual workflow record now lists the complete append-only repair chain. P1/P2 are subject to a final fixed-point rereview after this documentation record.

### #613 shared enablement-loader boundary repair

- Parent review identified that `loadLogicalNodeEnablementContext` is shared by the #613 enablement-submission central guard and the topology enablement edit path owned by future #614 work. Keeping commit `6652a1286`'s normalization therefore also makes a persisted `"wiseeff,charging_core"` compatible match a compatible-only `wiseeff,charging_core` rule during `createNodeEnablementDraft`. This is an intentional narrow security correction, not a claim that #614 is implemented.
- Test commit `f661fbcfd` adds an owned-temporary-PostgreSQL regression at `postCutoverWorkflow.integration.test.ts`. Through the public `createNodeEnablementDraft` service seam it binds organization, project, exact config revision, and stable logical-node identity; proves an incapable direct user receives 403 before draft/candidate creation; and proves a direct user with `parameter:edit-critical` still creates exactly one draft/candidate. The test does not assert Agent/System provenance on this legacy path.
- Sensitivity Red used a temporary, uncommitted mutation that returned raw `row.compatible` from only the enablement loader. The exact focused command then failed because the incapable-user promise resolved with an enablement draft/candidate instead of rejecting. `apply_patch` restored `canonicalizeLogicalNodeCompatible(row.compatible)`; the restored `writeLock.ts` blob `e5d3d3b3a7524ca01a95e018d8f60bc697e14816` and SHA-256 `5dd686210d8fed05040e068a1a5f09065896a63dd22ac0792f9dbd629312c4e8` match the pre-mutation file, and production diff is empty.
- Final Green passed the focused file at 15/15, `npx tsc -b --pretty false`, and the requested 9-file matrix at 75/75. `npm run test:all` passed: frontend 3096/3096; scripts 948 passed / 5 skipped; bridge 138/138; server 361 files / 2771 passed / 4 skipped with one planned-skipped file. `npm run build` and `npm run contract:check` passed with the inherited browser-externalization and large-chunk warnings.
- Pgvector-backed `npm run docs:check`, `npm run lint` (0 errors / inherited 299 warnings), `npm run selfhost:check`, `npm run acceptance:ci`, and `git diff --check origin/main...HEAD` passed on the exact repair tree. Test output retained the inherited jsdom navigation and `ps: process id too large` notices; no skip or warning is reported as a pass.
- #614 remains Open and still owns topology/writeback migration from legacy `actorType` to `TrustedInvocationContext`, including Agent/System provenance, refusal-audit durability, interfaces, and its full acceptance matrix. No Xiaoze owned-browser, viewport, Hosted, HDC, hardware, or live-provider verification was rerun or claimed for this test/docs-only repair; the earlier `6652a1286` Xiaoze acceptance remains historical evidence only.

## #614 implementation checkpoint

- Scope: migrate topology node-enablement draft creation, property-key cutover prepare, semantic parameter writeback, retained-legacy parameter writeback, and enablement writeback to one required branded `TrustedInvocationContext` and one branded refusal sink created from a server-owned PostgreSQL root. The review/software-merge route now constructs a User invocation and propagates it unchanged to all three writeback modes; internal Agent/System callers can provide their original invocation explicitly.
- The shared sensitive-write guard validates the frozen invocation brand, non-empty request id, branded root sink, and exact User/Agent principal and organization before policy or mutation. A capable direct User still passes critical rules; Agent and System receive the stable `parameter-sensitive-node-human-required` refusal. High and no-match behavior is unchanged, and successful Agent/System audit projection is not downgraded to User.
- HTTP routes are the User-provenance construction boundary. Body, query, and header `actorType`/provenance spoof values are ignored and are not added to DTOs or OpenAPI. Nested services do not construct User invocations or accept a raw transaction/database as the refusal sink.
- Property-key cutover uses a complete preflight pass over every would-rewrite location before the first candidate, source version, or object put. The PostgreSQL regression places the critical location second and proves Agent/System refusal leaves the run/items, candidates, current/source versions, object store, and success audits unchanged while the independent refusal audit remains durable.
- Topology, cutover, and writeback success audits use trusted projection from the same invocation and trace. Their database mutations and success audit share one transaction. Audit-failure injection rolls back drafts, candidates, current versions, binding revisions, review decisions, and merged change-request state. Immutable blob puts that precede a later database audit failure are physically non-transactional; tests prove the blob has no database row or current-version reference and report it as unreachable rather than claiming database-level object-store atomicity.
- The owned PostgreSQL provenance coverage spans all five operations and User, Agent, System, missing, malformed/unbranded, empty-request-id, forged-sink, cross-user, and cross-organization contexts. Separate risk cases cover critical, high, no-match, capable/incapable User, full Agent approval provenance, System service/job identity, durable refusal after caller rollback, and success-audit failure rollback. Object-store spies prove rejected validation/policy cells perform no put.
- The only narrow shared audit change is optional trusted invocation support in candidate creation. Existing candidate audit input could only project legacy User auth, so property-cutover System/Agent high/no-match success could not truthfully preserve initiator metadata. Existing callers remain source-compatible and #610-#613 behavior is retained.
- TDD Red: `npm run test:server -- --run server/modules/parameter-kernel/sensitiveNode.test.ts` failed 1 test (11 passed) because `assertTrustedSensitiveNodeWriteAllowed` did not exist. Green introduced the shared guard. After review repair, the expanded affected PostgreSQL command passed 14 files / 177 tests, including the inherited #613 exact-compatible enablement regression with a formal root-owned sink fixture.
- Final exact-tree verification after rebasing without conflict to `origin/main@e9d29dc293c377e48a89f3f94e6e8bb8f45a9807`: `npx tsc -b --pretty false` and the 14-file / 177-test affected PostgreSQL matrix passed; complete server passed 363 files / 2784 tests with 1 skipped; scripts passed 69 files / 948 tests with 5 skipped; bridge passed 21 files / 138 tests. Build, contract, pgvector-backed docs (including a non-skipped current schema artifact), lint (0 errors / 299 inherited frontend warnings), self-host, acceptance CI, and diff checks passed. Build retained the existing five browser-externalized Node-module warnings and large-chunk warning; tests retained jsdom navigation and oversized-process-id informational stderr.
- The final `npm run test:all` attempt is not claimed green: its frontend phase stopped the shell chain with one 5-second timeout in `NodeDebuggingPage.test.tsx` (417 files / 3105 tests passed; 1 file / 1 test failed). The direct file rerun passed 60/60, and `git diff origin/main...HEAD -- src` is empty, so the exercised frontend tree is identical to the final base. Earlier attempts had the same varying timeout class, including a clean detached `origin/main@5d5e35cff` comparison. No frontend fix was absorbed. Later phases were run independently with the passing counts above.
- Initial dual-axis review reported six findings. Spec: P1 merge audit still used legacy User projection for internal Agent/System; P1 capability denial could be mislabeled human-required; P2 cutover second-location evidence omitted exact source/current identity and refusal rows. Standards: P1 candidate creation accepted a cross-principal trusted invocation; P2 new PostgreSQL cleanup could hide standalone cleanup failure; P2 a unit refusal sink targeted the shared configured database. Repairs use the shared invocation/auth matcher before candidate put, capability-before-initiator ordering, trusted merge audit projection, exact cutover state/refusal assertions, primary-error-preserving cleanup, and an owned temporary unit database. Successful Agent/System merge audits and incapable Agent/System no-human-refusal behavior are now explicit PostgreSQL assertions. The repaired complete server suite again passed 363 files / 2784 tests with 1 skipped.
- The second Standards review found two remaining issues: System merge metadata still named the authenticated user as the executor, and the shared candidate context was not normalized by abandon/recompute/activate. The follow-up repair projects User, Agent tool, or System job/service identity into the execution participant and validates the trusted candidate context at every exported mutation entry. A non-create cross-user regression proves abandon state is unchanged. The 14-file / 177-test affected matrix and complete 363-file / 2784-test server suite passed again after this repair.
- Final independent Standards and Spec reviews on the fixed rebased HEAD each reported 0 findings (P0-P3). Stable patch IDs and `git range-diff` showed all eight implementation/repair commits remained patch-equivalent across the final unrelated-main rebase.
- Explicit exclusions: #615 legacy API/type ratchet and TD-068 closure, TD-123 device-write audit, frontend/public DTO/schema/migration changes, preview/start/finalize cutover migration, Xiaoze #611, DTS reload #612, HDC, hardware, live providers, and Hosted CI. The GitHub Actions monthly quota remains exhausted and no Hosted pass is claimed. This plan remains active until #615 completes TD-068.

## #614 parent-review repair checkpoint

- Parent review found two P1 defects in feature HEAD `43549fd0976e40f0c73eb6a527d4310c1016d417`: compatible-only policy looked through each file's mutable `current_version_id` instead of the cutover location/write-lock version, and successful Agent/System operations still copied `auth.user` into domain creator/reviewer/history fields and merge notifications.
- Exact-version Red used owned PostgreSQL with a critical compatible on the second cutover location's pinned version and a safe current head. The pre-repair promise resolved and staged the operation instead of returning 403. Green adds a server-owned `sourceFileVersionId` seam and constrains the resolver by Organization, project, file name, file version, and node locator. A version outside that scope fails closed; a structurally absent node retains compatible no-match. Semantic and enablement write locks now pass their pinned version and logical-node locator, and retained legacy writeback explicitly passes the current version it actually patches. The reverse locked-safe/current-critical System case succeeds, proving there is no mutable-head false refusal.
- Domain-attribution Red required a successful System semantic writeback to persist `created_by_user_id = null`; pre-repair PostgreSQL stored the unrelated authenticated user id. Green projects the accountable user only from the trusted invocation: User/Agent use `invocation.principal.user`, while System writes null to nullable candidate, file-version, config-revision, history/value creator/changer columns. Agent notifications use `Agent tool:<toolCallId> (session:<sessionId>)`; they never describe the principal as the direct executor.
- `parameter_drafts.user_id` and `parameter_review_decisions.reviewer_user_id` are nullable accountable-principal fields after the owner-authorized migrations. No synthetic user is introduced. User and Agent rows carry the validated principal user; System rows carry null user attribution plus the service/job identity required by the database checks. Critical System requests still reach sensitive preflight first and retain `parameter-sensitive-node-human-required`; high/no-match System topology drafts and software merges preserve their original successful behavior. The independent root sink records critical refusals with truthful service/job identity and null actor user. Agent keeps its validated principal for accountability while audit, history, and notification retain Agent execution identity.
- PostgreSQL regressions cover divergent cutover/semantic/enablement versions, critical Agent/System zero staging, reverse-version System success, nullable System creators, Agent principal-owned draft/review/history rows, truthful notification body/metadata, cross-principal rejection, durable domain-model refusal, and audit-failure rollback with no merge notification. Immutable object puts that precede an injected database audit failure remain physically non-transactional but unreachable from committed database state.
- Repair verification before the final fetch passed `npx tsc -b --pretty false` and the seven-file focused matrix (84/84), including owned PostgreSQL cutover and five-path provenance workflows. The complete server suite passed 362 files with one planned-skipped file and 2784 tests passed / 4 skipped. One pre-fetch `npm run test:all` passed all phases: frontend 418 files / 3106 tests; scripts 69 files / 948 passed / 5 skipped; bridge 21 files / 138 tests; server repeated the same 362-file / 2784-test result. After the final fetch confirmed unchanged `origin/main@e9d29dc293c377e48a89f3f94e6e8bb8f45a9807`, typecheck, focused 84/84, standalone server, scripts, bridge, build, contract, pgvector-backed docs/schema (not skipped), lint (0 errors / 299 inherited frontend warnings), selfhost, acceptance CI, and diff check passed again. Two final-HEAD `test:all` attempts nevertheless stopped in frontend: first, one DTS reload and one Node debugging 5-second timeout (416 files / 3104 passed), whose exact two-file rerun passed 100/100; second, one App auth-config assertion and the same Node timeout (416 files / 3104 passed), whose exact two-file rerun passed 199/199. `git diff origin/main...HEAD -- src` is empty; the explicit no-new-worktree rule prevented a fresh clean-main worktree comparison. These attempts are not claimed green. Output retained the inherited Vite five-module browser externalization and large-chunk warnings, jsdom navigation stderr, and oversized process-id stderr. Hosted CI remains unavailable; #615, TD-123, schema, frontend, HDC, hardware, and live-provider work remain excluded. This plan stays active.
- A later fixed-HEAD review found that semantic/enablement policy still obtained compatible from a physical overlay row rather than the exact logical-node revision pinned by `baseConfigRevisionId`. The owned-PostgreSQL Red removed the synthetic overlay `dts_nodes` row; both Agent/System critical writebacks incorrectly succeeded. The write lock now resolves locator and canonical compatible from `organization + project + base config revision + logical node`, carries both as authoritative lock context, and never consults a newer unbound logical-node revision or physical-file fallback for these paths.
- The same review narrowed trusted candidate provenance to candidate creation, the only #614 shared mutation. Abandon, recompute, and activate retain their legacy correlation-only contract and reject any runtime trusted invocation before their first read/write, because their nullable/non-null user attribution and nested DTS sync audits have not been migrated. This avoids a truthful trusted candidate audit beside false User domain rows without absorbing #615 or a platform candidate-actor migration.
- Spec review also found ambiguous nested cutover lookup: the exact-version resolver preferred the stripped parent locator over the supplied full node locator. A real PostgreSQL Red with a safe parent and critical second-location child incorrectly staged candidates. The resolver now prefers the exact full locator and falls back to its parent only for callers that supply a property path. The reverse critical-parent/safe-child System guard succeeds without audit, object put, or domain mutation.
- Final repair verification on the post-fetch tree passed typecheck, the owned-PostgreSQL 8-file / 84-test focused matrix, and the complete server suite (363 files; 2787 passed / 1 skipped). The first complete-server attempt exposed four `editService.test.ts` failures after an attempted typed-draft exact-loader expansion; that expansion was outside the required write-lock seam and incompatible with candidate working-tip revisions, so additive commit `85257b8b3` restored the pre-existing typed-draft boundary while retaining exact write-lock resolution. The exact two-file regression then passed 43/43 and the complete server rerun passed.
- Final `test:all` is not claimed green: the unchanged frontend stopped on the known `NodeDebuggingPage.test.tsx` 5-second timeout (417 files / 3105 passed; 1 file / 1 failed), while the exact file rerun passed 60/60 and the branch has no `src/**` diff. Independent final-tree commands passed scripts (69 files / 948 passed / 5 skipped), bridge (21/138), build, contract, pgvector-backed docs/schema (not skipped), lint (0 errors / 299 inherited warnings), selfhost, acceptance CI, and diff check. Output retained jsdom navigation, oversized-process-id, five browser-externalization, and large-chunk warnings. Hosted CI was not run.

## Documentation Impact Matrix

| Area | Status | Evidence |
| --- | --- | --- |
| Repository maps and agent guidance | Review | `AGENTS.md`; it already routes security/auth/audit work to the relevant docs. |
| Planning and technical-debt tracking | Review | `docs/PLANS.md`, `docs/exec-plans/tech-debt-tracker.md`; both retain TD-068 Open and its migration boundary. |
| Product and API contracts | Review | `docs/design-docs/api-contract.md`, `server/modules/contracts/`; route paths and request contracts remain unchanged, while server-owned provenance and actor-field stripping are covered by route tests. |
| Architecture and domain model | Review | `docs/adr/0038-trusted-invocation-provenance-separates-principal-and-initiator.md`, `docs/design-docs/full-stack-architecture.md`, `docs/design-docs/domain-model.md`. |
| Security and audit guidance | Review | `docs/SECURITY.md`, `server/modules/audit/auditedWrite.ts`; trusted-context and no-default-user rules remain a partial migration. |
| Quality and verification docs | Review | `docs/QUALITY_SCORE.md`, `docs/developer/verification-matrix.md`; #614 adds five-operation trusted-provenance, second-location preflight, and audit-rollback PostgreSQL coverage while keeping HDC outside its evidence boundary. |
| Chinese developer docs | Review | `docs/zh-CN/SECURITY.md`, `docs/zh-CN/design-docs/full-stack-architecture.md`, `docs/zh-CN/design-docs/domain-model.md`, `docs/zh-CN/PLANS.md`. |
| Generated artifacts, runbooks, frontend/design, references | Updated generated artifact | `docs/generated/db-schema.md` records the owner-authorized 0116-0121 columns/checks and nullable enablement binding identity; `docs/runbooks/`, `src/`, and `docs/references/` remain unchanged. |

## Documentation Update Gate

- [x] ADR-0038 and the bilingual security/architecture/domain/API planning references were reviewed against the implemented seam.
- [x] No public contract or frontend documentation became stale.
- [x] #614 scope, TDD Red/Green, five trusted write paths, durable refusal, success-audit rollback, object-store boundary, and HDC/Hosted exclusions are recorded here and in the synchronized Chinese plan.
- [x] #614 did not absorb or modify the original worktree's unrelated `src/App.tsx` and `src/App.test.tsx` changes.
- [x] Historical pre-repair frontend/full-server failures and clean-`origin/main` reproductions remain recorded separately from the current repair result; the current exact-tree server and `test:all` reruns are recorded with their warnings and skips.
- [x] Resolved the inherited acceptance-quality failures through #617 / PR #619 and reran the complete required local CI on the rebased final tree.
- [x] #610 obtained zero-finding final Standards and Spec reviews before its merge; #611 receives its own final parent review after this landing record.
- [ ] Move this plan to `completed/` only after the complete TD-068 migration and closure evidence land.

## Git & PR Workflow

The #610-#613 branch/worktree references above are historical. The #614 implementation is on `codex/td-068-parameter-governance-provenance` at `/Users/tzrea1/Develop/WiseEff-td614`. It started from fetched `origin/main@8b6a2ad2d80e6bdd24af150863ef1c7293039dbe` and rebased without conflict as main advanced, finally to `origin/main@e9d29dc293c377e48a89f3f94e6e8bb8f45a9807`. This session does not create or merge a PR, close #614/#609, or change Issue labels. TD-068 remains active because #615 owns the final legacy ratchet and closure evidence. The parent/session owner retains final PR, merge, Issue, and main-synchronization authority.

## #614 owner-authorized repair continuation (exact node identity and System domain execution)

This continuation supersedes the earlier parent-review limitation that treated every System high/no-match operation as a user-owned-domain refusal. The owner decision for #614 is that lower-tier and no-match public behavior remains unchanged: a System invocation may complete those operations, while critical sensitive rules still require a direct User and return the stable human-required refusal. The repair remains bounded to the five #614 production paths and their direct domain consumers.

- Repair started from feature HEAD `43549fd0976e40f0c73eb6a527d4310c1016d417`. After fetching `origin/main@948f6fedd57bd13a2ee8b880fc500ef8b5917466`, the branch was rebased without conflict to `b083ad453294560c524923e93a55af85248b2a0f`; the append-only Red commit is `510da3f1c70ce9f4deaeb54b040f91aea661821f`. A later fetch observed `origin/main@e87d7d23d6c9e442839527b0bc9879ce27d83b37`; final rebase and fixed-HEAD evidence will be recorded below after the repair commits.
- The second parent-review P1 was an exact-node fail-open: a left-lateral compatible query returned the same null as a missing `dts_nodes` row, and a shared path helper silently stripped a complete node locator to its parent. The new discriminated source-path seam distinguishes `node-locator` from `property-path`. Exact version lookup first binds Organization, project, file name, and file version; DTS node lookup then requires the complete locator. A missing exact node returns `CONFLICT` with `parameter-sensitive-node-identity-mismatch`; a present node whose `compatible` is SQL NULL remains a valid no-compatible result. Only an explicit property-path caller removes its property segment; node-locator callers never inherit a parent.
- Exact locked versions are now used by cutover preflight and semantic/enablement writeback. A non-authoritative caller compatible value is ignored when a source file/version is supplied; topology revision compatible remains authoritative only when loaded from the exact server-owned revision. No mutable `current_version_id`, latest revision, client compatible, or implicit parent fallback is used. The guard also rejects a target Organization different from the authenticated scope before rule lookup.
- TDD Red was genuine on the old implementation: the focused resolver/cutover test reported `ApiError expected rejection, promise resolved` after staging the missing child; System topology high and System software merge each raised `ApiError: This workflow requires an accountable user principal.` from the pre-repair `requireTrustedAccountableUser` calls. These failures were produced through the public production seams, not manufactured mock assertions. Green is covered by owned PostgreSQL cutover, semantic, enablement, and merge tests.
- The owner-authorized domain model uses nullable user attribution plus explicit initiator metadata. Migration `0116_parameter_execution_provenance.sql` adds `initiator_type`, service/job identity, and Agent correlation columns to drafts, review decisions, history/value rows, file versions/candidates, config revisions, and binding revisions; `parameter_drafts.user_id` and `parameter_review_decisions.reviewer_user_id` become nullable. Follow-up migration `0117_parameter_system_user_attribution_guard.sql` prevents System rows from carrying a user id, while `0118_parameter_governance_execution_identity.sql` adds the same truthful nullable submitter/initiator contract to submission rounds and change requests and permits logical-node enablement rows without a binding id; `0119_parameter_draft_enablement_owner_index.sql` removes the obsolete user-only enablement index. No synthetic user is introduced.
- Domain semantics are explicit: User and Agent use the trusted principal only as the accountable user; Agent remains the initiator with session/tool-call/approval metadata; System has null user attribution and a real service/job identity. History, file/version/candidate/config/binding projections, review decisions, workflow trail, and merge notifications consume the same trusted projection. System high/no-match topology drafts and software merges remain successful; the user-visible executor is `System <kind>:<name>`, and Agent uses `Agent tool:<toolCallId> (session:<sessionId>)`. Critical Agent/System refusals still use `parameter-sensitive-node-human-required` and durable root-sink evidence.
- PostgreSQL coverage includes operation × User/Agent/System × critical/high/no-match, missing/malformed/empty trace, forged sink, cross-user/cross-Organization, divergent locked/current compatible versions, missing exact child with a safe parent, exact compatible NULL, and reverse safe-locked/critical-current cases. Assertions cover rows, status, versions, locks, revisions, notification bodies/metadata, success/refusal audit projection, and object-store puts. Cutover preflight places the critical location second and proves zero candidate/item/source/current/object/success-audit mutation on refusal.
- Database success audit and domain writes share the transaction. Injected audit failure rolls back draft/candidate/current-version/file-version/binding-revision/history/value/review/change-request state and leaves no notification. If an immutable blob was put before a later database error, the test reports the physical blob as an unreachable orphan; object storage is not claimed to be transactionally atomic. Refusal evidence is written through the independently owned root pool and survives caller rollback.

### Final owner-repair evidence after exact-node and System execution findings

- The final repair rebase was performed from a clean worktree onto `origin/main@fabd2a52f299418d69c22b3656079a487cdb5923` with no conflicts. The append-only repair commits are `854cc3da5` (production exact identity/domain attribution), `2c5fad47e` (PostgreSQL and resolver regressions), `6a0fc8907` (the previous bilingual repair record), and `df3203beb` (typed-draft caller provenance and exact legacy source-version compatibility); rebase changed commit ids only as required by Git and did not reorder, squash, drop, or amend the existing chain.
- A second Red was added before the execution-label fix: `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run test:server -- --run server/modules/parameters/serviceReviewWorkflow.integration.test.ts` failed 1/29 because an Agent high-risk merge workflow trail exposed `SRW Editor` instead of the Agent tool/session label. Green is 29/29 after `reviewDecisionExecutionLabel` and participant projection prioritize the trusted initiator; Agent now renders `Agent tool:<toolCallId> (session:<sessionId>)`, while the accountable principal remains the validated user.
- The exact-node Red remains recorded above: the public cutover seam with a safe parent and missing critical child resolved and staged under the old lateral compatible lookup (`ApiError expected rejection, promise resolved`). Green now distinguishes missing exact identity (`CONFLICT`, `parameter-sensitive-node-identity-mismatch`) from an existing node whose compatible is SQL NULL, and only an explicit property-path input may resolve its owning node. No mutable current-version or client-compatible fallback is used.
- Rebase-era Green evidence: the eight-file owned-PostgreSQL affected matrix passed 8 files / 118 tests; the exact Agent/structured/JSON regression rerun passed 4 files / 23 tests; the complete server suite passed 363 files / 2,801 tests with 1 skipped. `npm run test:all` passed frontend 420 files / 3,137 tests, scripts 69 files / 948 passed with 5 skipped, bridge 21 files / 138 tests, and server 363 files / 2,801 passed with 1 skipped. A no-`TEST_DATABASE_URL` server invocation was attempted first and failed during global setup against the un-migrated default database (`relation "project_parameter_values" does not exist`); the required port-5433 rerun is the authoritative result.
- Internal Agent typed-draft callers now pass their original trusted invocation into candidate/file-version/draft/rebase/governance projections. A legacy user-owned draft can be inspected only by an explicit same-principal Agent/System preflight for a truthful sensitive refusal; the service rejects owner mismatch before any submission write. Legacy structured submission resolves the server-owned file's current version before the exact compatible guard, preserving #613 behavior without accepting client version data.
- The owner-authorized nullable user-attribution model remains bounded to the five #614 paths and direct consumers: System rows use null user ids plus checked service/job identity; User and Agent rows use the validated principal, with Agent execution labels and metadata retained. No synthetic user, public selector, frontend change, unrelated legacy ratchet, #615 work, TD-123 work, schema outside these consumers, or Hosted CI claim is included. This plan remains active until #615 completes the broader TD-068 cleanup.
- This checkpoint records the bounded schema/domain change and generated `docs/generated/db-schema.md` update. It does not absorb #615 legacy ratchet or close TD-068, and it does not touch TD-123, frontend/public selector contracts, HDC, hardware, live providers, or Hosted CI. The plan remains active until #615 completes the broader TD.

## #614 follow-up repair: execution-identity constraints and carried provenance

- The follow-up started from the parent-reviewed `43549fd0976e40f0c73eb6a527d4310c1016d417` chain and was rebased without conflict onto fetched `origin/main@fabd2a52f299418d69c22b3656079a487cdb5923`. It addresses the remaining Standards P1s: bidirectional User/Agent/System domain identity, carried binding-revision provenance, and Agent draft cleanup ownership. Existing commits remain append-only.
- Migration `0120_parameter_execution_identity_checks.sql` establishes those strict checks as `NOT VALID`: trusted User/Agent rows carry the validated principal and no System fields; System rows carry no user attribution and a non-empty `service`/`job` identity. PostgreSQL still enforces the predicate for every new or updated row while existing pre-#614 rows with no historical attribution remain upgradeable. Migration `0121_parameter_execution_identity_legacy_compatibility.sql` reasserts the same strict predicate and does not create a write-time legacy-null escape hatch. A future owned backfill may validate the checks after historical rows are resolved. No synthetic or borrowed user is introduced. Generated schema documentation records 119 migrations and the `NOT VALID` constraints.
- `carryForwardBindingRevisions` now receives the same trusted domain attribution as the candidate/config revision and writes initiator type, System identity, and Agent correlation on copied rows. The Agent action cleanup uses the exact trusted owner projection rather than a bare principal `userId`, preventing a failed submission from deleting another execution's draft.
- Red evidence for the exact-node P1 remains the owned-PostgreSQL cutover test's pre-repair `ApiError expected rejection, promise resolved`: a missing exact child was treated as compatible no-match and staging continued. The earlier System high/no-match Red was the pre-repair `This workflow requires an accountable user principal.` from topology/merge after sensitive policy had passed. Green now keeps System high/no-match successful, preserves critical Agent/System 403 plus durable refusal, and distinguishes missing exact node from persisted `compatible IS NULL`.
- A migration-upgrade Red seeded a pre-0120 history row with the historical `user` default and null creator, and the old 0120 failed before 0121 (`check constraint "parameter_history_entries_execution_identity_check" ... is violated`). Green changes 0120 to strict `NOT VALID` and 0121 to a strict reassertion: the legacy row survives the upgrade, while an identical new unprojected insert fails with PostgreSQL `23514`. The complete server suite passed 363 files / 2,805 tests with 1 skip. The affected #614 PostgreSQL matrix now includes this upgrade regression alongside exact-node, cutover second-location, semantic/legacy/enablement writeback, merge notification, migration, and Agent cleanup coverage.
- Domain semantics remain: User and Agent accountable fields come from the validated invocation principal; Agent execution labels retain tool-call/session/approval; System user attribution is NULL and service/job identity is queryable. Success audit, domain rows, and notifications use the same invocation projection. Audit failure rolls back reachable database state; an immutable blob written before a later database failure is reported only as an unreachable orphan. The active plan remains active and excludes #615, TD-123, frontend, HDC, hardware, live providers, and Hosted CI.
- Before the final fetch/rebase, fixed code `fa40de204` passed the 7-file / 114-test focused PostgreSQL matrix, `npm run test:server` (363 files / 2,805 tests, 1 skip), and `npm run test:all` (frontend 420 / 3,137; scripts 69 / 948 with 5 skips; bridge 21 / 138; server 363 / 2,805 with 1 skip). `npx tsc -b --pretty false`, build, contract, pgvector-backed `docs:check` with schema active, self-host, acceptance, and `git diff --check` passed. Lint returned 0 errors and 301 inherited frontend warnings. Build retained five browser-externalization warnings and one large-chunk warning; test stderr contained three existing jsdom navigation messages and the scripts suite's `ps: process id too large: 999999999` informational line. No frontend timeout occurred. A final fetch then observed `origin/main@d7be22aeee662ddcf9c807a3945db9a0deb672c3`; the clean rebase and post-rebase evidence are recorded in the next checkpoint. Hosted CI remains unavailable because the monthly quota is exhausted and is not claimed as passed.

## #614 final review repair: migration upgrade safety

- The two fresh fixed-HEAD review findings were addressed append-only after `fd2e1db73`: `24070ca23` makes the existing branch-introduced 0118/0119 files diff-clean and installs the 0120/0121 identity checks as `NOT VALID`, with a real PostgreSQL upgrade Red/Green test; `fa40de204` adds the explicit `legacy` marker needed by non-TD-068 writers and keeps legacy rows out of public trusted initiator projections. No prior commit was amended, dropped, reordered, or squashed.
- TDD Red used `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run test:server -- --run server/modules/parameter-topology/migration.test.ts -t 'preserves legacy null rows'` against the pre-repair 0120/0121 SQL. Applying 0120 after 0119 and a historical null-attribution row failed before 0121: `check constraint "parameter_history_entries_execution_identity_check" of relation "parameter_history_entries" is violated by some row`. Green applies 0120/0121 successfully, marks the old unprojected row `initiator_type = 'legacy'`, keeps creator-bearing rows as `user`, and rejects a new explicit `initiator_type = 'user'` + null creator with PostgreSQL `23514`.
- The bounded domain model remains truthful: #614 trusted User/Agent/System writes use only the three branded initiator types and strict principal/System metadata checks. The explicit `legacy` marker is the temporary, queryable state for unrelated pre-TD-068 writers and is owned by #615's later ratchet; it cannot carry System metadata. A narrow database trigger converts a legacy-default row with a non-null creator into `user`, preserving old accountable-user semantics without inventing a principal. DTO mappers suppress the internal marker rather than exposing an unsupported public actor selector.
- The migration upgrade test uses an owned temporary PostgreSQL database and replays through 0119 before applying 0120/0121. It verifies upgrade success, legacy marker conversion, creator-bearing User preservation, strict new-row rejection, and cleanup through the existing `try/finally` temporary-database helper. Generated `docs/generated/db-schema.md` records the `legacy` default and `NOT VALID` checks. The focused repair slice currently passes 7 files / 114 tests and the complete server suite passes 363 files / 2,805 tests with 1 skip; final fixed-HEAD gate counts are recorded after the final rerun.
- This repair does not migrate #615's legacy API/type ratchet, DTS reload #612, TD-123, frontend/public contracts, or platform audit architecture. It does not claim Hosted CI, HDC, hardware, or live-provider evidence. The plan remains active until TD-068's later legacy cleanup is complete.

## #614 final fixed-HEAD evidence after rebase

- The final fetch observed `origin/main@d7be22aeee662ddcf9c807a3945db9a0deb672c3`. The clean branch was rebased onto that commit with no conflicts; no files required conflict handling. The post-rebase code/evidence checkpoint is `1420f47a808fea9adb38edf803b7872581de83bc` (the following docs-only commit records this checkpoint and does not change production behavior). The branch is ahead 34 and behind 0.
- Post-rebase TDD/Green evidence is unchanged and green: the focused owned-PostgreSQL matrix (`sensitiveNode`, cutover, writeback provenance, post-cutover workflow, writeback service, review workflow, and migration) is 7 files / 114 tests; `npm run test:server` is 363 files / 2,805 tests with 1 skip. `npm run test:all` passed frontend 420 files / 3,138 tests, scripts 69 files / 948 passed with 5 skips, bridge 21 files / 138 tests, and server 363 files / 2,805 passed with 1 skip.
- All post-rebase static gates passed: `npx tsc -b --pretty false`, `npm run build`, `npm run contract:check`, pgvector-backed `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run docs:check` (schema check active and current), `npm run lint`, `npm run selfhost:check`, `npm run acceptance:ci`, and `git diff --check origin/main...HEAD`. Lint reported 0 errors and 301 inherited frontend warnings. Build reported the five existing browser externalization warnings (`stream`, `http`, `url`, `https`, `zlib`) and the existing large-chunk warning. Tests emitted three existing jsdom navigation informational messages and `ps: process id too large: 999999999` from the scripts suite. No frontend timeout occurred.
- The final review repair keeps the bounded migration model: 0120/0121 are strict `NOT VALID` checks for trusted User/Agent/System rows, while explicitly marked metadata-free `legacy` rows preserve unrelated pre-TD-068 writers until #615's ratchet. The migration-upgrade test proves old rows survive, creator-bearing rows remain User, and a new explicit User-with-null attribution is rejected with `23514`; DTO mappers do not expose `legacy` as a public initiator. This does not claim TD-068 complete, and Hosted CI remains unavailable due to the monthly quota.

## #614 Standards P1 repair: populated binding-revision upgrade

- The fresh fixed-HEAD Standards review of `d7be22aeee...c312756ee` found two items. The P1 was a migration-order defect: 0120 backfilled a populated binding-revision row to `legacy` before replacing the old User/Agent/System-only type check. The P2 required an exact-final pgvector-backed `docs:check` after the evidence-only plan commit. No other P0-P3 findings were reported by either axis.
- TDD Red used the owned PostgreSQL upgrade test after adding a real `project_parameter_bindings` row and a pre-0120 `project_parameter_binding_revisions` row with the historical User default and null attribution. The old migration failed at the backfill with `new row for relation "project_parameter_binding_revisions" violates check constraint "project_parameter_binding_revisions_initiator_type_check"`; this was a production upgrade failure, not a manufactured assertion.
- Green is `409136b8871b785226e6893e7d577eb485aa0200`: 0120 now replaces the binding-revision type check to include `legacy` before setting the default and converting historical rows. The upgrade test now asserts the binding revision becomes metadata-free `legacy`, creator-bearing rows remain User, and a new explicit User-with-null write is rejected with `23514`. The focused seven-file matrix passed 114/114 and the full server suite passed 363 files / 2,805 tests with 1 skip.
- `TEST_DATABASE_URL=postgres://wiseeff:wiseeff@127.0.0.1:5433/wiseeff npm run docs:check` passed on the repaired code HEAD before this plan update; after this final evidence-only plan commit it was rerun on the exact final HEAD and passed again (`Documentation governance check passed`; `db-schema artifact is current`). The generated schema remains current and records the legacy marker plus `NOT VALID` checks. The plan remains active; #615, TD-123, frontend, Hosted CI, HDC, hardware, and live-provider evidence remain excluded.

## #614 final fixed-HEAD repair continuation (parent-review findings)

This checkpoint records the repair after the parent review of the fixed #614
HEAD. It remains an active plan: #615 still owns the legacy ratchet and the
broader TD-068 closeout.

- Start state was clean feature HEAD `f29bf4956c76688b76467b01ba1b81e903a63b36` (review base `e9d29dc293c377e48a89f3f94e6e8bb8f45a9807`, fixed base `d7be22aeee662ddcf9c807a3945db9a0deb672c3`). The required fetch observed `origin/main@246730efefb97336428618a20bbc809334bc6fce`; the existing chain was rebased without conflict to `793ac0b192c78104f02f9cc21ed92e2544c20833` before these additive repair commits. No earlier commit was amended, dropped, reordered, or squashed.
- Four real Reds were recorded before Green: malformed Agent correlation was accepted by the pre-0122 database checks; a trusted System typed binding draft reused `auth.user.id`; an incapable critical Agent/System received only generic capability denial and no durable human-required refusal; and merge notification/public projection exposed raw System/correlation identity. The Reds came from owned PostgreSQL production seams and public repository/notification projections, not synthetic assertions.
- Migration `0122_parameter_execution_identity_discriminated_constraints.sql` is the forward database boundary. It drops/recreates the related checks with a complete User/Agent/System/legacy truth table on drafts, review decisions, history, values, file versions, file candidates, config revisions, submission rounds, change requests, and binding revisions. The union is `NOT VALID` for historical rows but is enforced for every new or updated row. User and Agent require a user column; Agent requires nonblank session/tool-call/approval; System requires only `service|job` plus a nonblank name and has null user/correlation; legacy is metadata-free. A narrow trigger keeps creator-bearing historical legacy-default rows as User; it never rewrites malformed explicit User/Agent/System rows into legacy. #615 owns the eventual legacy backfill/validation ratchet.
- The trusted sensitive resolver now binds compatible-only rules to the server-owned exact file/version and complete node locator. It constrains Organization, project, file name, file version, and node identity; a missing exact node is a stable `409 parameter-sensitive-node-identity-mismatch`, while an existing node with `compatible IS NULL` remains a valid no-match. Only an explicitly typed `property-path` may resolve its owning node; an ambiguous trusted caller defaults to the stricter `node-locator` semantics. No mutable current/head/client-compatible fallback is used.
- System high/no-match behavior remains successful as required by Issue #614. Nullable domain user attribution is null for System; service/job kind is persisted separately. User and Agent accountable fields use the trusted invocation principal, while Agent remains an Agent initiator with internal correlation. Candidate/file-version/config-revision/draft/history/value/review/binding records, audit, workflow participants, and notification all derive from one invocation. Public DTOs retain their existing lifecycle fields and expose no session/tool-call/approval or raw System name; notifications and workflow participants use `WiseEff Agent` or `WiseEff System service|job` coarse labels. Full correlation remains in internal domain/audit evidence.
- Critical sensitive refusal is ordered after exact identity/rule matching but before capability checks and any domain/object write. Both capable and incapable Agent/System invocations receive `403 parameter-sensitive-node-human-required`, `requireHuman: true`, and durable root-owned refusal evidence; incapable User retains the existing missing-capability error and no human-required refusal. Refusal survives an outer transaction rollback. Success audit and domain rows remain one database transaction; injected audit failure rolls them back. Object-store puts that precede a later database failure are reported only as unreachable orphan blobs, not as cross-store atomic rollback.
- The owned PostgreSQL Green matrix covers topology enablement, property-key cutover (critical second location), semantic writeback, retained legacy writeback, enablement writeback, and software merge with User/Agent/System, missing/malformed/empty-trace/forged-sink/cross-user/cross-Organization cases, divergent locked/current compatible versions, missing exact child, persisted compatible NULL, safe locked/critical current reverse cases, notification projection, refusal persistence, and audit rollback. Focused verification after the repair passed 15 files / 216 tests before documentation; the exact final counts and all static gates are recorded at final handoff.
- The final repair also removes the unused accountable-user helper and centralizes the internal row attribution shape in `TrustedInvocationDomainAttributionRow` / `trustedDomainAttributionFromRow`; public projections remain separate. The generated schema artifact records migration 0122. This repair does not modify the original dirty worktree, public actor selectors, frontend, #615, TD-123, DTS reload, HDC, hardware, live-provider, or Hosted CI surfaces. GitHub Actions quota is exhausted; no Hosted result is claimed.

### Final rebase blocker (2026-08-28)

- The final fetch observed `origin/main@551eec66c9ffacca668ba8c579857dddadf71e5d`. The clean branch rebased without semantic production conflicts (the generated-schema header and an unrelated notification helper were reconciled), ending at `ebfbb2d61574f4dbeea58748655661ada04ef714` and `ahead 41 / behind 0`.
- That main commit adds `server/migrations/0116_node_write_observation_outcomes.sql`, while the existing #614 history necessarily retains `0116_parameter_execution_provenance.sql`. The repository's real migration-numbering test now fails with the exact duplicate prefix `0116` (1 failed / 3 passed), and the full server suite consequently fails 1 test / 2,830 pass in 364 files. The direct reproduction is deterministic on the rebased tree; no `src/**` diff is involved.
- The task explicitly forbids modifying or renumbering existing migrations `0116`–`0121`, forbids absorbing the unrelated debugging/TD-123 migration, and forbids weakening the uniqueness invariant. Therefore this is an owner-level migration-history blocker, not a safe #614 repair. No final dual-axis review or Hosted-CI pass is claimed until the owner supplies a coordinated numbering decision (for example, a repository-wide migration renumbering plan that preserves applied checksums).
