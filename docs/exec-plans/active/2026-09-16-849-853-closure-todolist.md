# Issues #849/#853 closure todo list

> Chinese: [Chinese](../../zh-CN/exec-plans/active/2026-09-16-849-853-closure-todolist.md)

Status: active; the user confirmed P0 and authorized T0.6 on 2026-09-16. T1.1 and later work remain unstarted in this execution sequence.
Baseline: `origin/main@4010a600fbc6108ce84860d053eb6d88d7d484cc`, verified 2026-09-16.
Contract: [#849](https://github.com/tzrea1-Q/WiseEff/issues/849). Authoritative execution status: [#853](https://github.com/tzrea1-Q/WiseEff/issues/853). This file is its detailed execution order and acceptance map; update both language versions with the Issue after each delivered todo.

## Execution agreement

The user amended this program on 2026-09-16: browser acceptance uses **one PC viewport, 1440x900**. This replaces every prospective three-viewport requirement in #849/#853 and their plans, including specialized browser matrices. Historical tablet/mobile results remain historical evidence. All route, actor, operation, keyboard/focus, screenshot/snapshot, console/network, and S1/S2 requirements remain. Viewport-specific automated cases and coverage wording are aligned in T0.6; unrelated programs' tests are outside that change.

The main agent owns integration and executes one todo at a time. On completion, report the actual candidate/commit/PR state, checks, artifacts and remaining limits, then **wait for the user's confirmation before starting the next todo**. Confirmation is recorded separately from implementation or merge completion. Do not start the next todo's implementation in a background worker while waiting.

Every delegated worker, including independent reviewers, uses **`gpt-5.6-luna` with `reasoning_effort=xhigh`**, the available maximum for that model. Verify the spawn result; report an unavailable model instead of silently substituting one. Workers receive a bounded outcome, owned paths, accepted contract and stop boundary; only the main agent publishes/merges within the user's authorization. Required Spec/Standards reviews remain independent of the implementer.

T0.1–T0.5 are delivered via #869, #870, #872, #876 and #875, with supporting main-red repair #873. Their evidence remains bound to their original candidates. They are regression obligations, not work to recreate. #847 is closed; integrate its existing workspace. #824 remains a partial Draft and is not inherited as readiness. TD-124's eight YAML/TOML/ENV items remain deferred; explicit format refusal and vendor YAML metadata support remain in scope.

## Ordered checklist and completion criteria

Each unchecked row is one confirmation checkpoint. Existing implementation is inspected and reused; only gaps are developed. Within a row, implementation, relevant testing, independent review where required, and evidence recording form one deliverable. A local candidate is recorded as such until its PR/Hosted/merge obligations are met.

- [x] **P0 — Create the complete todo list and record the PC amendment.** Publish matching English/Chinese lists, synchronize the user amendment to #849/#853 and link the active plans. Check documentation and the diff; report this preparation for confirmation. Does not close T0.6.
- [ ] **T0.6 — Reconcile current documentation and acceptance definitions.** Audit the three active documents against latest main and merged PRs; resolve contradictory B1/B4/B5, source, archive, S2 and Hosted statements while retaining dated evidence. Align program-owned viewport loops, requirement/operation IDs and bilingual coverage maps to PC-only; run affected checks plus `docs:check` and `git diff --check`. Refresh migration/ADR/TD allocation before later schema work (0150 already exists). Keep the update gate active for subsequent todos.
- [ ] **T1.1 — B1 source identity and complete DTS/JSON workflow.** Threat matrix and independent Spec review precede schema implementation. Add the decided source-occurrence identity, backfill existing DTS bindings without changing their IDs, cover observations/matches, `resolve_current_binding()` and `protect_binding_identity()`, then prove equivalent transitional reads and switch. Keep revision pins in ProjectValue; verify SQL/ACL and trigger ordering, retain `logical_node_id` until the full matrix passes, and keep JSON ingest owned by `parameter-files` and DTS ingest by `parameter-topology`. Prove instance/config-set/file isolation and exact JSON Pointer/DTS locators, then import preview → candidate → real draft → submit/review/apply → source reparse → export/reimport through real auth, dedicated PostgreSQL and source storage. Reject duplicates, ambiguous/unsupported syntax, missing/stale locators and unsafe writes; preserve typed scalars, decimals, arrays/cells, literal dotted/slashed keys, repeated nodes and unrelated source content. Disclose serialization changes; retain explicit deferred-format refusal.
- [ ] **T1.2 — B4 capability v4 and charging_core subject.** Complete the R3 threat matrix and independent Spec review before capability implementation. Preserve nested/recursive arrays, mixed item schemas, array descriptions and metadata-defined min/max cardinality; enforce depth/container budgets and fail closed on unknown keywords. Publish the reviewed NodeType subject. Keep v1/v2/v3 interpretation unchanged and prove v3 consumers reject v4 before writes. Do not infer constraints from example/observed rows or flatten cell matrices. Run compiler, admission, runtime, historical-release and real PostgreSQL checks with independent review.
- [ ] **T1.3 — Complete successor, reviewed sources and B2 materialization.** Record each input's source digest, exact locator, old/formal identity, preserve/transform/merge/exclude disposition and predecessor lineage. Explicitly exclude structural/ambiguity fixtures; unresolved current-scope fields/identities or unexplained omissions block release, and TD-124 items create no active bindings/values. Account for all 125 inputs: 113 vendor plus four current DTS/JSON inputs and eight TD-124 deferrals. Preserve semantic fields, provenance and each project's initial/recommended values. Resolve every board business occurrence against reviewed real declarations; reconcile the historical dangling-target count discrepancy with measured identities. Carry ConfigurationSchema through `buildCompleteSuccessor`, retire acme subject/alias/definition selection without reassigning identity, and preserve releases/receipts. Obtain reviewed `csub_drv_sc8562` Driver placement capacity; B6 must still fail closed when unavailable. Missing or ambiguous project ownership blocks the target plan. Preserve the existing review/authorization path, initialization locks and one-in-flight semantics; do not initialize through direct SQL or invented approvals. Capture exact non-parameter identities/fields/relationships/shared-object checksums before and after. Through the real publisher/installer and initialization owners, prove exact Binding identity sets **124 per Atlas/Aurora/Nebula, 372 total**. Prove determinism, order independence, no unexplained omission, completed replay as a no-op, no reset after ordinary startup/upgrade/publication, and custom-project/non-parameter preservation. Local initialization is not target execution.
- [ ] **T2.4 — Real DTS toolchain.** After T1.3, validate every final demo baseline with real `dtc` and `fdtoverlay`; remove persistent dangling-anchor stubs and prove source/subject ownership. Keep demo labeling and exact tool/artifact evidence. Fix production source defects before later UI or target qualification.
- [ ] **T2.1 — Complete parameter UI on canonical data.** After T1.3, integrate #847 and the canonical pending-draft adapter; verify creation/edit/removal/reload, reason/eligible-assignee selection, reject/withdraw/resubmit, actor separation, review/apply, source diff, history/compare/baseline/export and reimport. Include definition/module navigation, search/filter/pagination/counts, registration, initialization and honest empty/error states. Exercise all required actors and DTS/JSON operations at PC 1440x900 with real services, snapshots/screenshots, keyboard/focus and console/network evidence. Preserve archived notice dismissal/non-editability, authorized gone and scope-hidden 404 without archive leakage. Remove misleading unmatched-import preview outcomes through the existing governed observation/authoring path.

Complete the eleven T2.2 children serially after T1.3. Each includes runtime routes, clients, jobs/scripts and mock ports in that family; classify references as canonical current, exact canonical history or archived notice. Use the shared ratchet, repair behavior before retiring its allowance, run affected tests and PC checks for visible changes, and record the measured reduction. The historical 3513 allowance count is a baseline to remeasure, not evidence that every allowance is an active production call.

- [ ] **T2.2-CGH — Catalog/governance consumers:** registration, lifecycle, definition identity, search/details/counts and publication-facing references.
- [ ] **T2.2-TOP — Topology consumers:** exact binding/source occurrence ownership and separation of structural/node-enablement intent from value drafts.
- [ ] **T2.2-PRJ — Project consumers:** initialized/uninitialized/custom project states, configuration instances and canonical values.
- [ ] **T2.2-FIL — File consumers:** candidate/source/version/config-set membership, canonical history, comparison, baseline and export.
- [ ] **T2.2-AGT — Agent consumers:** canonical reads and governed drafts, trusted invocation/provenance, tenant/project authorization and human approval; stale checkpoints/tool arguments cannot revive legacy writes.
- [ ] **T2.2-LOG — Log consumers:** recommendations use canonical current references; preserve historical analysis records and archived-reference rendering.
- [ ] **T2.2-DBG — Debug consumers:** observations remain separate from configured values; exact bound identities and approved promotion create canonical drafts.
- [ ] **T2.2-DTS — Reload consumers:** real bound handoff, selection, verify, promotion and residue cases replace skipped placeholders; retain canonical binding/source pins and keep software files out of device-overlay paths.
- [ ] **T2.2-KNW — Knowledge consumers:** canonical current references, preserved historical records, authorized notice-only old links and cross-organization non-disclosure.
- [ ] **T2.2-MOD — Module consumers:** explicit placement and subject kind, registration/capacity behavior and canonical module search/counts.
- [ ] **T2.2-OPS — Operations consumers:** scheduled/background/script and restart paths use canonical owners and cannot reseed or resurrect archived state.
- [ ] **T1.4 — Final legacy cutover and T2.2 aggregate gate.** After all eleven families, prove zero remaining legacy allowances with no checker weakening or hidden references; remove fallback/mixed read/dual-write and TD-125 only after successor contracts are met. Verify direct production-role, HTTP and worker denial of legacy current reads/writes. Fence only affected stale drafts, approvals, memberships, queues, caches and checkpoints at the rebuild epoch; preserve unrelated jobs/history and reject stale replay after restart.
- [ ] **T2.3a — Reviewed archive disposal and recovery decision.** Enumerate parameter-owned relations, FK/trigger/view closure, source bytes, embedded historical references, minimal stubs and preserved/shared exceptions. Reconcile archive graph/counts/bytes/digests and define online residue checks, archive custody/retention/authorized retrieval and whole-state recovery, including post-cutover writes. Independently review the exact deletion scope and recovery evidence; present the concrete destructive action for separate human approval before executing it.
- [ ] **T2.3b — Implement and verify approved disposal.** Follow T2.3a's disposition; integrate verified capture-before-delete and repeatable/resumable phase records. Prove refusal on corrupt/missing archives and source/object failures, no dangling references or cascaded non-parameter deletion, and no unauthorized online old payloads. Destructive execution requires the approval above; retained historical/shared exceptions stay explicit and offline archives are retained.
- [ ] **T3.1 — Complete S1 and full server gate.** On the integrated candidate, cover independent authenticated sessions, stale value/definition/source pins, draft/current separation, truthful partial batch results, forbidden self/unauthorized approval, uncertain-commit response loss and idempotency. Inject object preparation failure, DB/audit failure after object persistence and missing referenced objects; verify coherent current pointers/history/workflow/audit and collectible orphan blobs across restart. Exercise the real publication manager/installer. Run complete `test:server`, required build/contracts/schema/ratchet checks and record exact pass/fail/skip counts.
- [ ] **T3.2 — Complete local acceptance suites.** Execute quality, smoke, local-non-HDC, target-synthetic-acceptance and minimal-upgrade suites on the candidate; run their existing required Gate0/runtime-ownership, requirement-coverage, operation-evidence and artifact-safety checks; implement missing required scenarios rather than treating skipped placeholders as success. Use PC 1440x900 where applicable and preserve all non-viewport assertions. Own the frontend/API processes, use free permitted ports and a dedicated lane DB; do not reuse an unrelated mock service. Record commands, roles, artifacts and any unavailable target/hardware proof separately.
- [ ] **T3.3a — Complete S2 controller and Docker rehearsal.** Close gaps in the existing plan/execute/inspect/recover and export/import rehearsal entries. Pin target/database/image/schema/release/seed/scope/archive identities; reject drift and incomplete inventory. Demonstrate real traffic/write fencing, worker/queue drain, publication freeze and verified PostgreSQL/object-store/Redis recovery points. Publish only with exclusive authorized temporary unfreeze, verify activation receipt and refreeze on success/failure. Inject interruption at every durable phase; prove continuation or isolated recovery-required state and full restore, non-parameter preservation, safe reopening and sanitized output from both supported working directories.
- [ ] **T3.4a — Freeze and independently review one candidate.** After T3.1/T3.2/T3.3a, bind S1/S2, schema, seed, release, browser, consumer and recovery evidence to one exact candidate and actual environment. Run independent Standards and Spec reviews, the final seed/release fixture review and seal checks. Prepare the reviewable target plan and restore evidence; any later change invalidates affected evidence and requires resealing.
- [ ] **T3.3b — Authorized target rehearsal/execution evidence.** Use the sealed candidate and concrete target plan; obtain the separate target/destructive authorization when required. Run the agreed target quiescence, archive/rebuild, preservation and post-restart probes, documenting target-specific results and recovery boundaries. A local Docker or synthetic pass does not complete this item. Without target authority/environment, keep it open and report that blocker.
- [ ] **T3.4b — Integration PR, Hosted and merge.** Reconcile the exact candidate with latest main, recheck shared identifiers and rerun affected gates after rebase. Complete PR/Hosted/merge with evidence bound to the actual integration candidate; repeat relevant target qualification if integration changed its runtime or migration semantics. Preserve required independent reviews and record all skipped conditions.
- [ ] **T3.5 — Final documentation, receipts and Issue closure.** Refresh both-language plans/matrix/report, coverage and runbooks; run final docs/diff gates. Verify PR state, merge SHA, branch cleanup, latest main and clean designated worktree. Reconcile every #849 story/implementation/testing decision against this checklist and evidence, including partial/skipped/target boundaries. Close #853 first only when all work is satisfied, then close #849; report both and stop for the final confirmation.

## Contract coverage and dependencies

| #849 contract | Owning todo(s) |
| --- | --- |
| Model, exact identity, source formats; implementation decisions 1–6, 11–16 | T1.1, T1.2, T2.1, T1.4, T3.1 |
| Inventory, semantic parity, real demo sources, exact seeds and initialization; decisions 7–10, 13, 17 | T1.3, T2.4, T3.1 |
| Eleven consumers, old links, preserved records and replay fencing; decisions 18–22 | T2.1, all T2.2 children, T1.4, T2.3a/b |
| Archive controller, bounded sunset exception, publication isolation and recovery; decisions 20–27 | T2.3a/b, T3.3a/b, T3.4a/b |
| Delivery, all 13 testing decisions and all 62 stories (story 60 amended to PC) | T0.6, T3.1–T3.5 and their implementation owners above |

The displayed order is serial under the user's confirmation rule. T1.1/T1.2 are technically independent but this does not authorize advancing the next todo before confirmation. T1.3 requires both and delivered B6. Final cutover requires all consumer families; disposal requires its reviewed approval; target work requires a sealed candidate and tested recovery. Final integration and closure require the complete actual evidence set.

## Git & PR Workflow

P0 branch: `codex/849-853-closure-todolist`, isolated worktree `/Users/tzrea1/Develop/WiseEff-worktrees/849-853-closure-todolist`, base above. Each later implementation starts from freshly fetched main in an isolated branch, reuses accepted program gates and records its actual base/head. The main agent integrates; workers do not open/merge PRs. Stop at every user confirmation checkpoint. P0 prepares documentation and updates the explicitly amended issue contracts; it does not perform a product, schema, deployment or destructive change.

## Documentation Impact Matrix

T0.6 execution record: branch `codex/849-853-t06-alignment`, same isolated worktree, freshly fetched main `4010a600f` plus the retained, unmerged P0 commit `aa258f289`. This is an explicit stack; do not drop the P0 documentation when preparing integration. Root `AGENTS.md` applies (no `AGENTS.override.md` or closer scoped guide in the edited directories); relevant workflow, verification and R1 protocol sections were read. The main agent reconciles the six active documents; `t06_pc_acceptance` and `t06_workbench_pc` own disjoint acceptance files/coverage metadata; `t06_status_audit` provides independent read-only review. All delegates use `gpt-5.6-luna` / `xhigh`. No application/schema/target changes or next-todo implementation are included.

| Area | Action | Owner |
| --- | --- | --- |
| Planning | Add this bilingual list and index links; link existing plan/matrix/report to current ordering and PC amendment | P0 |
| Issue contract | Amend #849 viewport and #853 execution agreement; preserve other requirements and pending state | P0 |
| Historical/current reconciliation | Resolve stale narrative, align PC test loops and requirement/operation maps | T0.6 |
| Architecture/API/schema/security/runbooks | Update the closest bilingual owners with their corresponding implementation | T1/T2/T3 |
| Final evidence | Reconcile all checklist states and archive completed plans only after actual completion | T3.5 |

## Documentation Update Gate

T0.6 bounded checks (2026-09-16): `test:scripts` for the coverage and operation-matrix checkers passed 35/35; typecheck and build passed (browser externalization/chunk-size warnings remain). Playwright `--list --reporter=list` collected 74 scoped tests in 13 specs, plus one runtime-warmup test (75 total), without executing them. A baseline-to-candidate check preserved every acceptance/operation marker in those 13 specs and confirmed file-local 1440x900 defaults. The operation matrix check passed; docs governance and diff checks passed, with the pgvector schema subcheck skipped. Full `acceptance:coverage` did not pass: a collection-only report contains unexecuted cases, and static comparison on both untouched main and this candidate finds the same two orphan map IDs (`PROJ-REVIEW-READINESS-001`, `PROJ-REVIEW-ROLES-001`). Do not erase their rows or relabel this as runtime coverage; T3.2 must reconcile that inherited gap. No browser/API/DB run, final Hosted or target proof was produced here. Independent final R1 review is recorded in the #853 completion receipt.

Run `npm run docs:check` and `git diff --check` for P0 and T0.6; record schema-subcheck skips explicitly. Later todos retain relevant native tests/build/browser and independent-review gates. Final documentation is refreshed at T3.5. Completion/confirmation receipts are recorded in #853 with the corresponding commit/PR and artifact paths.

P0 verification (2026-09-16): documentation governance passed; the database-schema subcheck was skipped because local PostgreSQL lacks pgvector. `git diff --check` passed. The English/Chinese lists contain the same 28 IDs in the same order. Both GitHub Issue bodies were read back and exactly matched the prepared updates; both remain OPEN. This is a documentation/planning result, with no new application, migration, Hosted or target qualification.
