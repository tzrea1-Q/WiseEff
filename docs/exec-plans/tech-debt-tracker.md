# Technical Debt Tracker

> Chinese: [Chinese](../zh-CN/exec-plans/tech-debt-tracker.md)

This tracker captures known work that should not live only in chat or TODO comments. Keep items short, linked, and actionable.

## Open

| ID | Area | Debt | Impact | Next Action |
| --- | --- | --- | --- | --- |
| TD-001 | Runtime | Mock mode remains intentionally available for demos and component tests. | Production behavior can drift from demo behavior if API seams and mock behavior diverge. | Keep mock/API parity tests near each workflow and never use mock runtime as a production business data source. |
| TD-003 | Contracts | M5 added a generated OpenAPI artifact/check from route metadata, but frontend clients are still handwritten. | Frontend DTOs and backend responses can drift as endpoint payloads evolve beyond route-level coverage. | Generate an OpenAPI client or add schema-level DTO validation for M1-M5 endpoints. |
| TD-004 | Generated Docs | `docs/generated/db-schema.md` is manually derived from migration. | Schema summary can become stale if migrations change. | Add a script or CI check when migrations expand. |
| TD-005 | Doc Hygiene | Some historical feature plans are now in completed plans but may not state implementation status clearly. | Future agents may over-trust old implementation details. | Review completed plans during related feature work and mark superseded sections. |
| TD-007 | Log Worker | M6.4 adds Redis/BullMQ durable queue transport and worker consumption, but target-environment queue evidence and capacity tuning are not yet recorded. | Local tests prove queue semantics, DB leases, retry, and dead-letter behavior, but production rollout still needs target Redis evidence, queue backlog monitoring, and throughput tuning. | Run `npm run queue:check` and `npm run selfhost:smoke` against the deployed self-hosted target, then carry queue metrics/alerts into M6.5 and capacity gates into M6.6. |
| TD-008 | Contracts | Log-analysis client and DTOs are handwritten. | API/frontend drift can break M2 E2E late. | Generate or validate an OpenAPI client for M1/M2 endpoints. |
| TD-009 | AI Adapter | M2 analysis is deterministic fixture/rule-driven logic. | Product behavior does not yet exercise real model latency, cost, hallucination controls, or prompt/version traceability. | Add an AI adapter boundary with golden tests and model trace metadata. |
| TD-012 | Contracts | Parameter, log, debugging, and Agent HTTP clients/DTOs are handwritten. | API/frontend drift can break acceptance late and duplicate schema rules. | Generate an OpenAPI client or add schema contract validation across M1-M4 endpoints. |
| TD-013 | Agent Approvals | M4 covers persisted approval records for Agent tools, but standalone device writes still use confirmation tokens rather than Agent approval ids. | Agent/device convergence could split approval evidence across two models. | Unify high-risk device writes with approval records when Agent-driven device tools are enabled. |
| TD-014 | Debugging Catalog | M3 seeds the debugging parameter catalog; CRUD/governance is not productized. | Operators cannot safely manage node definitions, access modes, or risk metadata outside seed scripts. | Add catalog CRUD with admin permissions, validation, import/export, and audit. |
| TD-015 | Debugging UI Runtime | API write snapshots from `/node-debugging` are not fully promoted into rollback card state. **Note (2026-07-01):** `/debugging` (parameter debugging) is retired from navigation; verify rollback via API and operation history on `/node-debugging`. | Backend rollback works, but rollback card hydration may not reflect all writes until snapshot hydration is reconciled. | Reconcile snapshot hydration on `/node-debugging`; until then verify rollback via API and operation history. |
| TD-019 | Release Operations | M5 now has a pilot readiness route and smoke command. M6.6 adds self-hosted release-gate and capacity-gate evidence writers plus release/rollback runbooks. On 2026-05-30 the local `.env` proved database/local-object-store/worker/live-Agent health, local non-HDC smoke with only `deviceGateway` blocked, local browser acceptance, production-auth API-mode E2E with static bearer injection, and a local backup/restore drill. | The code can report blocked gates correctly, but commercial pilot readiness should not be claimed from local checks alone, and local non-HDC smoke must stay explicit. | Capture target-environment staging evidence, target OIDC identity readiness evidence, HDC device-lab smoke, deployment rollback rehearsal, target capacity evidence, target synthetic evidence, and target object-store evidence; keep `docs/generated/m5-pilot-acceptance.md` and `docs/generated/m6-release-readiness.md` current. |
| TD-020 | Production Auth | M6.2 adds OIDC/JWKS auth configuration and local runtime wiring, while static bearer injection through `VITE_WISEEFF_API_AUTHORIZATION` remains only a local smoke convenience. M6.6 release readiness now consumes identity evidence as an explicit dependency. | Static local tokens are acceptable for smoke tests, but they are not a production identity lifecycle, and target environments still need provisioned users plus target OIDC evidence. | Keep open until target-environment OIDC evidence exists: discovery/JWKS, `/api/v1/me` with real target tokens, token expiry/issuer/audience negative checks, browser/runtime token handling, and redacted smoke evidence. |
| TD-021 | User Governance | M6.2 adds backend user-governance APIs and audit-oriented documentation, narrowing the gap from prototype-only role changes. | Commercial user-governance signoff still depends on full browser operation evidence that proves UI + API + DB + audit behavior in the target gate. | Keep open until `PERM-USER-MGMT-001` has updated full browser/evidence coverage with API, DB, audit, Admin success, non-Admin rejection, and self-lockout/no-final-admin assertions. |
| TD-018 | Agent API Clients | Xiaoze thread and AG-UI clients/DTOs are handwritten while TD-012 remains broad. | Thread list and AG-UI envelopes can drift from backend schemas as the surface expands. | Include Xiaoze endpoints in the generated OpenAPI client or add contract tests that validate frontend DTOs against server schemas. |
| TD-022 | Self-Hosted Runtime Evidence | M6.1 added self-hosted compose/env/proxy templates, metadata checks, and smoke runner, but no target Linux server was available in this development environment. | The repository can validate deployment shape, but a real self-hosted target must still prove live service reachability, TLS, object-store, Xiaoze LLM readiness, and pilot-readiness behavior. | Run `npm run selfhost:smoke -- --base-url <target-url>` on the first deployed self-hosted target and archive redacted evidence. |
| TD-023 | Backup/Restore Evidence | M6.3 adds S3-compatible object-store compatibility probes, backup/restore drill scripts, restore target safety checks, and local redacted evidence, but no real non-customer target restore was available in this development environment. | The repository can prove evidence shape, redaction, failed-command handling, and target safety, but cannot claim target backup/restore readiness without a real restore into isolated PostgreSQL and object-store targets. | Run `npm run restore:drill`, execute the real PostgreSQL/object-store restore, then run `npm run backup:drill` and `npm run backup:check` against the target; archive redacted evidence and set `M5_BACKUP_RESTORE_DRILL_AT` only after it passes. |
| TD-024 | Capacity Baseline | M6.6 added `npm run capacity:gate` and a k6 smoke script for health, user, parameter, and log read paths, but no non-customer self-hosted target capacity run has been recorded. | The repository can validate capacity evidence shape, but first-pilot latency/error/throughput and infrastructure thresholds are not proven. | Run k6 and collect p95 latency, error rate, throughput, CPU, memory, database connections, queue backlog, and object-store probe evidence against a non-customer self-hosted target. |
| TD-025 | Rollback Rehearsal Evidence | M6.6 defines release rollback procedure and evidence fields, but no target rollback rehearsal has been archived. | Operators do not yet have proof that the self-hosted deployment can return to a known-good state under release pressure. | Rehearse rollback on a non-customer target, including stop writes, queue drain/pause, artifact restore, optional database/object-store restore, smoke, and evidence capture. |
| TD-031 | Xiaoze LLM Env Naming | Xiaoze sole Agent cleanup (2026-06-26) kept `AGENT_API_BASE_URL`, `AGENT_API_KEY`, and `AGENT_MODEL` as the live LLM config vars documented for Xiaoze-only runtime. | Env names still read like the retired M4 provider stack and can confuse operators or docs readers. | Optional follow-up: rename to `XIAOZE_LLM_*` (or equivalent) with migration notes in env templates, self-hosted checks, health gates, and runbooks; out of scope for the cleanup plan. |
| TD-033 | Debugging Platform | Legacy tables `debugging_parameters`, `debugging_parameter_node_bindings`, and `parameter_reload_bindings` remain for audit/history after node-only runtime cutover. | Runtime and Admin UI no longer depend on them, but schema and seed scripts still write legacy rows during transition. | Drop or enforce archive-only after migration evidence is stable; track in node-only plan Phase 6. |
| TD-034 | Notifications | TopBar notification bell is a static placeholder (no handler, hard-coded red dot). Prototype `ADD_NOTIFICATION` only drives ephemeral bottom toasts. | Users cannot open an inbox; workflow events are not durable or per-user; badge misleads. | Implement `docs/exec-plans/active/2026-07-06-wiseeff-notification-center.md` Phase 1 (backend inbox APIs + TopBar panel + MVP producers). Until then, consider hiding the static dot to reduce confusion. |
| TD-035 | Parameter Import | P2 full `.dts` file parser with node-path module suggestions; P3 optional `reviewMetadata` on import batch API plus server-side DTS parse for large files. | Admins must paste DTS fragments and skip-reason audit stays client-only until follow-up ships. | Add `parseDtsFull.ts` with fixture tests; extend import batch API with `reviewMetadata` and optional server parse endpoint. See `docs/exec-plans/active/2026-07-06-parameter-batch-import-wizard.md` P2/P3. |
| TD-038 | Modules | Transitional flat `module` text columns and `project_modules` reconciliation remain after hierarchical module trees shipped. | Analytics/rollback paths may read stale string labels; per-project module rows can drift from org `parameter_modules`. | Drop transitional `module` text columns after target-environment proof; reconcile `project_modules` derivation with org tree source-of-truth; optional hierarchical dashboard hotspot rollups. |
| TD-039 | Parameter Files | **P1 structural core landed on `feat/dts-structural-model`:** `server/modules/dts/` CST parser + resolver + lossless serialize; tables `dts_nodes` / `dts_properties` / `dts_phandle_refs` (migration `0042`); upload ingest behind `DTS_STRUCTURAL_INGEST` (default on); type-normalized derived `parsed_index`; CST writeback. `/include/` remains rejected. | Transition: `(name, module)` identity fallback still used when `source_*` unbound (`identityFallbackUses`); flat `parsed_index` retained as compatibility view until config-set/baseline era (P2). Front-end still consumes derived index (structured edit UI is P3). Align remaining import-wizard DTS needs with TD-035. | P2: config set / release baseline / dtc gate / lossless export; remove or gate `(name, module)` fallback after source-path bind coverage is proven; P3 structured UI. |

## Completed

| ID | Area | Resolution |
| --- | --- | --- |
| TD-000 | Knowledge Base | Reorganized product, design, execution, generated, and reference docs into harness-style structure on 2026-05-25. |
| TD-002 | Backend | M0-M5 added the productized backend modules for parameters, logs, debugging, Agent, contracts, operations, auth, audit, PostgreSQL migrations, and runtime seams. |
| TD-006 | Log Storage | M6.3 upgraded the S3-compatible adapter seam with path-style URLs, AWS4-HMAC-SHA256-style request signing, metadata headers, and bucket/object write/read/head/delete readiness probes. Provider bucket provisioning, lifecycle/KMS policy, replication, credential rotation, and target backup/export operations remain deployment work. |
| TD-017 | Agent Provider | M5 added the live provider registry seam, provider trace metadata, health fallback handling, and safety tests. Prompt optimization and broader eval coverage remain future work. |
| TD-011 | Debugging Safety | M3.5 added `debug_device_leases`, acquisition/release helpers, and service-level conflict handling before node writes and rollback. |
| TD-016 | Commercial Readiness | M3.5 added health/readiness endpoints, production config gates, route manifest tests, object-store readiness, job/device leases, and request/audit correlation. |
| TD-010 | Debugging Gateway | M5 added an HDC adapter behind `DebugDeviceGateway`, production env gates, argv-safe command execution, timeout/stderr/nonzero normalization, and fake-runner coverage. Real hardware evidence remains a pilot/device-lab acceptance artifact. |
| TD-028 | Xiaoze HITL Resume Loop | **Closed 2026-06-24.** `XiaozeHttpAgent` bridges CopilotKit `forwardedProps.command` into AG-UI `resume: [{ interruptId, status, payload }]`, clearing `HttpAgent.pendingInterrupts` before the next run. Evidence: `npm run test:server -- agUiEndpoint`; `npm test -- src/features/agent`; `npm run acceptance:e2e -- e2e/acceptance/xiaoze-action.acceptance.spec.ts` (4/4); browser approve/reject on `/parameters` with `playwright-cli` (0 console errors); screenshot `work/ui-checks/xiaoze-td028-approve-desktop.png`. |
| TD-029 | Xiaoze Checkpointing | **Closed 2026-06-29 (P0).** Production and self-hosted deployments require `XIAOZE_CHECKPOINTER=postgres`; LangGraph checkpoint tables are ensured by `npm run db:migrate`. Local dev/tests/deterministic runs default to in-memory checkpointing. Evidence: `docs/generated/xiaoze-checkpointer-evidence.md`; `npm run test:server -- durableCheckpointer checkpointer env`. |
| TD-030 | Xiaoze Chat History | **Closed 2026-06-30.** API mode lists and persists threads via `/api/v1/agent/xiaoze/threads` and `XiaozeThreadContext`; mock mode still uses browser storage. |
| TD-032 | Debugging Platform | **Superseded 2026-07-01** by node-only debugging pivot (`docs/exec-plans/active/2026-07-01-wiseeff-node-only-debugging-platform.md`). Original TD-032 split catalog + parameter reload path is retired; `/debugging` hidden again; runtime catalog is `debug_nodes` only. |
| TD-036 | Product Feedback | **Closed 2026-07-08** on branch `feat/product-feedback`. Sidebar product feedback now persists through `/api/v1/product-feedback` with object-store image attachments, Admin triage ships at `/feedback-admin`, and documentation/contract/schema coverage is updated in this branch's commits. |
| TD-037 | Modules | **Closed 2026-07-09** on branch `feat/hierarchical-modules`. Parameter and debugging domains now use independent org-scoped module trees (`parameter_modules`, `debug_node_modules`), `module_id` FKs, subtree filters, tree-select UI, and MOD-TREE acceptance IDs. Follow-ups tracked as TD-038. |
| TD-026 | Pi Agent Expansion | **Closed 2026-06-24 (superseded by TD-027).** Pi adapter expansion was cancelled when Xiaoze became the sole Agent surface. | — | — |
| TD-027 | Xiaoze / Pi Provider | **Closed 2026-06-24 (P1).** Removed `piProvider.ts`, `AGENT_API_FORMAT=pi`, `@earendil-works/pi-ai`, Pi smoke scripts, and M4 provider registry stack. Xiaoze uses LangChain `ChatOpenAI` against `AGENT_API_*` plus orchestrator approval for mutating actions. | — | — |
