# Technical Debt Tracker

> Chinese: [Chinese](../zh-CN/exec-plans/tech-debt-tracker.md)

This tracker captures known work that should not live only in chat or TODO comments. Keep items short, linked, and actionable.

## Open

| ID | Area | Debt | Impact | Next Action |
| --- | --- | --- | --- | --- |
| TD-001 | Runtime | Mock mode remains intentionally available for demos and component tests. | Production behavior can drift from demo behavior if API seams and mock behavior diverge. | Keep mock/API parity tests near each workflow and never use mock runtime as a production business data source. |
| TD-003 | Contracts | M5 added a generated OpenAPI artifact/check from route metadata, but frontend clients are still handwritten. | Frontend DTOs and backend responses can drift as endpoint payloads evolve beyond route-level coverage. | Generate an OpenAPI client or add schema-level DTO validation for M1-M5 endpoints. |
| TD-005 | Doc Hygiene | Some historical feature plans are now in completed plans but may not state implementation status clearly. | Future agents may over-trust old implementation details. | Review completed plans during related feature work and mark superseded sections. |
| TD-007 | Log Worker | M6.4 adds Redis/BullMQ durable queue transport and worker consumption, but target-environment queue evidence and capacity tuning are not yet recorded. | Local tests prove queue semantics, DB leases, retry, and dead-letter behavior, but production rollout still needs target Redis evidence, queue backlog monitoring, and throughput tuning. | Run `npm run queue:check` and `npm run selfhost:smoke` against the deployed self-hosted target, then carry queue metrics/alerts into M6.5 and capacity gates into M6.6. |
| TD-008 | Contracts | Log-analysis client and DTOs are handwritten. | API/frontend drift can break M2 E2E late. | Generate or validate an OpenAPI client for M1/M2 endpoints. |
| TD-009 | AI Adapter | The log-analysis kernel behind `LogAnalysisAdapter` is now the P1–P3b agent loop on `main` (`docs/exec-plans/active/2026-08-12-agent-log-analysis-system.md`). Remaining gap is quality evidence: the golden set is still synthetic format-coverage cases, so real-model latency, cost, hallucination controls, and prompt/version traceability are not proven on expert-annotated logs. | Quality gate stays `inactive-pending-real-cases`. | Collect 20–50 de-identified expert cases per domain and run `logs:eval:quality` against a live model; until then do not claim production analysis quality. |
| TD-012 | Contracts | Parameter, log, debugging, and Agent HTTP clients/DTOs are handwritten. | API/frontend drift can break acceptance late and duplicate schema rules. | Generate an OpenAPI client or add schema contract validation across M1-M4 endpoints. |
| TD-013 | Agent Approvals | M4 covers persisted approval records for Agent tools, but standalone device writes still use confirmation tokens rather than Agent approval ids. | Agent/device convergence could split approval evidence across two models. | Unify high-risk device writes with approval records when Agent-driven device tools are enabled. |
| TD-014 | Debugging Catalog | M3 seeds the debugging parameter catalog; CRUD/governance is not productized. | Operators cannot safely manage node definitions, access modes, or risk metadata outside seed scripts. | Add catalog CRUD with admin permissions, validation, import/export, and audit. |
| TD-019 | Release Operations | M5 now has a pilot readiness route and smoke command. M6.6 adds self-hosted release-gate and capacity-gate evidence writers plus release/rollback runbooks. On 2026-05-30 the local `.env` proved database/local-object-store/worker/live-Agent health, local non-HDC smoke with only `deviceGateway` blocked, local browser acceptance, production-auth API-mode E2E with static bearer injection, and a local backup/restore drill. | The code can report blocked gates correctly, but commercial pilot readiness should not be claimed from local checks alone, and local non-HDC smoke must stay explicit. | Capture target-environment staging evidence, target OIDC identity readiness evidence, HDC device-lab smoke, deployment rollback rehearsal, target capacity evidence, target synthetic evidence, and target object-store evidence; keep `docs/generated/m5-pilot-acceptance.md` and `docs/generated/m6-release-readiness.md` current. |
| TD-020 | Production Auth | M6.2 adds OIDC/JWKS auth configuration and local runtime wiring, while static bearer injection through `VITE_WISEEFF_API_AUTHORIZATION` remains only a local smoke convenience. M6.6 release readiness now consumes identity evidence as an explicit dependency. | Static local tokens are acceptable for smoke tests, but they are not a production identity lifecycle, and target environments still need provisioned users plus target OIDC evidence. | Keep open until target-environment OIDC evidence exists: discovery/JWKS, `/api/v1/me` with real target tokens, token expiry/issuer/audience negative checks, browser/runtime token handling, and redacted smoke evidence. |
| TD-021 | User Governance | M6.2 adds backend user-governance APIs and audit-oriented documentation, narrowing the gap from prototype-only role changes. | Commercial user-governance signoff still depends on full browser operation evidence that proves UI + API + DB + audit behavior in the target gate. | Keep open until `PERM-USER-MGMT-001` has updated full browser/evidence coverage with API, DB, audit, Admin success, non-Admin rejection, and self-lockout/no-final-admin assertions. |
| TD-018 | Agent API Clients | Xiaoze thread and AG-UI clients/DTOs are handwritten while TD-012 remains broad. | Thread list and AG-UI envelopes can drift from backend schemas as the surface expands. | Include Xiaoze endpoints in the generated OpenAPI client or add contract tests that validate frontend DTOs against server schemas. |
| TD-022 | Self-Hosted Runtime Evidence | M6.1 added self-hosted compose/env/proxy templates, metadata checks, and smoke runner, but no target Linux server was available in this development environment. | The repository can validate deployment shape, but a real self-hosted target must still prove live service reachability, TLS, object-store, Xiaoze LLM readiness, and pilot-readiness behavior. | Run `npm run selfhost:smoke -- --base-url <target-url>` on the first deployed self-hosted target and archive redacted evidence. |
| TD-023 | Backup/Restore Evidence | M6.3 adds S3-compatible object-store compatibility probes, backup/restore drill scripts, restore target safety checks, and local redacted evidence, but no real non-customer target restore was available in this development environment. | The repository can prove evidence shape, redaction, failed-command handling, and target safety, but cannot claim target backup/restore readiness without a real restore into isolated PostgreSQL and object-store targets. | Run `npm run restore:drill`, execute the real PostgreSQL/object-store restore, then run `npm run backup:drill` and `npm run backup:check` against the target; archive redacted evidence and set `M5_BACKUP_RESTORE_DRILL_AT` only after it passes. |
| TD-024 | Capacity Baseline | M6.6 added `npm run capacity:gate` and a k6 smoke script for health, user, parameter, and log read paths, but no non-customer self-hosted target capacity run has been recorded. | The repository can validate capacity evidence shape, but first-pilot latency/error/throughput and infrastructure thresholds are not proven. | Run k6 and collect p95 latency, error rate, throughput, CPU, memory, database connections, queue backlog, and object-store probe evidence against a non-customer self-hosted target. |
| TD-025 | Rollback Rehearsal Evidence | M6.6 defines release rollback procedure and evidence fields, but no target rollback rehearsal has been archived. | Operators do not yet have proof that the self-hosted deployment can return to a known-good state under release pressure. | Rehearse rollback on a non-customer target, including stop writes, queue drain/pause, artifact restore, optional database/object-store restore, smoke, and evidence capture. |
| TD-031 | Xiaoze LLM Env Naming | Xiaoze sole Agent cleanup (2026-06-26) kept `AGENT_API_BASE_URL`, `AGENT_API_KEY`, and `AGENT_MODEL` as the live LLM config vars documented for Xiaoze-only runtime. | Env names still read like the retired M4 provider stack and can confuse operators or docs readers. | Optional follow-up: rename to `XIAOZE_LLM_*` (or equivalent) with migration notes in env templates, self-hosted checks, health gates, and runbooks; out of scope for the cleanup plan. |
| TD-033 | Debugging Platform | Legacy tables `debugging_parameters` and `debugging_parameter_node_bindings` remain for audit/history after the node-only runtime cutover. `parameter_reload_bindings` was **dropped** in migration `0037` and must not be described as live schema; `/debugging` parameter-reload stays product-offline and is distinct from DTS reload (`/dts-reload`). | Operators/agents can confuse the retired parameter-reload concept with DTS reload, or plan drops against a table that no longer exists. | Keep archive-only treatment for the remaining legacy catalog tables after migration evidence is stable; never revive parameter-reload HTTP or its binding table. |
| TD-038 | Modules | Transitional flat `module` text columns and `project_modules` reconciliation remain after hierarchical module trees shipped. | Analytics/rollback paths may read stale string labels; per-project module rows can drift from org `parameter_modules`. | Drop transitional `module` text columns after target-environment proof; reconcile `project_modules` derivation with org tree source-of-truth; optional hierarchical dashboard hotspot rollups. |
| TD-039 | Parameter Files | **Program body closed 2026-07-15 (P0–P3.1 archived).** Hardening closeout archived 2026-07-16. Landed: CST parser/resolver/serialize; structural tables; config sets/baselines/gate; sensitive-node rules; structured read/search HTTP; frontend `DtsStructuredRepository` + structured UI; `submitStructuredEdits` writeback (TD-041 closed); Port-injected legacy file/conflict panels; `DTS_IDENTITY_FALLBACK_MODE`. **Superseded for path-derived identity retirement** by `docs/exec-plans/active/2026-07-16-parameter-topology-schema-management.md` (semantic specs/bindings + atomic cutover). | Residual `(name, module)` fallback remains only until cutover retires it; flat `parsed_index` is compatibility-only pre-cutover. | Track fallback removal against the topology cutover plan; do not reopen a separate TD for path identity. |
| TD-040 | Parameter Files / DTS Gate | P2 follow-ups: (1) **CLOSED**; (2) optional dt-schema hook landed; (3) container sandbox deferred; (4)/(0046) cascades resolved. **Production fail-closed schema + toolchain validation** is owned by `2026-07-16-parameter-topology-schema-management.md` (Task 8/10/17 metrics+alerts). | Pre-cutover optional schema remains env-gated; host-`dtc` blast radius accepted under current threat model. | Follow topology plan for release-mode fail-closed publish; revisit container sandbox only if threat model changes. |
| TD-043 | DTS Toolchain UX | Parameter-maintenance program demoted `dtc` from edit/merge hot path and workbench governance. Optional Admin “full toolchain validate” UI (dedicated panel beyond existing revision validate) is not built; `dts:seed:generate` still optionally uses `synthetic-power-base.dts`. | Operators lack a first-class Admin surface for intentional L2 assist; generate CLI may confuse SoT. | Add optional Admin L2 panel when needed; keep generate legacy-only or delete after boards are permanently hand-maintained. |
| TD-042 | Parameter Identity Cutover | Phase 7 fixed read-only dry-run, semantic matching (incl. inferred draft specs), cutover dropping active↔legacy PPV FKs, and post-cutover API smoke on temp DB. Local dirty shared DB dry-run now maps 193/193 defs + 557/557 values with blockers=[]. Cutover-workflow review (Tasks 1–10) landed post-cutover semantic activity paths, honest inferred counters, and topology acceptance without DB bypass. **Round 4** landed valid vendor dt-schema, durable stage/finalize, exact occurrence writeback, scope/manifest gates, and golden counts. **Round 5** landed immutable base/candidate binding revisions and fail-closed merge dependencies. **Round 6** adds evidence-only scope reconcile, lossless manual identity, global authz, full valueShape, real-role UI/merge acceptance, tenant cleanup/test isolation, all-origin candidate-less draft invalidation, durable set/delete, and migration 0063: submission locks/promotes and persists the exact candidate on item/request, while merge locks and revalidates it. | **No legal clean non-customer snapshot + maintenance window** was available, so apply → check → cutover → whole-DB restore → old API smoke was **not** executed on a production-like snapshot. The marker-verified disposable post-cutover DB is synthetic and not a restore rehearsal. | **BLOCKER:** Do not claim production cutover ready until a clean snapshot rehearsal completes per `docs/runbooks/parameter-identity-cutover.md`. Round 4–6 fixes are implementation evidence only. |
| TD-048 | Parameter specs | `parameter_spec_versions.version` is unique per definition and bindings point at a version row, but activation updates in place so every definition has exactly one version numbered 1 (`service.ts:574-591`). The mock runtime bumps `version` on activate and therefore disagrees with the API. | "Deprecate the old version, keep the new" is inexpressible; a semantic edit to an active definition leaves no version trace; mock and API demo different histories. | Decide the versioning model per D1/D2 in `docs/design-docs/2026-07-30-parameter-governance-deferred-questions.md`, then close the mock divergence in the same round. |
| TD-049 | Parameter specs | Eight ranking queries order spec versions with `case when lifecycle = 'active' then 0 else 1 end`, which places `deprecated` and `draft` in one bucket even though only `draft` is non-releasable (`schemaLoader.ts:189-195`). | A non-releasable draft can outrank the deprecated definition a binding actually uses, purely on version order. | Read each call site in context and decide between `active → deprecated → draft` ordering and selecting on releasability; see D6 in the deferred-questions doc. |
| TD-050 | Parameter specs | Migration `0068` bulk-deprecated the `status` property cohort to remove node enablement from the definition library (ADR-0003), but deprecation means "no longer recommended", not "not a definition". Those rows still parse. | The product has no marker for "structural, not semantic", so the next structural property will borrow deprecation again. | Decide whether a distinct non-parameter marker is needed; see D7 in the deferred-questions doc. |
| TD-051 | Parameter topology | Resolving an identity mapping task rewrites binding identity through `applyReviewedIdentityMapping` with no inverse, so resolving to the wrong logical node is unrecoverable except by uploading a new DTS. | An Admin mistake on the mapping queue is permanent for that revision. | Decide between an inverse remap and idempotent re-resolution; see D3 in the deferred-questions doc. |
| TD-052 | Module attribution | `aggregateSubtreeParameterCounts` rolls one `parameterCount` up the tree, but definition count (distinct definitions) and measured-occurrence count (bindings) diverge now that driver groups hold definitions directly, and deprecation adds a third count on the same surfaces. | Tree and library numbers can be read as the wrong fact. | Split the counts per D4 in the deferred-questions doc (carried over from ADR-0010 out-of-scope). |
| TD-053 | Driver schema overlays | Retiring an overlay withdraws it from the registry with no successor requirement, and a `superseded` organization row has no defined presentation on the organization's own governance surface. | Parse coverage can be dropped by one click; contributors whose overlay was promoted see it disappear without explanation. | Decide D5 and D8 in the deferred-questions doc. |
| TD-056 | Parameter files | Version history on `/parameter-admin/projects/:id/files` now shows number, origin, time, operator and a per-version download (POD-C6), but there is no rollback-to-version: `ParameterFileRepository` has no "make this version current" operation, and the operator is a raw user id because `ProjectParameterFileVersion.createdByUserId` is all the port carries. | An operator can read history and download an old version, then has to re-upload it by hand to go back; the "operator" column is an id rather than a name. | Launch closeout Batch 2 in `docs/exec-plans/active/2026-08-17-launch-actionable-tech-debt-closeout.md`. Add a rollback/promote-version operation to the parameter-files API and port, and resolve `createdByUserId` to a display name, then extend the version list in the same round. |
| TD-057 | Parameter files / governance gate | `ConfigSetBaselinePanel` no longer validates the invented `revision-teaching-1` id (POD-C1), so the revision validation action is withheld on the project operations surface: nothing there selects a config revision, and mock `getTopology` echoes back whatever revision key it is given. | The revision gate — including its `requiresConfirmation` block on release — can only be exercised from the workbench, not from the surface that publishes baselines. | Give the config-set view a real revision source (list/select project config revisions through the topology seam), then restore the gate entry against that id. |
| TD-059 | Frontend dialogs | `ModalDialog` / `ConfirmDialog` (`src/components/common/`) own the modal contract, and `ParameterSpecDetailDialog` already imports `ModalDialog`. Remaining `*Dialog.tsx` surfaces still on Radix `Dialog` or `WorkbenchSheet`: `DtsBindingDraftDialog`, `DtsBindingDetailDialog`, `DtsBindingCompareDialog`, `DtsBindingHistoryDiffDialog`, `DtsNodeEnablementDialog`, and `DtsReloadCandidateEditDialog`. | Unmigrated surfaces can still drift from the focus, stacking, and Escape contract (POD-F1–F5). | Migrate leftover dialogs onto the primitive when those surfaces are touched. Do not start from `ParameterSpecDetailDialog`. |
| TD-062 | PCW shell stretch | Wave-3 (#273–#278) met the soft gate (`ProjectConfigurationWorkbench.tsx` = 1496 LOC) and closed #258. The stretch target of **800–1000** orchestration-only lines remains open. | Further thinning needs more presentation/orchestration splits without changing product behaviour. | Optional follow-up only; do not reopen #258. Prefer extracting remaining bootstrap effects / MainStage bindings when touching the shell again. |
| TD-055 | Parameter specs / policy | Definition editor and `PATCH /api/v2/parameter-specs/:specId` no longer accept `policyTarget` (SE-D1). `parameter_policy_targets` remains keyed by `(organization_id, parameter_spec_id, product_code)` with readers in spec detail, editService, and Agent perception tools, but **no production writer**. | Operators cannot set org/product policy targets; initialization still prefers `policyTarget ?? schemaDefault` when a row exists. | Build a product-scoped policy-target governance surface (or explicitly retire the table and readers in a dedicated ADR). |
| TD-063 | DTS reload / Product | Promoting a proven debug value from a reload run into a governed parameter-library change request is out of scope for `/dts-reload` (ADR-0019). | Hardware-proven values still require a separate, manual path into the library, so experiment→library traceability is incomplete. | **Owner: Product / Debugging platform.** Design a governed promotion flow that creates a change request from reload-run evidence without writing debug values directly into bindings. |
| TD-064 | DTS reload / Frontend | Parameter workbench hand-off that carries a selected binding set into `/dts-reload` was deferred. | Engineers re-find parameters by hand between `/parameters` and `/dts-reload`. | **Owner: Product / Frontend.** Add a hand-off deep link once the standalone reload surface remains stable. |
| TD-065 | DTS reload / Platform | Value shapes beyond the supported set (u32/u8/u16 cells including `/bits/ 8`, single strings, string lists, GPIO-style phandle-cells) — booleans, empty properties, bare phandle lists (e.g. `<&gic>`), mixed values, and property deletion — remain refused. | Some library parameters stay not-debuggable for shape reasons even when paths resolve. | **Owner: Debugging platform.** Widen shape support ticket-by-ticket with overlay + preflight fixtures; do not silently guess encodings. |
| TD-066 | DTS reload / Ops | Artifact retention cleanup beyond the 90-day download `410` gate (`reload-artifact-expired`) is not automated (object-store blob deletion / GC). | Expired overlays remain as storage until an ops cleanup lands; metadata stays readable by design. | **Owner: Debugging platform / Ops.** Add retention job or runbook that deletes expired overlay blobs while preserving run digests and audit rows. |
| TD-067 | Device bridge / Reliability | Multi-replica bridge connection routing: bridge WebSockets have single-process affinity, so in-request DTS reload deploy (ADR-0020) only works on the replica holding the socket. | Horizontal API scale-out can strand reload deploys or require sticky routing. | **Owner: Platform / Reliability.** Document sticky/affinity requirements for bridge sockets; longer-term consider a bridge connection router or out-of-band deploy worker that can reach the owning process. |
| TD-068 | DTS reload / Security | `assertDtsReloadHumanActor` (and parameters `SensitiveWriteActorType`) keys off a caller-supplied in-process `actorType`, not authenticated identity on `AuthContext`. An agent presenting a user HTTP token is indistinguishable from a human. | The Agent refuse gate only binds in-process callers that pass `actorType: "agent"`; it is not authenticated actor typing. | **Owner: Security / Backend.** When AuthContext gains a trusted actor type (or equivalent), wire DTS reload and parameter sensitive writes to it without inventing a divergent mechanism. Documented in `docs/SECURITY.md` (#304). |
| TD-071 | Server tests | **Bulk migration landed 2026-08-12**: 49 local auth factories across 10 modules now delegate to `makeTestAuthContext` (identities/permissions verbatim), behavior-identical ObjectStore fakes use `createMemoryObjectStore`, and 12 seed spines use `seedCoreGraph`. Remaining by design: deliberately-different fakes (failure injection/recording/custom keys), const-literal auth objects, domain-specific seed bodies, and `parameters/seedM1{BindingRevisionHistory,SemanticTopology}.integration.test.ts` whose per-test runtime sits on the 30s timeout line pre-migration (reverted; needs its own timing fix). | New suites may still copy old patterns if unaware of the shared factories. | Point new suites at `server/testing/*` in review; fix the seedM1 borderline timing (raise per-file budget or split the seed pipeline) before migrating those two files. |
| TD-072 | Server tests | **Landed 2026-08-12 evening** (test-architecture review candidate 2): the order-queued fake-DB pattern (`QueuedResult[]`) is retired from the converted modules — logs, product-feedback, dts-reload, parameter-files, parameter-modules, parameter-specs, parameter-topology, and `parameters/projectService` — on the per-worker PG harness, with guard/scoping/lease/rollback assertions upgraded from SQL-text to real state transitions. The debugging and post-split `parameters/{repository,service}` conversions were deferred to avoid colliding with the concurrently open TD-096 / database-layer-deepening C2 program, which owns those files. | Remaining SQL-text assertions live in the simple-fake suites tracked by TD-096 (`agent`, `jobs`, `audit`, `notifications`, debugging, parameters split slices); production `cutoverAwareIdentity` defensive branches for queue-fake artifacts were retired upstream by `parameterIdentityMode`. | Let TD-096 / database-layer C2 own the remaining conversions; port the PG-harness pattern from the landed modules. |
| TD-073 | Frontend tests | No shared render harness: 0 common render entry across 327 src test files, `createRepository` re-implemented 11×, and the `src/infrastructure/mock` adapters are used by only 1–4 tests each (review candidate 3). | Page-test preambles run 147–584 lines; one hardcoded label string can break 9 cases at once; ADR-0002's mock adapters earn no leverage in tests. | Introduce a render harness that assembles the App port props from the mock adapters with per-port overrides; migrate the largest page tests first. App-shell decomposition has settled. |
| TD-075 | Acceptance governance | **Phase 1 landed 2026-08-12 night** (honesty + integrity): permanent skip stubs now carry `@acceptance-planned`/`@operation-planned` markers that never satisfy coverage; the 19 stub-only requirements state `required: false` until automation lands; the coverage map is machine-reconciled against `requirements.ts` in both directions (16 missing rows added, 3 orphan IDs registered — one of which, `XIAOZE-ACTION-EDITEDARGS-001`, turned out to be fully automated but unregistered); matrix `specFiles` references must exist on disk (dead `debugging-local-bridge` ref fixed); spec collection is recursive; the browser-acceptance run enforces map reconciliation in-process. | Remaining (phase 2): the four registries are honest but still four artifacts — consolidate `requirements.ts`/`operationMatrix.ts`/markers into one typed registry and generate the map from it; bind requirement IDs to runtime `results.json` outcomes so dynamically-skipped required tests fail coverage; the three workflow state machines in `check-acceptance-state-models.ts` still re-implement production rules (only the permission model imports production code). | Phase 2 when acceptance next expands: registry unification, results-based judgment, and state-model rule imports. |
| TD-076 | Acceptance fixtures | **Phase 1 landed 2026-08-13** (branch `feat/acceptance-cast-seam`): the seven-person ChargeLab cast is one data-only module (`e2e/acceptance/helpers/cast.ts`) consumed by bearerAuth, roleFixtures, the disposable runtime, and the parameters spec; `runNpmScript` is one implementation in `helpers/database.ts` (win32 handling fixed there; 12 local copies retired; the copy in `log-analysis.acceptance.spec.ts` stays until PR #343 lands). Remaining: ~352 inline SQL sites, 6 competing auth mechanisms with divergent missing-env behavior, 5 runtime-boot implementations, `semanticFixtureCleanup`'s hand-ordered FK deletes (review candidate 6). | A mid-size spec is ~71% ceremony vs ~15% user behavior; auth semantics silently change with env vars. | Phase 2 with a browser-acceptance verification window: one auth entry with consistent failure semantics, a seeding/cleanup seam, and runtime-boot consolidation. |
| TD-077 | Frontend style tests | 17 test files make 46 `readFileSync` text assertions against `src/styles.css`, and 6 cases regex-match production `.tsx` source (review candidate 8). | Assertions bind to source text, so formatting or property reordering breaks them; jsdom cannot verify computed styles anyway. | Move style behavior checks onto the Playwright quality gates (computed style/contrast) and convert real conventions into one explicit contract check; replace source-text assertions with lint rules. |
| TD-079 | Acceptance / CI | The CI acceptance database is **pre-cutover**: the job runs migrations + seeds but never the semantic identity cutover, so `mustUseSemanticParameterIdentity` is false there and the retired legacy submission shape is still accepted. This is why TD-078 (every approved Xiaoze write failing post-cutover) stayed green in CI while failing against every post-cutover database. | CI acceptance exercises a submission mode production no longer runs; cutover-only regressions cannot surface in the shared acceptance job. Suite-internal Xiaoze post-cutover coverage now exists, but leftover PPV fixtures still block flipping that job. | Parallel branch `fix/td-079-acceptance-semantic-fixtures` owns this closeout. **Owner: Quality / Platform.** Suite-internal Xiaoze post-cutover coverage landed in `e2e/acceptance/xiaoze-action-semantic.acceptance.spec.ts`. Remaining: flip the shared CI acceptance job after leftover `project_parameter_value_id` fixtures (`parameters.acceptance.spec.ts`, `parameter-files.acceptance.spec.ts`, `dts-structured.acceptance.spec.ts`, etc.) are migrated. Do not flip CI env vars until then. |
| TD-097 | Frontend / HCI Waves 2–3 | Remaining deferred scope from the HCI trust-repair audit. **Shipped so far**: permission-denied page + error-message seam + audit search pushdown/time window/CSV export (PR #411, plan completed); review batching for the inspection stages (PR: `feat/hci-review-batching`); Chinese-labelled audit CSV export (app/kind/action/severity labels + raw kind slug column) and detail deep links (`/parameter-review?request=`, `/logs?logId=` with hydration-safe restore) on `feat/hci-audit-export-deeplinks`. **Remaining**: terminology sweep (mixed EN/zh strings, AI Chinese output — agent module), macOS shortcut conventions, a11y systemics (focus management beyond dialogs, landmark/heading audit), responsive convergence for tables/toolbars. | The remaining audited friction stays in the product. | **Owner: Frontend.** Continue slicing; keep server-side changes out of the agent module while the approval-chain program is active. |
| TD-100 | Verification / HDC | The browser walk of the approval flow completed 2026-08-13 (evidence in `work/ui-checks/td100/`): the card renders above the open chat, approve/reject clicks land, reject-with-reason resolves cleanly with zero console errors, and `XIAOZE-APPROVAL-CARD-001` now automates it. Remaining: batch high-risk device writes still lack an HDC hardware manual pass (aggregated confirm, written/skipped accounting). | Device-write evidence still depends on hardware access. | **Owner: QA.** Schedule the HDC device-lab pass with the pilot checklist. |
| TD-090 | Log analysis / Knowledge retrieval | `read_domain_knowledge` strictly restricts retrieval to the log domain's linked knowledge entries (falling back to org-wide generic retrieval only when no links exist); the plan wording allows restrict-or-boost. | Domains with sparse links may miss relevant org-wide knowledge during analysis until an admin curates more links. | **Owner: Log analysis / Knowledge platform.** Add a weighted-fusion mode (linked entries boosted, org-wide supplementary recall) if expert feedback shows the strict mode starves analyses. |
| TD-116 | Log analysis / Webhooks | **Renumbered 2026-08-16** from a duplicated TD-102 (the Agent / Xiaoze approval-execution dead-end keeps that number). Result-webhook delivery is at-most-once: the retry chain lives in-process (fire-and-forget with an in-flight set), so a crash mid-backoff loses the remaining attempts. Documented in the integration guide (webhooks are a notification channel; the REST API stays the source of truth). | Consumers relying on webhooks alone can miss terminal-state notifications after a worker crash. | **Owner: Log analysis.** If a real consumer needs stronger guarantees, persist deliveries as an outbox (reuse the notification outbox pattern) drained by the worker loop. |
| TD-117 | Parameter specs / Identity | Referenced `property_key` rename is refused (D-ID-2). ADR-0017 § Follow-up asks whether that rename should become a staged cutover job (mirroring ADR-0014 version cutover) rather than staying forbidden. Out of scope for `2026-08-04-parameter-definition-identity-correction`. | Operators who mistype a key that projects already bind must still deprecate/recreate or wait for a cutover design. | **Owner: Parameter specs.** Decide the staged-cutover shape in a later ADR/plan; do not reopen ADR-0017 or add an inline editor field until then. |
| TD-103 | Log analysis / Security | Webhook signing secrets are stored plaintext in `log_domains.webhook_secret` (HMAC needs the raw value); the API is write-only and responses/audit only carry configured-state + last four. | A database leak exposes signing secrets, letting an attacker forge webhook payloads toward consumer endpoints. | **Owner: Security / Log analysis.** Upgrade to envelope encryption (KMS) for at-rest secrets when platform key-management infrastructure exists; rotate on upgrade. |
| TD-105 | Log analysis / Ops | `log_webhook_deliveries` grows without bound: every attempt writes a row and nothing prunes them. | Delivery-heavy domains accumulate unbounded audit-adjacent rows, bloating the table and the recent-deliveries query. | **Owner: Log analysis / Ops.** Add retention (keep last N per domain or age-based cleanup, e.g. in the worker loop or a scheduled job) once real delivery volume exists. |
| TD-110 | Frontend / Runtime state | **Renumbered 2026-08-13** from a duplicated TD-108. C2 boot emptiness and `CLEAR_API_RUNTIME_DOMAIN` landed earlier. **Track A landed 2026-08-16** via #474. **Catalog honesty landed 2026-08-17** via #480. **Mock seeding landed 2026-08-17** via #486: `createPrototypeState` lives in `src/infrastructure/mock/prototypeState.ts`; `createApiInitialState` is an empty shell and does not call it; `src/mockData.ts` is a test-facing re-export. Remaining: section-state is promoted only when a page is already being touched; API boot still carries structural demo `users` until `HYDRATE_USERS`. The architecture-review program `2026-08-16-frontend-runtime-follow-up.md` is **completed** (#474–#488); this remainder is hygiene. | New list surfaces still lack a ready-made section-state pattern. | **Owner: Frontend.** Promote `idle/loading/ready/empty/error` when a page is already in the diff. Do not invent a hollow shared section-status module. |
| TD-109 | Frontend / Mock adapters | **Wave 1 landed 2026-08-16** via #475 (`mockApiError`). **Wave 2 landed 2026-08-16** via #478: identity-mapping, candidate-activation, and spec-lifecycle guards live in `src/domain/`. **DTS `/include/` envelope leftover landed 2026-08-17** via #481. **`Object.assign(new Error)` leftover landed 2026-08-17** via #483. Remaining: extract a further frontend-side machine into `src/domain/` only when a mock grows a new duplicated rule. Do not import `server/` from `src/`. The architecture-review program `2026-08-16-frontend-runtime-follow-up.md` is **completed** (#474–#488); this remainder is hygiene. | New mock write paths can still hand-copy server rules. | **Owner: Frontend.** Thin new mock adapters to domain-rules-plus-store. Do not import `server/` from `src/`. |
| TD-112 | Frontend / Tables | Aesthetics-uplift FA-13 table convergence deliberately stopped at step 1: `admin/DataTable` is the ratified list shell and `/user-permissions` adopted it (the duplicate `workbenchUi.DataTable` was deleted), but the remaining bespoke table implementations (workbench, logs, debug-admin, reload grids) still hand-roll sorting, pagination, and keyboard access. | List behavior and a11y (`aria-sort`, keyboard rows, pagination) stay inconsistent across products; every table fix must land N times. | **Owner: Frontend.** Migrate bespoke tables onto the `DataTable` shell as their surfaces are touched; new list surfaces must start on it. |
| TD-113 | Frontend / Design tokens & lint | The aesthetics-uplift P4 gates lock honest stocks, not zero. **Wave 1 landed 2026-08-14** on branch `chore/td113-token-burn-wave1`: mechanical burn of exact `:root` matches for the four ratchet rules (raw-color/font-size/z-index/shadow) across `styles.css`, `parameter-home.css`, `linear-template.css`, and `ColumnFilter.tsx` z-index; `scripts/ui-standards-baseline.json` now raw-color **1274** (−430), raw-font-size **214** (−763), raw-z-index **50** (−26), raw-shadow **142** (unchanged — inline shadows lack exact token matches). **Wave 2 landed 2026-08-15** on branch `chore/td113-token-burn-wave2`: rgba/hsla alpha literals with exact `:root` RGB matches burned to `color-mix(in srgb, var(--token) P%, transparent)` across `styles.css` and `linear-template.css` (144 replacements); exact full-value `box-shadow` token matches still none (raw-shadow **142** unchanged). Baseline raw-color **1130** (−144). **Wave 3 landed 2026-08-15** on branch `chore/td113-token-burn-wave3`: added `raw-spacing` / `raw-radius` ratchets to `npm run ui:check`; mechanical burn of exact `--space-*` / `--radius-*` px matches across `styles.css`, `linear-template.css`, `parameter-home.css`, and `xiaozeAssistantMarkdown.css` (1279 spacing + 399 radius replacements). Baseline now raw-spacing **1250** (pre-burn **2215**, −965), raw-radius **148** (pre-burn **547**, −399); raw-color **1130**, raw-font-size **214**, raw-z-index **50**, raw-shadow **142** unchanged. **Wave 4 landed 2026-08-16** on branch `chore/td113-token-burn-wave4`: exact leftover hex (26) and rgba (82) burns outside `:root` / `.dark` token blocks in `styles.css` and `linear-template.css` (pure-hex `:root` values and wave-2 `color-mix` format; `#fff` disambiguation per surface/foreground/border rules). Baseline raw-color **1022** (−108); raw-font-size **214**, raw-z-index **50**, raw-shadow **142**, raw-spacing **1250**, raw-radius **148** unchanged. `eslint.config.js` still records 294 warnings across 19 warn-level rules (unchanged this wave). Full token penetration and eslint promotion remain open. | Residual off-grid spacing/radius, unmatched rgba/hex, bespoke shadows, and eslint warn stock still hide defects on untouched surfaces. | **Owner: Frontend.** Continue wave burns on touched surfaces; ratchet baselines in the same change (`npm run ui:check -- --update-baseline`; promote eslint rules to error when they reach zero). |
| TD-114 | Frontend / Component residue | Aesthetics-uplift residue, partially closed 2026-08-14: dead `WorkspaceHeader`/`PageToolbar` deleted from `src/components/layout/` (live export is `useTopBarActions` only); `MetricBentoCard` `severity` renamed to `tone`. Remaining: two `<Button className="full">` hooks in `ParameterReviewPage` are layout-only (`.action-panel .full` grid-column) — intentionally not folded into Button variants. | Layout class overrides on review actions still bypass the variant vocabulary. | **Owner: Frontend.** Fold the two Button layout hooks into a supported width/layout prop only if a variant-free expression appears; otherwise leave documented as layout. |

## Completed

| ID | Area | Resolution |
| --- | --- | --- |
| TD-082 | Backend / HTTP errors | **Closed 2026-08-17** on `chore/td-082-apierror-status-codemod`. Mechanical AST codemod removed the ignored positional HTTP status argument from every `new ApiError(...)` call site (996 sites), then dropped the deprecated constructor parameter. `status` is derived only from `API_ERROR_STATUS`. No behaviour change. |
| TD-044 | Node enablement e2e | **Closed 2026-08-17** on `test/td044-enable-gate-toggle-guard`. `PARAM-ENABLE-GATE-001` / `TOGGLE-001` / `GUARD-001` are runnable Playwright acceptance on the disposable post-cutover topology runtime: API+DB structural-gate evidence (no `status` review task/binding; semantic counts exclude `STRUCTURAL_PROPERTY_KEYS`; cutover already finalized), disable with reason+confirm plus mixed-round submit without `mixed-working-tips` and a distinct `enablement-changed` audit, and `status=reserved` read-only plus secondary-override acknowledgement. `PARAM-ENABLE-VISIBLE-001` already landed. Product behavior remains `2026-07-27-dts-node-enablement` / ADR-0003. |
| TD-096 | Testing / Parameters | **Closed 2026-08-17** on `test/td096-dts-reload-deploy-behavior`. Converted the last remainder, `dts-reload/deploy.test.ts`, from hand-rolled fake DBs and SQL-text substring assertions (`createFakeDb` / `createDeployDb` answering `from dts_reload_runs`) to `createInMemoryTestDatabase()` behavioural tests at `deployReloadRun`, plus helper unit tests for `bridgeCanonicalDeviceId` / `computeReloadLeaseTtlMs`. Seeds use `seedCoreGraph` and `insertReloadRun` / `insertReloadRunTarget` like `history.test.ts`. Port doubles remain for bridge list, debug leases, object store, behavioural verify, and audit — those are not the database. No residual SQL-shape contract suite. Prior slices (parameters family, debugging, jobs, agent, product-feedback, logs, audit, domains, release-readiness, module attribution, driver-schema overlays) already landed. |
| TD-102 | Agent / Xiaoze UX | **Closed 2026-08-17** on `fix/td102-approval-execution-dead-end`. Approved tool execution failures halt the planning graph with Chinese assistant text (halt prefix plus mapped reason), persist via AG-UI `finalize`/`complete()` instead of `RUN_ERROR`, and leave the LangGraph interrupt so a later `run` without resume is not pending-interrupt. New conversation / thread switch calls `clearXiaozeAgentPendingTurn` to empty CopilotKit `HttpAgent.pendingInterrupts`. Browser ID `XIAOZE-APPROVAL-EXEC-FAIL-001` is an honest `@acceptance-planned` stub (`required: false`); graph + AG-UI assembly tests are the blocking gate. |
| TD-094 | Agent / Xiaoze UX | **Closed 2026-08-17** on `fix/td102-approval-execution-dead-end` (batch 2). Successful `approveToolCall` no longer `appendAgentMessage`; `persistTurn` owns the visible reply. `resolveApproval` reads the tool-result summary. Audit + tool-call status stay. Reject-path double-write is unchanged. |
| TD-034 | Notifications | **Closed 2026-08-17.** Durable inbox + APIs + TopBar panel landed (`NotificationBell`, `TopBarNotifications`, `server/modules/notifications/`, migrations `0033`/`0034`). Automated `NOTIF-INBOX-001` / `NOTIF-READ-001`. Optional leftover: `NOTIF-DEEPLINK-001` e2e (inbox already navigates via `actionUrl`; not a new TD). Plan archived at `docs/exec-plans/completed/2026-07-06-wiseeff-notification-center.md`. |
| TD-074 | Frontend architecture | **Closed 2026-08-17** by app-shell decomposition (PR #324 / `src/application/state/appState.ts`, ADR-0023). `appReducer` and `AppAction` no longer live in `src/App.tsx`; reducer suites import the state module. Remaining page-test preamble cost is TD-073. |
| TD-085 | Log analysis / UX | **Closed 2026-08-16** on `fix/td085-confidence-provenance`. Confidence **numbers stay visible**. `confidenceCaption` labels `agent` as an uncalibrated model self-estimate, `rules-fallback` as a deterministic rules-engine score, and missing/null legacy reports keep each surface's existing copy. Applied on the `/logs` conclusion card, `/log-admin` table, and `LogRecordDrawer`. Covered by `confidenceProvenance.test.ts`, `logsPage.test.tsx`, `LogRecordDrawer.test.tsx`, and `LogAdminPage.test.tsx`. |
| TD-089 | Log analysis / Eval | **Closed 2026-08-16** on `fix/td089-rubric-stub-actions` (#468). The deterministic rubric stub keeps the 0.4 coverage threshold and canonicalizes a small verb/thermal/charge synonym table on the `actionsScore` path only; root-cause overlap stays literal. Real-model judge, golden cases, and baselines are unchanged. Covered by `qualityEval.test.ts`. |
| TD-092 | Log analysis / Data model | **Closed 2026-08-16** on `fix/td092-feedback-run-id` (#471). Additive migration `0112_log_feedback_run_id.sql` stamps `log_feedback.run_id` from the log's current analysis run at insert; insights join via `coalesce(lf.run_id, lr.current_run_id)`. Backfill inherits `current_run_id` at migration time (honest, not reconstructed). List/detail still read the current run. Covered by `repository.test.ts`. |
| TD-093 | Log analysis / Intake | **Closed 2026-08-16** on `fix/td093-json-log-upload`. Promoted `.json` onto the server text allowlist (`supportedLogExtensions` is now `.log` / `.txt` / `.csv` / `.json`; UTF-8 line-oriented parse, typically JSON-lines) so API intake matches the existing client precheck instead of dropping a format the UI already offered. Mock gained `.csv` on the same text set via `src/domain/logs/uploadExtensions.ts`; archives stay API-only because only the server unpacks them. Covered by `parser.test.ts`, `uploadExtensions.test.ts`, `logRuntime.test.ts`, and `logsPage.upload.test.tsx`. |
| TD-086 | Log analysis / Frontend | **Closed 2026-08-16** on `fix/td086-log-domain-form-errors`. `LogDomainGovernanceSection` maps domain create/update API failures onto inline field alerts: `VALIDATION_FAILED` + `details.issues` (the live format-profile contract; there is no `INVALID_LOG_FORMAT_PROFILE` code) onto the profile JSON field, and `CONFLICT` with `details.name` or a blank-name validation onto the name field. `createLogRuntimeActions` skips the generic toast for those mapped errors and still notifies unexpected failures. Covered by `logDomainGovernanceError.test.ts`, `logRuntime.test.ts`, and `LogAdminPage.test.tsx`. |
| TD-087 | Ops / Self-hosted | **Closed 2026-08-16** on `fix/td087-selfhost-log-analysis-keys`. `requiredEnvKeys` now includes the P1 log-analysis LLM family (`LOG_ANALYSIS_API_BASE_URL`, `LOG_ANALYSIS_MODEL`, `LOG_ANALYSIS_API_KEY`, `LOG_ANALYSIS_API_TIMEOUT_MS`, `LOG_ANALYSIS_TOKEN_BUDGET`, `LOG_ANALYSIS_DETERMINISTIC`). The metadata check is key presence in `ops/self-hosted/.env.example`, not filled values; deterministic mode does not drop `LOG_ANALYSIS_API_KEY` from the template (empty remains valid). Live fail-closed stays `/health/ready` `logAnalysisLlm`, which already relaxes the API key when `LOG_ANALYSIS_DETERMINISTIC=true`. Covered by `check-self-hosted-config.test.ts`. Follow-on `fix/docs-check-log-analysis-llm-keys` added the same family to `scripts/check-doc-governance.ts` `requiredEnvExampleKeys` so local `.env.example` cannot drop the keys unnoticed. |
| TD-106 | Quality runtime / Ops | **Closed 2026-08-15** on `fix/td106-quality-runtime-prune`. `reset-quality-runtime.ts` now prunes `probe-edit-%.dts` parameter file versions (and dependent DTS rows) plus runtime schema-promotion demo driver groups (`FoldRegistryTestDG`, `compatible:vendor,fold_registry_test`, and the fold-registry test display-name variant) inside the same transaction before transient user deletion. Curated seed modules and modules with binding/definition references are skipped. |
| TD-104 | Acceptance / CI | **Closed 2026-08-15** on `fix/td104-acceptance-webserver-warmup`. Quality and acceptance Playwright configs now run a `runtime-warmup` dependency project that loads the SPA entry via `page.goto` after webServer ready so Vite's first transform is billed to warmup rather than the first product spec; product spec timeouts unchanged. |
| TD-095 | CI / Test reliability | **Closed 2026-08-15** on `fix/td095-checkpointer-resume-flake`. `/logs` visual flake was closed earlier on `fix/td095-logs-visual-flake` (`settleQualityRoute("/logs")` waits for seeded filenames plus the failure alert; `stableMasks` covers `.rawlog-table__time` and metadata captured-at cells). The remaining `durableCheckpointer.integration.test.ts` flake was a real interrupt-checkpoint durability race: `planningGraph.run` now polls a process-lifetime probe `PostgresSaver` pool (not a new pool per HITL) until the interrupt checkpoint is readable before returning HITL, and the integration proof resumes through a second saver instance. Evidence: `npm run test:server -- durableCheckpointer planningGraph`. |
| TD-091 | Frontend / Xiaoze UX | **Closed 2026-08-14** on `fix/td091-td107-hint-and-search`. `body:has([role="dialog"]) .xiaoze-toggle-hint` sets `pointer-events: none` and `visibility: hidden` so open Sheet/ModalDialog/ConfirmDialog surfaces no longer lose clicks to the FAB hint; the hint stays mounted so per-page "already shown" storage is untouched. Covered by `XiaozeToggleHint.styles.test.ts`. |
| TD-107 | Frontend / Debugging | **Closed 2026-08-14** on `fix/td091-td107-hint-and-search`. Debugging toolbar search sizing now uses `width: min(260px, 100%)` with `flex: 0 0 auto` instead of `flex-basis: 260px`, and the `@media (max-width: 960px)` override sets `width: 100%`, `height: auto`, and clears min/max width so column flex no longer stretches the field to 260px tall on phones. Locked by `ParametersTable.test.tsx`. |
| TD-080 | Frontend testing | **Closed 2026-08-14** on `fix/td080-topology-test-isolation`. `ApiProjectTopologyWorkspace.test.tsx` mocks `createHttpParameterModuleRegistryRepository`, `createHttpParameterRepository`, and `resolveParameterFileRepository` with empty resolved stubs plus a global fetch sentinel that fails any leaked HTTP; a regression test asserts default workspace render never calls `fetch`. Explicit per-test seams (`parameterFileRepository`, `listDrafts`, `deleteDraft`) remain unchanged. |
| TD-084 | UI primitives / Dialogs | **Closed 2026-08-14** on `fix/td081-td084-visible-defects`. Shared `.confirm-dialog:has(.confirm-dialog__scroll)` caps height to `calc(100dvh - 48px)` with `overflow: hidden` and a three-row grid so `.dialog-actions` stays pinned; `ConfirmDialog` wraps pre-footer content in `.confirm-dialog__scroll` (`overflow-y: auto`). Covered by `ConfirmDialog.test.tsx` and `ModalDialog.styles.test.ts`. Short dialogs without the scroll wrapper are unchanged. |
| TD-081 | Frontend styling | **Closed 2026-08-14** on `fix/td081-td084-visible-defects`. `.topology-tree__item small` uses the readable `var(--text-muted)` token instead of the near-white shadcn `--muted` surface alias; locked by a structural assertion in `ProjectTopologyWorkspace.test.tsx` (CSS landed earlier in #387). |
| TD-111 | Frontend / Content | **Closed 2026-08-14** on `fix/td111-error-presentation`. Routed the FA-17 residue surfaces through `presentError`/`presentErrorMessage`: knowledge admin/page and dialogs (including index `item.error` column), product-feedback admin and submit dialog, parameter-admin-next governance panels (`ProjectsOperationsPanel`, identity mapping, spec governance create/review), and parameter-topology trays/dialogs/workbench/spec detail (excluding `mapParameterTopologyError` mapped messages and structured diagnostic lines). Topology copy pass landed on `fix/td111-topology-error-copy` (`mapParameterTopologyError` now routes through `presentError` with scenario-specific Chinese fallbacks). `ErrorBoundary` dev detail and application-layer/runtime catch blocks remain outside the listed UI surfaces. |
| TD-115 | Frontend / User governance | **Closed 2026-08-14** on `fix/td115-and-td114-residue`. `formatLastActive`/`normalizeTimestampInput` now accept Postgres text timestamps (space separator, optional milliseconds, short `+00`/`+08` offsets) before routing through `formatRelativeOrAbsolute`; legacy English and already-formatted Chinese pass through; unparseable strings render the product unknown label with the raw value kept in `lastActiveTooltip`. Covered by `formatLastActive.test.ts`. |
| TD-108 | Device bridge / Security | **Closed 2026-08-13 (decision B2)** on `fix/device-bridge-health-redaction`. The 2026-08-12 audit flagged the local bridge's open CORS as a P0 candidate; investigation showed device-write authority never flows through the bridge's loopback HTTP surface (it flows through authenticated, audited server routes), so the real surface was fingerprinting + a `/health` info leak (`bridgeId`, `serverUrl`, `launcherPath`, tool state) + a CSRF-style reconnect trigger. Decision B2 (of options A keep-open / B allowlist-narrow / B2 redact / B3 env-flag): `/health` keeps open CORS so any WiseEff origin can still detect a running bridge, but now **redacts identifying fields for non-allowlisted browser origins** — only the paired origin (allowlist match) or same-machine tooling (no `Origin` header) reads identifying fields; every other origin gets `{ ok, paired, connected, updatedAt }`. `/connect` stays open by design (first-contact endpoint; already gated by a short-lived single-use pairing code unless already paired to the named server). `packages/device-bridge/src/healthServer.ts` + tests; posture recorded in `docs/SECURITY.md` (EN+zh). |
| TD-069 | DTS reload / Frontend | **Closed 2026-08-13** by #372 (`refactor/dts-reload-run-session`; plan archived at `docs/exec-plans/completed/2026-08-13-dts-reload-run-session.md`). The row's closing condition — close after the split branch merges — is met. `DtsReloadPage.tsx` dropped **2,597 → 917** lines (pure rendering layer): debug-value validation extracted to `src/domain/dtsReload/debugValue.ts` (table-driven tests), run orchestration to `src/application/dts-reload/dtsReloadRunSession.ts` (+ hook) with confirmation-token gating tests, presentation helpers colocated in `dtsReloadPresentation.tsx`, and a mock `DtsReloadRepository` adapter + runtime resolve restoring ADR-0002 mock-mode parity. Further shrink only on real need (e.g. extracting the candidates table); the confirm-dialog overflow found during its browser verification stays open as TD-084. |
| TD-101 | Acceptance coverage | **Closed 2026-08-13** on `feat/hci-trust-followups` (PR #422; this registry row was accidentally reverted during a parallel tracker merge and is restored here). The three promised ids shipped as real automation: `XIAOZE-APPROVAL-CARD-001` (browser approval card usable with the chat open), `PARAM-DRAFT-REMOVE-001` (tray removal deletes server-side and survives reload, green on the disposable runtime), and `LOG-CONFIDENCE-PERCENT-001` (confidence bar renders the normalized percent inside the LOG-HAPPY flow) — all registered in requirements.ts, the operation matrix, and the coverage map. |
| TD-099 | Frontend / Overlay tokens & tests | **Closed 2026-08-13** on `feat/hci-trust-followups`. The duplicate approval-stacking rules collapsed to the data-attribute pair (`[data-slot="alert-dialog-overlay"]:has(+ [data-testid="xiaoze-approval-card"])` / `[data-testid="xiaoze-approval-card"]`); the wave-0 `.xiaoze-approval-*` classes and their component wiring were removed, and the styles test now pins the surviving rule set plus the absence of the legacy classes. The "jsdom double-render" mystery dissolved under a dispatch-stack probe: the skipped escape-guard spec's third act still drove the old handwritten backdrop (`mouseDown` on the dialog card) — the state flip it observed was its own cancel-button click. Rewritten as a paired pointer-down/up on the ModalDialog backdrop and unskipped (23/23 green). |
| TD-098 | Frontend / Notifications | **Closed 2026-08-13** on `feat/hci-notify-convergence`. One toast renderer: `AppToastLayer` became a render-less bridge that drains the reducer `state.notifications` queue into the design-system `ToastProvider` (word-level tone inference: failure vocabulary → danger/alert, completion → success, else info) and consumes each entry via `DISMISS_NOTIFICATION`. `ToastCard` gained the labelled manual close button (a11y parity with the old layer) and the `app-toast` testid; `.app-toast` styles were deleted, `.toast-viewport` already sat on `--z-toast`; the visual-gate `settleAppToasts` helper follows the `.toast` selectors. |
| TD-083 | Knowledge retrieval / Ops | **Closed 2026-08-13** on `fix/knowledge-pgvector-td083`. A startup ensure (`server/modules/knowledge/indexing/vectorEnsure.ts`, run in `server/index.ts` before the index worker starts) late-installs `knowledge_chunks.embedding` when pgvector appears after migration `0104`: extension create + column add + full reindex enqueue under a dedicated advisory lock — concurrent replicas install exactly once (proven by a two-session integration test); extension-less servers no-op silently; privilege refusals log honestly and stay FTS-only. CI postgres service images switched to `pgvector/pgvector:pg16` (build-and-test + acceptance jobs), so `vectorSearch.integration.test.ts` and the new `vectorEnsure` suites run in CI instead of skipping; the skip-with-reason path remains for genuinely extension-less environments. Self-hosted runbook EN+zh now document restart-based auto-install with the manual SQL kept as reference. |
| TD-088 | Acceptance / CI tooling | **Closed 2026-08-13** on `feat/log-analysis-p3a-monitoring-annotation-intake` (P3a). `check-operation-evidence.ts` gained a first-class focused-run mode: `npm run acceptance:evidence -- --run <runDir> [--require <opIds>]` validates the records of one run directory (forensic metadata + declared-assertion payloads; `--require` demands explicit coverage) and writes `evidence-check.{md,json}` into the run directory; the default full-run behavior and the docs/generated index are unchanged. Documented in the verification matrix; covered by `scripts/check-operation-evidence.test.ts` focused-run cases. |
| TD-070 | Agent / Xiaoze UX | **Closed 2026-08-12** on `refactor/xiaoze-turn-stream`. The resume branch was folded into the same `xiaozeTurnStream` open/ingest/finalize path as every other turn: approved turns now stream `STEP_STARTED`/`STEP_FINISHED`, live `xiaoze_turn_state` snapshots, and streamed answer deltas, and the post-approval tool execution itself is a timeline step (`planningGraph` actNode records it). Verified against an isolated deterministic API: an approved turn shows the submit-parameter-change tool step and the reply step with phases thinking, tool, composing, done; a chained second interrupt after resume now surfaces instead of being dropped. |
| TD-004 | Generated Docs | **Closed 2026-08-12** by #329 (`refactor/db-migration-pipeline`, database-layer deepening C5). `docs/generated/db-schema.md` is now rendered by `npm run db:schema-doc` from a disposable database migrated from scratch; `npm run db:schema-doc:check` fails on drift and runs inside `npm run docs:check`. |
| TD-058 | Parameter files / conflicts | **Closed 2026-08-07** by #235 (`feat/project-configuration-workbench-conflict-arbitration`). Conflict list DTO carries `baseValue` + human `fileVersionLabel`; resolve accepts optional `reason` into audit metadata; `bulk-preview` / `bulk-resolve` land on API + port + workbench Conflicts dock with impact confirmation; acceptance `PROJ-CONFIG-CONFLICT-001`. |
| TD-015 | Debugging UI Runtime | **Closed 2026-08-05** on `feat/node-debugging-ui-closure` (C2). `/node-debugging` hydrates session events after detect, exposes rollback UI, and requires explicit high-risk write confirmation (no silent confirm token). |
| TD-078 | Agent / Parameters | **Closed 2026-08-12** on `fix/xiaoze-action-semantic-submit`. `action.submitParameterChange` now resolves the binding head revision, creates a typed binding draft (`createBindingDraft`), and submits with the draft identity + `actorType: "agent"`; failed submissions delete the agent-created draft; the raw-INSERT fallback became an explicit error. Acceptance spec dropped retired `project_parameter_value_id` predicates and resolves a seeded binding at runtime. Proven by a non-mocked pglite integration test (draft → change request; critical sensitive refusal and 404 leave no residue). CI-side environment drift split out as TD-079. |
| TD-060 | Parameter initialization | **Closed 2026-08-05** on `feat/project-parameter-initialization` (C1). Migration `0091` + draft/review APIs; semantic binding merge/materialize; Port/HTTP/mock; API hydrate + lock; design amended off flat `recommendedValue` SSOT. |
| TD-061 | Parameter admin audit | **Closed 2026-08-05** on `feat/parameter-admin-audit-hints` (C3). Removed `PUSH_AUDIT_HINT` / `auditHints`; admin recent strip is a projection of `listAuditEvents` (`parameter-management` / `parameter-admin` / `parameters`); project update/delete write `createAuditEvent` via `updateProjectForAuth` / `deleteProjectForAuth`. |
| TD-047 | Parameter specs | **Closed 2026-08-01 (PR2 / D-AG-03)** on `feat/drop-parameter-spec-driver-module`. No physical `driver_module` column; identity writes are subject-only via `buildSubjectScopedManualSpecIds`; migration `0088` fail-closes when identity-bearing rows lack `attribution_subject_id`; list filter is `attributionSubjectId`; API `driverModule` is display-only from subject. |
| TD-054 | Contracts | **Closed 2026-08-01** via #216 (`feat/governance-platform-closeout`). `parameterSpecs.listPromotionCandidates` is registered in `server/modules/contracts/schemaRegistry.ts`; `openapi.test.ts` passes (10/10). Closeout archived in `docs/exec-plans/completed/2026-08-01-governance-platform-closeout.md`. |
| TD-046 | Module attribution placement | **Closed 2026-08-01** on branch `feat/attribution-registration-placement` (D-AG-04). Migration `0089_driver_registration_default_business_category.sql` adds registration `default_business_category_module_id`; auto placement + replay APIs; keyword `businessCategoryForNodePath` demoted to seed/bootstrap-once. Plan: `docs/exec-plans/active/2026-08-01-attribution-deferred-implementation.md` PR3. |
| TD-041 | Parameter Files / DTS UI | **Closed 2026-07-15 (P3.1 on `feat/dts-structured-edit-loop`).** Wired structured edit → change set → `submitStructuredEdits` → existing CR review/merge → CST writeback; change-set rows map via `source_file_name`/`source_node_path`; CR and writeback payloads use `rawText` (not `normalizedValue`). Acceptance `PARAM-DTS-EDIT-002`; playwright-cli evidence in `work/ui-checks/p31-*`. |
| TD-000 | Knowledge Base | Reorganized product, design, execution, generated, and reference docs into harness-style structure on 2026-05-25. |
| TD-002 | Backend | M0-M5 added the productized backend modules for parameters, logs, debugging, Agent, contracts, operations, auth, audit, PostgreSQL migrations, and runtime seams. |
| TD-006 | Log Storage | M6.3 upgraded the S3-compatible adapter seam with path-style URLs, AWS4-HMAC-SHA256-style request signing, metadata headers, and bucket/object write/read/head/delete readiness probes. Provider bucket provisioning, lifecycle/KMS policy, replication, credential rotation, and target backup/export operations remain deployment work. |
| TD-017 | Agent Provider | M5 added the live provider registry seam, provider trace metadata, health fallback handling, and safety tests. Prompt optimization and broader eval coverage remain future work. |
| TD-011 | Debugging Safety | M3.5 added `debug_device_leases`, acquisition/release helpers, and service-level conflict handling before node writes and rollback. |
| TD-016 | Commercial Readiness | M3.5 added health/readiness endpoints, production config gates, route manifest tests, object-store readiness, job/device leases, and request/audit correlation. |
| TD-010 | Debugging Gateway | M5 added an HDC adapter behind `DebugDeviceGateway`, production env gates, argv-safe command execution, timeout/stderr/nonzero normalization, and fake-runner coverage. Real hardware evidence remains a pilot/device-lab acceptance artifact. |
| TD-028 | Xiaoze HITL Resume Loop | **Closed 2026-06-24.** `XiaozeHttpAgent` bridges CopilotKit `forwardedProps.command` into AG-UI `resume: [{ interruptId, status, payload }]`, clearing `HttpAgent.pendingInterrupts` before the next run. Evidence: `npm run test:server -- agUiEndpoint`; `npm test -- src/features/agent`; `npm run acceptance:e2e -- e2e/acceptance/xiaoze-action.acceptance.spec.ts` (4/4); browser approve/reject on `/parameters` with `playwright-cli` (0 console errors); screenshot `work/ui-checks/xiaoze-td028-approve-desktop.png`. |
| TD-029 | Xiaoze Checkpointing | **Closed 2026-06-29 (P0).** Production and self-hosted deployments require `XIAOZE_CHECKPOINTER=postgres`; LangGraph checkpoint tables are ensured by `npm run db:migrate`. Local dev/tests/deterministic runs default to in-memory checkpointing. Evidence: `docs/generated/xiaoze-checkpointer-evidence.md`; `npm run test:server -- durableCheckpointer checkpointer env`. |
| TD-030 | Xiaoze Chat History | **Closed 2026-06-30.** API mode lists and persists threads via `/api/v1/agent/xiaoze/threads` and `XiaozeThreadContext`; mock mode still uses browser storage. |
| TD-032 | Debugging Platform | **Superseded 2026-07-01** by node-only debugging pivot (`docs/exec-plans/active/2026-07-01-wiseeff-node-only-debugging-platform.md`). Original TD-032 split catalog + parameter reload path is retired; `/debugging` hidden again; runtime catalog is `debug_nodes` only. |
| TD-036 | Product Feedback | **Closed 2026-07-08** on branch `feat/product-feedback`. Sidebar product feedback now persists through `/api/v1/product-feedback` with object-store image attachments, Admin triage ships at `/feedback-admin`, and documentation/contract/schema coverage is updated in this branch's commits. |
| TD-037 | Modules | **Closed 2026-07-09** on branch `feat/hierarchical-modules`. Parameter and debugging domains now use independent org-scoped module trees (`parameter_modules`, `debug_node_modules`), `module_id` FKs, subtree filters, tree-select UI, and MOD-TREE acceptance IDs. Follow-ups tracked as TD-038. |
| TD-045 | Module scaffolding leftovers | **Closed 2026-07-30** on branch `feat/attribution-taxonomy-not-topology` (ADR-0010 / migration `0080`). Scaffolding modules (`i2c@…`, `pmic@0`, `batt`, `amba`, `gic`, `gpio`, `spmi`) leave the product tree; bindings re-parked on the org unclassified root. |
| TD-026 | Pi Agent Expansion | **Closed 2026-06-24 (superseded by TD-027).** Pi adapter expansion was cancelled when Xiaoze became the sole Agent surface. | — | — |
| TD-027 | Xiaoze / Pi Provider | **Closed 2026-06-24 (P1).** Removed `piProvider.ts`, `AGENT_API_FORMAT=pi`, `@earendil-works/pi-ai`, Pi smoke scripts, and M4 provider registry stack. Xiaoze uses LangChain `ChatOpenAI` against `AGENT_API_*` plus orchestrator approval for mutating actions. | — | — |
