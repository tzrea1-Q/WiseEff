/**
 * Server-side copy of src/domain/parameter-topology/modulePlacement.ts.
 * Keep in sync when placement rules change — server cannot import @/domain.
 */

export * from "../../../src/domain/parameter-topology/modulePlacement";
