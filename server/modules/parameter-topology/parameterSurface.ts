/**
 * Server-side copy of src/domain/parameter-topology/parameterSurface.ts.
 * Keep in sync when surface rules change — server cannot import @/domain.
 */

export * from "../../../src/domain/parameter-topology/parameterSurface";
