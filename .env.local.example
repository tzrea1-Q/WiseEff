# Copy to `.env.local` (gitignored) and fill secrets for live LLM debugging.
# `.env.local` overrides values from `.env` without touching committed config.

XIAOZE_LLM_API_KEY=

# Optional overrides:
# XIAOZE_LLM_MODEL=gpt-4o-mini
# XIAOZE_LLM_API_BASE_URL=https://api.openai.com/v1
