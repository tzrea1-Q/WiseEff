/**
 * Single source of truth for device-bridge HTTP API paths shared by server and frontend.
 * Keep these next to the RPC contract so path literals cannot drift across modules.
 */
export const DEVICE_BRIDGE_RELEASES_PATH = "/api/v1/device-bridges/releases";
